# Days of Memories (SNK, NDS) 한글화 가능성 분석 — 진행 노트

마지막 업데이트: 2026-08-06 (**`tool.md` 기반 Node.js 웹 툴(`webtool/`) 신규 개발 완료 + 실제 ROM으로
언팩→CSV추출→편집/검증→재삽입→패킹→다운로드→왕복무결성확인까지 엔드투엔드 검증 완료** — 이전:
테스트 결과 실제 ROM 빌드(`temp/dom_block27_test.nds`) + melonDS 확인 완료 → CSV 번역 컬럼 편집→
재삽입→패킹을 향후 정식 한글패치 워크플로우로 확정 — 이전: CSV
편집→재삽입 파이프라인 실전 테스트(블록 27-46) 완료 + `mes_translate_reinsert.py`의 디버그블록
필터 불일치 버그 수정 + 오프닝 장면 한글 글리프 111개를 `font_map_full.json`에 정식 등록 — 이전: 주인공 이름 변수 마커(`0x505C`) 확정 및 `<이름>` 전용 표기 도입 — 이전: `find_dialogue_blocks()` v8 알고리즘 확정(고정 마커 의존 제거)으로 대사 38,154개 최종 회수, 화자(speaker) ID 필드 발견 및 CSV에 speaker 컬럼 추가, "화자ID 자리 역감사"로 0x412류 숨은 전각 글자 추가 발견(♪,<,>,◎,×). 이전 세션: `mes_translate_reinsert.py`에 블록 길이(토큰 수) 검증 추가·검증 완료. 이전: 2026-08-05, v5/v6 히라가나 구간 한글 그라운드트루스 프로브 실행 및 스크린샷 실증 검토, 타일 오버랩 버그 확정 및 수정, 이름입력 키보드로 그라운드트루스 확정 및 글리프 우측 클리핑 버그 발견, 번역용 일괄 추출/재삽입 파이프라인 구현 및 검증, 그래픽 리소스(nbfc/nbfp/nbfs 배경 이미지) 포맷 역공학 및 추출/삽입 파이프라인 검증, 코드→타일 변환 공식 확정, 전체 코드 매핑표 완성, 오프닝 장면 완역 실기 검증(길이 고정 필수 발견))

**⚠️ 폴더/기록 규칙**: 이 파일(`ANALYSIS_NOTES.md`)이 작업 기록의 진본이다. 한글패치에 필요한
최종 산출물(스크립트/데이터)만 `analysis/` 루트에 두고, 조사·테스트용 임시 자료는 반드시 프로젝트
루트의 `temp/`(`analysis/`의 하위 폴더 아님, `unpack/`과 같은 층)에 둘 것. 상세 규칙은 프로젝트
루트 `CLAUDE.md` 참고.

## 목표

`unpack/` 아래 압축 해제된 NDS 게임 데이터의 커스텀 텍스트 인코딩(폰트 코드 ↔ 실제 글자)을
역공학하여, `.mes` 스크립트 파일을 디코딩 → 번역 → 리팩(repack)까지 가능한지 타진.

## 핵심 경로

- 언팩 루트: `unpack/`
- 폰트 타일: `unpack/data/Font_DOM.nbfc` (917,504바이트, 8bpp 8x8 타일 14,336개 flat array)
- 폰트 팔레트: `unpack/data/Font_DOM.nbfp.dec` (512바이트, RGB555 256색, 압축 해제됨)
- 정답지(ground truth) QA 문자열: `unpack/data/common.mes`
- 대사 스크립트: `unpack/data/Script/dom1<캐릭터>_*.mes` (340개, 캐릭터별 L01-L10/M01-M02/O0727등/S01-S08/ending1-2)
- 씬/이벤트 스크립트(순수 대사 아님): `unpack/data/Script/dom1DefScn_*.mes`
- 이번 세션 산출물: `analysis/` (이 문서와 같은 폴더) — 스크립트 + `font_map_full.json` + `all_codes.json`
  - 원본 작업 파일들은 `/tmp/dom_analysis/`에도 있지만 `/tmp`는 재부팅 시 날아갈 수 있으므로
    **여기 `analysis/` 폴더가 진본**. 렌더링 이미지(`allcodes_batch_*.png`, `crop_*.png`)는
    `/tmp/dom_analysis/`에만 있고 여기엔 복사 안 해뒀음 — 필요하면 `render_batches.py` 재실행하면 됨.

## 확정된 인코딩 공식 (u16 LE 코드 1개 = 문자 1개, 단순 캐릭터 단위, 딕셔너리 압축 레이어 없음)

```python
NUM_TILES = 14336  # Font_DOM.nbfc 전체 타일 수

def decode_value(v):
    bank = (v >> 8) & 0xFF
    low  = v & 0xFF
    if bank == 0:
        if low >= 0x80:
            return ('half', 2*low - 14)   # 반각: 세로 2타일 스택 (8x16px)
        else:
            return ('ctrl', v)             # 제어/마커 코드
    if v + 33 >= NUM_TILES:
        return ('ctrl', v)                 # 범위 밖 = 제어 코드 (전체의 64%!)
    return ('full', v)                     # 전각: 타일 인덱스 = 값 자체, 2x2 블록 (16x16px)
```

- 전각 렌더링: `[TL, TL+1, TL+32, TL+33]` 2x2 타일 블록
- 반각 렌더링: `[TL, TL+32]` 세로 2타일

## 1,107개 코드 전수 매핑 완료

340개 대사 파일에서 실제 사용된 distinct 코드 1,107개를 전부 분류 완료.
결과: `analysis/font_map_full.json` (구조: `{formula, stats, codes: {코드hex: {kind, char/note}}}`)

| kind | 개수 | 설명 |
|---|---|---|
| `full` (전각 글자) | 356 | 알파벳 M~Z, 로마자 소문자, 히라가나/작은글자쌍, 가타카나, 다수 한자 |
| `half` (반각 글자) | 19 | 숫자 0-9 + 문장부호 몇 개 |
| `blank` | 22 | 유효 타일 범위지만 잉크 없음 → 텍스트 내 서식 제어(줄바꿈/후리가나 마커 추정) |
| `ctrl` (범위 밖) | 710 | **전체의 64%** — 순수 제어 코드, 글자 아님 |

- 354/356 전각 글자는 확신 있게 식별. **(2026-08-04 세션에서 `0283`=せ, `0284`=ぜ 확정** — `0282`(ず)와
  고주온 순서 비교, 고배율 재렌더링(`render_ambiguous.py`)으로 검증.**)**
- `3131`: 고배율 재렌더링(`render_tilestrip.py`)으로 확정 — 공식대로 `[v,v+1,v+32,v+33]`을 쓰면 깨진
  타일이 나오지만, **타일 인덱스를 1 밀어서 `[v+1,v+2,v+33,v+34]` (TL=0x3132)를 쓰면 정상적인 한자**가
  나옴. 원본 ROM 폰트 테이블의 해당 슬롯 하나만 off-by-one으로 어긋나 있는 것으로 추정. 한자의 정확한
  의미/훈독은 아직 미상 (`font_map_full.json`에 note로 기록, 문맥 대조 시도했으나 주변 코드 신뢰도가
  낮아 특정 못함).
- 반각 문장부호 9개(`0080,0082,0083,0084,0085,0086,009C,009D,009E`): 고배율 재렌더링으로도 기호 정체는
  특정 못함. 대신 구조적 결함을 발견함 — 반각 공식의 `[TL,TL+32]` 세로 스택 규칙상 `bottom_tile(low=X)`가
  항상 `top_tile(low=X+16)`과 수학적으로 동일해짐 (`2*(X+16)-14 = (2*X-14)+32`이므로). 즉 이 9개 코드의
  "아래쪽 타일"이 16만큼 떨어진 다른 확정 문자(예: 숫자)의 "위쪽 타일"과 항상 겹침 — 이는 세로 2타일
  스택 렌더링 자체가 이 슬롯들엔 맞지 않을 가능성을 시사함 (실제로는 8x8 단일 타일이거나, 문장부호가
  아니라 제어 코드일 수 있음). 데이터 파일 분석만으론 한계 — ARM9 폰트 렌더 루틴 디스어셈블리가 있어야
  확정 가능. `font_map_full.json`에 note로 상세 기록.

## 중요 결론들

1. **딕셔너리 압축 레이어는 없음** (기각됨). 근거: distinct 코드가 1,107개뿐(단어 사전이면
   수천 개 필요), `0x0002`...`0x6E5C 0x6E5C 0x0000` 종결 패턴으로 블록을 제대로 자르면
   평균 21.5자의 정상적인 대사 줄 길이 분포가 나옴, 서로 다른 두 파일(Athena_L01/L03)에서
   동일한 3코드 시퀀스가 그대로 재사용되는 것 확인 (딕셔너리가 아니라 문자 그대로 반복).
2. **대사 텍스트의 대부분(64%)이 실제로는 제어 코드다.** `.mes` 포맷은 단순 문자열이 아니라
   변수 치환/후리가나/타이밍-연출 제어 등을 포함한 간이 스크립트 언어에 가까움. 한글화하려면
   폰트 교체 + 문자 코드 매핑뿐 아니라 이 제어 코드들의 실제 의미(어디서 줄바꿈되는지, 어떤 게
   이름 치환 변수인지 등)도 파악해야 완전한 번역이 가능함.
3. **`0x0306` (べ로 렌더링됨)은 필러/제어 코드 — 통계 검증 완료 (`analyze_0306.py`).**
   3,994개 대사 블록 중 1,727개(43.2%), 304개 파일에서 총 4,304회 등장. **연속 반복 길이 분포가
   압도적으로 정확히 2회(1,994/2,070 run, ~96%)에 몰려 있고, 그 다음으로 흔한 게 정확히 4회(2쌍
   연속, 74회)** — 1회/3회/6회는 극소수. 이 "정확히 2번씩 짝지어 등장" 패턴은 확정된 블록 종결자
   컨벤션(`0x6E5C 0x6E5C`, 정확히 2번 반복)과 통계적으로 동일한 모양이며, 실제 대사라면 게임 전체
   대사 블록의 43%에서 "べべ"라는 말더듬이 반복될 이유가 없음. 따라서 글자 자체는 정상적으로 디코딩
   되는 유효한 히라가나 타일이지만, **스크립트 상에서는 문자 그대로 출력되는 게 아니라 구조적
   마커(포즈/비트 등)로 재사용되고 있을 가능성이 매우 높음** — 다만 정확히 어떤 연출 효과인지까지는
   데이터 파일 분석만으로 100% 증명되지 않음. `font_map_full.json`의 `0306` 항목에 note로 기록.
4. 텍스트 블록 구조: `0x0002` 마커로 시작, `..., 0x6E5C, 0x6E5C, 0x0000`으로 끝나는 것만
   진짜 대사(3,994개 블록 확인). 이 패턴 없이 끝나는 `0x0002...0x0000` 구간은 숫자 메타데이터/헤더임.
5. 뱅크(코드 상위 바이트)별 테이블 레이아웃: 뱅크0=반각(숫자/기호), 뱅크1=전각 대문자 알파벳 M-Z,
   뱅크2=로마자 소문자 꼬리+히라가나 시작, 뱅크3=히라가나 계속, 뱅크4=가타카나,
   **뱅크5 이상=한자 영역 시작** (이전 세션까지 못 찾았던 한자 위치를 이번에 확정).
6. **ctrl 코드 710개는 독립적인 710가지 기능이 아니라 상당수가 2-토큰 짝(opcode+parameter, 혹은
   32비트 명령을 u16 두 개로 쪼갠 것)일 가능성이 높음.** `ctrl_pair_detection.py`로 모든 ctrl 코드의
   "바로 다음에 오는 코드" 바이그램 분포를 조사한 결과, 다수의 코드가 거의 결정론적으로 특정 코드
   하나만 뒤따름을 발견 — 예: `4B10 → 7014`는 38개 파일에 걸쳐 117/117회(100%), `3E12 → 431C`는
   16개 파일에서 24/24회(100%), 그 외 `840A→7202`, `6906→A81C`, `4800→6912`, `C814→5F16` 등도
   ratio 0.8 이상. 이는 "710개 제어 코드"라는 원시 카운트가 실제 독립 명령 종류 수를 과대평가하고
   있음을 시사함. 단, 각 (짝) 코드가 구체적으로 어떤 동작(변수치환/줄바꿈/이름삽입/타이밍 등)을
   하는지는 여전히 미상 — 이걸 완전히 규명하려면 문맥 비교만으론 한계가 있고 ARM9 실행 코드
   디스어셈블리가 사실상 필요함 (범위 밖으로 판단, 보류).
7. `common.mes`는 대사가 아니라 **폰트 커버리지 QA 테스트 문자열**(88바이트 압축 / 76바이트 압축
   해제 / u16 38개)임 — `0x0002`/`0x6E5C 0x6E5C` 대사 블록 구조가 없음. 문장부호+숫자,
   알파벳+가타카나+한자, 알파벳+히라가나+문장부호+한자 3묶음을 순서대로 담고 있어 디코더 파이프라인
   검증(sanity check) 용도로 유용함. `decode_common_mes.py`로 재현 가능. 이 QA 문자열에서 새로
   발견된 미확인 한자/가나 후보(`0500,0502,0504,0506`, `0414~041E` 부근)가 있으나 저신뢰 추정이라
   `font_map_full.json`에는 반영하지 않음 (낮은 확신으로 억지 추정하지 않는다는 이번 세션 원칙).

## 완료된 항목 (2026-08-04 세션)

1. ✅ `0283`,`0284`,`3131`,반각 문장부호 9개 재검증 — 위 "1,107개 코드 전수 매핑 완료" 섹션 참고
   (`render_ambiguous.py`, `render_tilestrip.py`, `render_grid.py`)
2. 🟡 `ctrl` 710개 코드 기능 규명 — **부분 완료.** 구조적으로 다수가 2-토큰 짝임을 발견(위 결론 6번,
   `ctrl_pair_detection.py`). 각 짝의 구체적 의미(변수치환/줄바꿈/이름삽입 등)는 데이터 분석만으론
   특정 불가 — ARM9 디스어셈블리가 필요한 영역으로 판단, 이번 세션 범위에서는 보류.
3. ✅ `0x0306` 필러 코드 가설 검증 — 위 결론 3번 참고 (`analyze_0306.py`). 통계적으로 강하게 뒷받침됨
   (완전한 100% 증명은 ARM9 코드 확인 없이는 불가하지만, 실용적 목적으로는 "제어 코드로 취급" 결론 확정).

## task #4 진행 상황 (2026-08-04 세션) — 디코더/리팩 파이프라인 실증 완료

**핵심 결론: 리팩 파이프라인은 이미 end-to-end로 동작함을 실증했다.**

1. ✅ **LZ10 압축기 구현 완료** (`lz10.py`). 기존엔 압축 해제만 있었음. 표준 그리디 백레퍼런스
   탐색 방식으로 구현, `dom1*.mes` 40개 파일 샘플에 대해 `decompress → compress → decompress`가
   원본과 바이트 단위로 완전히 일치함을 확인 (재압축 시 원본과 동일한 바이트를 만들 필요는 없고,
   "게임의 LZ10 압축 해제기가 같은 결과를 내는가"만 중요 — 이 조건 충족).
2. ✅ **통합 디코더/인코더 완성** (`mes_codec.py`): `load_values()`(파일→u16 리스트),
   `dump_values()`(u16 리스트→파일, LZ10 압축 포함), `render()`(디스플레이용 문자열 변환),
   `find_dialogue_blocks()`(진짜 대사 블록 범위 추출) 제공. **ctrl 코드는 의미를 몰라도 위치
   그대로 보존하며 통과시키는 방식**으로 처리 — 정확한 기능 규명 없이도 번역 삽입이 가능한
   설계. `decode(x)` → `encode(decode(x)) == x` 왕복 검증을 25개 파일 샘플에서 통과.
3. ✅ **NitroPacker 자체에 `pack` 명령이 이미 존재함을 확인** — 이 프로젝트가 애초에
   HaroohieClub NitroPacker 툴이므로, `unpack/DAYSOFMEMORY.json` 프로젝트 파일 기반으로
   FAT/FNT를 포함해 ROM 전체를 재구성하는 기능이 이미 구현되어 있음. 즉 리팩 파이프라인을
   새로 만들 필요 없이 **기존 도구를 그대로 활용 가능**.
4. ✅ **전체 파이프라인 실제 검증(round-trip 실증)**: `dom1Athena_L01.mes`의 첫 대사 블록
   (`科ぇ括褐y潟開拐べべ`)의 글자 코드만 순서를 뒤집어 수정(`べべ拐開潟y褐括ぇ科`) →
   `mes_codec.dump_values()`로 LZ10 재압축하여 파일 저장 → `NitroPacker pack`으로 ROM 전체
   재구성 → 그 ROM을 `NitroPacker unpack`으로 다시 풀어서 → 수정한 대사가 **글자 하나 안
   틀리고 그대로 복원됨을 확인**. 즉 "글자 코드 수정 → 리팩 → 게임에 반영"이라는 번역 파이프라인의
   핵심 경로가 실제로 작동함이 증명됨 (테스트 후 원본 파일은 복원해둠, 스크래치 결과물은 삭제함).

**이걸로 증명 안 된 것 / 남은 리스크:**
- 실제 게임(에뮬레이터/실기)에서 렌더링이 정상적으로 되는지는 확인 못함 — 여기서 확인한 건
  "데이터 레벨에서 왕복이 정확하다"는 것뿐, ARM9 렌더 루틴이 실제로 그 코드를 어떻게 그리는지는
  블랙박스로 남아있음 (지금까지의 `decode_value` 공식은 대사 파일에 실제 쓰인 코드들을 통계적으로
  역산한 것이지, ROM 코드에서 직접 확인한 건 아님).
- `.mes` 파일 크기가 늘어나도 되는지는 이번 테스트로는 검증 안 됨(이번엔 같은 글자 수만
  바꿔서 압축 후 크기가 우연히 같았음, `896 bytes`). 번역문이 원문보다 길어지면 압축 후
  크기가 달라질 수 있는데, NitroPacker의 `pack`은 프로젝트 JSON에 있는 파일들을 다시
  풀 스캔해서 FAT/FNT를 새로 구성하는 방식이라 **파일 크기 변경 자체는 지원되는 것으로 보이나
  (실제 파일 크기와 다른 크기로 repack 테스트는 아직 안 해봄)**, 확실히 하려면 원문보다 훨씬 긴
  더미 텍스트로 같은 왕복 테스트를 한 번 더 해봐야 함.

## 아직 남은 것 (한글 표시 방안 — 사용자 결정 필요)

번역 삽입 자체(파이프라인)는 됐지만, **이 폰트에 한글 타일이 없다**는 근본 문제는 아직 그대로임.
가능한 접근은 최소 두 가지이고 트레이드오프가 커서 임의로 하나를 골라 진행하지 않았음:

- **(A) 새 한글 타일을 `Font_DOM.nbfc`에 추가.** 완성형 음절(2,350자) 전체가 아니라 실제 번역
  대본에서 쓰이는 음절만(보통 수백~1천 자 내외) 추가하면 타일 수를 절약 가능. 각 음절은
  기존 전각 문자처럼 16x16(2x2 타일 블록)로 그리면 되고, 코드 값은 현재 안 쓰이는 u16 범위를
  새로 할당하면 됨(코드 공간은 넉넉함 — 현재 쓰이는 코드는 1,107개뿐, 65,536개 중). **단, 이러면
  `NUM_TILES`가 늘어나고 `decode_value`의 "범위 밖=ctrl" 판정 경계도 같이 늘려야 하는데, 이 경계가
  게임 엔진에도 실제로 존재하는 것인지(하드코딩된 타일 개수 상한이 있는지) 확인이 안 됨 — 에뮬레이터
  실기 테스트 전엔 리스크 있음.**
- **(B) 기존 미사용 코드 슬롯/제어 코드 일부를 재활용.** 새 타일 공간을 늘릴 필요는 없지만,
  ctrl 코드 710개 중 어떤 것도 "완전히 안전하게 재활용 가능"이라고 확정하지 못한 상태(task #2에서
  기능 특정 못함) — 잘못 건드리면 게임이 깨질 위험이 (A)보다 오히려 큼.

**결정이 필요한 지점**: 에뮬레이터로 실제 렌더링 테스트가 가능한 환경인지, 완성형 음절을 얼마나
커버할지(전체 번역 대본 기준 서브셋 vs 안전하게 자모 조합형 등), 이 두 가지에 따라 다음 단계가
크게 갈리므로 이번 세션에서는 임의로 확정하지 않고 옵션만 정리해둠.

→ **melonDS 에뮬레이터 사용 가능함 확인 (사용자 답변) → (A) 신규 타일 추가 방식으로 실제 프로토타입
제작 완료.**

### (A) 프로토타입 결과물

- `add_korean_test_glyph.py`: 한글 글자 "가" 1개를 새 타일로 만들어 `Font_DOM.nbfc`에 추가하는
  스크립트. 기존 파일 끝에 32타일짜리 행 2개(64타일=4,096바이트)를 통째로 덧붙이는 방식이라
  기존 "flat array, 32타일 stride" 주소 체계를 그대로 유지함 (14,336 → 14,400타일). 4개 타일만
  실제 글자로 채우고 나머지 60개는 빈 타일로 둠.
- 코드 값은 **1,107개 코드 corpus 어디에도 안 쓰인 완전 미사용 코드 `0x3801`**을 선택함 (이미
  실제 대사에서 ctrl 코드로 쓰이고 있는 값을 재활용하면, 게임 엔진이 "타일 범위 밖=제어 코드"라는
  우리 추론과 다르게 그 값 자체를 하드코딩된 opcode로 특별 처리할 위험이 있어 회피함).
- 렌더링 자체는 파이썬 시뮬레이션으로 정상 확인함 (`new_glyph_preview.png`).
- 테스트용으로 `dom1Athena_L01.mes`의 첫 대사 블록(원래 `科ぇ括褐y潟開拐べべ`)을
  `가가가가가`(0x3801 x5)로 통째로 교체 → `NitroPacker pack`으로 ROM 재구성 →
  **`analysis/korean_test.nds`** 생성 완료. `NitroPacker unpack`으로 재검증해서 폰트 타일 수
  증가(921,600바이트)와 대사 교체(`가가가가가`)가 정확히 패키징됐음을 확인함.
- 원본 `unpack/` 소스 트리는 테스트 후 원상복구해둠 (`Font_DOM.nbfc.orig_backup`,
  `dom1Athena_L01.mes.orig_backup`에 백업 보관 — 필요하면 diff/재현 가능).

### 다음 확인 필요 (사용자 액션)

`analysis/korean_test.nds`를 melonDS로 직접 실행해서, Athena 캐릭터의 L01 대사 파일에 해당하는
장면(어떤 트리거로 재생되는지는 게임 진행 지식이 필요해 이쪽에서 특정 못함 — 사용자가 게임
진행하면서 확인 필요)에서 **"가가가가가"가 화면에 정상적으로 표시되는지** 확인 필요. 확인 결과에
따라:
- 정상 표시됨 → (A) 방식이 실제로 안전함이 실증됨 → 실제 번역에 필요한 음절 전체를 이 방식으로
  확장하는 단계로 진행 가능.
- 깨지거나 표시 안 됨/게임이 이상 동작함 → ARM9 쪽에 하드코딩된 타일 개수 상한이 있다는 뜻이므로
  (B) 방식 또는 ARM9 패치까지 고려해야 함.

### v5/v6: 히라가나 구간(0x02xx/0x03xx) + 0x0810 전체 그라운드트루스 프로브 (2026-08-05)

**목적**: `font_map_full.json`에서 `kind=='full'`이고 0x0200~0x03FF 범위인 모든 코드(+0x0810,
총 25개) 각각을 서로 다른 한글 음절로 덮어쓰고, **`.mes` 스크립트는 전혀 건드리지 않은 채** 원작
그대로의 스토리를 플레이 — 화면에 나타나는 한글 음절을 보고 실제 엔진이 그 코드를 정말로 그
위치에서 렌더링하는지, 우리 `decode_value` 공식이 맞는지 코드 단위로 직접 검증.

- `paint_hiragana_range_korean.py` (v5): 25개 코드에 `SYLLABLES` 리스트를 순서대로 배정,
  `korean_test_hiragana_v5.nds` 빌드 → melonDS로 사용자가 실제 플레이하며 스크린샷 3장 확보.
- **결과 요약(스크린샷 재검토, 압축으로 유실된 이미지는 원본 JSONL 로그에서 base64 재추출해 확인):**
  1. ✅ **핵심 확인 — 코드 단위 치환이 실제로 동작한다.** 침실 씬 스크린샷에서 "머"(0x0312 페인트)와
     "비"로 보이는 글자가 "8：30" 바로 앞에 정확히 나타났고, 같은 대사 블록의 다른 부분("しまった。
     いま何時だ！？")은 **우리가 건드리지 않은 원본 히라가나 그대로** 유지됐다. 즉 25개 타겟 코드만
     선택적으로 정확히 바뀌고 나머지는 전혀 영향받지 않음 — `decode_value`(코드값=타일인덱스,
     2x2 블록) 공식이 근본적으로 틀렸다는 이전 우려는 상당히 해소됨.
  2. ✅ **"미할당 글자 비" 미스터리 해소 — 매핑 버그가 아니라 폰트 가독성 문제였다.** 사용자가
     "'비'는 '바'인데 자간 때문에 그렇게 보이는 거 아니냐"고 지적 → 25개 음절 전체와 픽셀 diff를
     계산한 결과 정확히는 "바"가 아니라 **"버"(0x0316에 배정, v6 기준)**가 256픽셀 중 15픽셀
     차이(ㅓ의 짧은 연결획 유무)로 압도적으로 유사함을 확인. AppleSDGothicNeo를 8x8 타일 2x2
     블록(16x16px) 해상도로 렌더링하면 버/비가 사실상 구별 불가능 — **사용자의 지적 방향(자간/폰트
     문제)이 맞았고, 구체적 음절만 바에서 버로 정정됨.**
  3. ⚠️ **실제 타일 인덱스 오버랩 버그 확정, 단 이전에 생각했던 것보다 범위가 훨씬 좁음.**
     2x2 풋프린트 `{code, code+1, code+32, code+33}`는 두 코드의 gap이 정확히
     `{1, 31, 32, 33}` 중 하나일 때만 실제로 타일을 공유한다 (gap<34면 전부 위험하다고 본 v5 당시의
     보수적 판단은 틀림 — 직접 집합 교집합으로 재계산해 확인). v5의 25개 타겟 중 실제로 겹치는 건
     `0x0282–0x0283–0x0284` 체인뿐(가운데 0x0283이 양쪽과 겹침, gap=1). 이 셋은 원작에서도
     せ/ぜ 관련 다쿠텐 글자라 애초에 폰트 시트 설계상 촘촘하게 붙어있었을 가능성이 있음.
  4. ❓ **미해결로 남은 항목 — "ま" 라벨 정합성.** `font_map_full.json`의 원래 라벨은 0x030A='ま'로,
     이 구간 라벨들이 い/う/ぇ, ず/せ/ぜ, へ/べ/ぼ, ま/み/む/め/も, や/ゃ/ゆ/ゅ/よ 순서로 **문법적으로
     매우 깔끔한 오십음도 배열**을 이루고 있어 신뢰도가 높아 보임. 그런데 v5에서 0x030A는 '하'(v5
     기준. 순서상 8번째 SYLLABLE)로 페인트됐는데도, 스크린샷의 "しまった。いま何時だ" 부분의 "ま"는
     페인트되지 않은 원본 그대로로 나타났다. 즉 이 대사에 실제로 쓰인 "ま" 코드는 0x030A가 아니거나,
     동일 글자를 그리는 코드가 여러 개(중복 슬롯) 존재할 가능성이 있음 — **아직 규명되지 않은 채
     남아있는 항목.** 확정하려면 해당 대사가 들어있는 `.mes` 파일을 특정해서 실제 코드 시퀀스를
     직접 덤프해봐야 함(스크린샷만으로는 한계).
- **v6 수정 (`paint_hiragana_range_korean_v6.py`)**: v5의 "gap<34면 위험" 식 휴리스틱 대신, 두
  코드의 실제 타일 인덱스 집합 교집합을 직접 계산해 진짜 겹치는 코드만 골라내고, 겹치는 그룹에서는
  하나만 남기고 나머지는 자동 제외(그리디 최대 독립집합)하도록 수정. 페인팅 중에도 각 타일 인덱스가
  정확히 한 코드에 의해서만 쓰였는지 런타임에 검증(assert)하도록 안전장치 추가. 실행 결과 `0x0283`
  1개만 자동 제외되고 나머지 24개 코드가 오버랩 없이 안전하게 페인트됨 — `korean_test_hiragana_v6.nds`
  빌드 완료(16,789,504바이트, v5와 동일 크기). 빌드 후 `unpack/data/Font_DOM.nbfc`와
  `font_map_full.json`은 정확히 원본으로 복원해둠(diff 0줄 확인).
- **다음 확인 필요(사용자 액션)**: `korean_test_hiragana_v6.nds`를 melonDS로 플레이해서, v5와 같은
  침실/8:30 장면에서 이번엔 (0x0283이 빠진 상태로) 글자가 깨지지 않고 정상적으로 나오는지 확인.
  특히 이전 v5 스크린샷에서 "じ[X]ねーか"의 X 위치에 모음 삐침이 두 겹쳐 보이는 듯한 이상 글자가
  나왔던 부분이 v6에서는 정상으로 보이는지가 핵심 검증 포인트.

### v6 실측 결과 (2026-08-05) — 이름 입력 키보드로 그라운드트루스 확정, 글리프 우측 클리핑 버그 발견

사용자가 `korean_test_hiragana_v6.nds`를 플레이하며 확보한 스크린샷 2장(이름 입력 키보드 "かな・漢字"
탭, 아파트 씬 大地 대사)을 픽셀 단위로 재검토.

**결정적 그라운드트루스 확보**: 이름 입력 키보드는 오십음도(あ행,か행,さ행...) 고정 순서로 배열되므로,
**격자 위치만으로 실제 글자 정체가 100% 확정**된다. 이 격자와 우리가 칠한 24개 음절을 대조한 결과:

| 격자 위치(진짜 글자) | 우리가 칠한 음절 | 화면에 보인 것 |
|---|---|---|
| う | 바 | 비 |
| え | 아 | 이 |
| せ | 차 | 치 |
| ほ | 카 | 키 |
| ま | 하 | 히 |
| み,む,め,も | 거,너,더,러 | 거,너,더,러 (그대로, 정상) |

- ✅ **"미할당 글자" 미스터리 최종 해소 — 사용자 가설이 정확했다.** 네이티브 해상도로 확대해보면
  ㅇ 다음에 와야 할 세로획이 **글자 칸(타일) 경계선에 거의 붙어서** 나오고, ㅏ의 오른쪽 삐침은
  아예 안 보인다. 즉 우리가 16x16 박스를 꽉 채워 그린 글리프의 **오른쪽 끝부분이 실제 렌더링에서
  잘린다.** ㅏ·ㅑ 계열 모음을 가진 음절(가나다라마바사아자차카타파하, 14개)이 전부 이 클리핑에
  취약하고, ㅓ 계열(거너더러머버서어저처)은 삐침이 왼쪽에 있어 영향받지 않음 — 위 표와 아파트 씬
  대사("ちょっと", "いま") 양쪽에서 일관되게 재현됨. 이전 v5에서 "미할당 글자 비"라며 별도
  미스터리로 다뤘던 것과, "'비'=자간 문제로 보이는 '바' 아니냐"던 사용자의 최초 가설 모두 이걸로
  정확히 설명됨(다만 구체적 메커니즘은 "폰트 유사성"이 아니라 "우측 클리핑"이었음 — 이전 세션에서
  픽셀 diff로 찾은 "버가 15픽셀 차이로 가장 유사"는, 클리핑된 '바'가 우연히 '버'와 비슷한 형태가
  되어 생긴 결과로 재해석됨).
- ⚠️ **원인 추정(미확정): 가변폭 글리프 + 폭(width) 테이블.** `unpack/data/`에서 `Font_DOM.nbfc`/
  `.nbfp` 외에 별도의 문자별 폭 테이블 파일은 발견되지 않음 — 존재한다면 ARM9에 하드코딩돼 있을
  가능성이 있음. 원래 각 코드의 원본 글자가 16px보다 좁은 폭으로 그려지도록 폭 값이 지정돼 있고,
  우리가 새로 그린 꽉 찬 16px Korean 글리프가 그 폭 제한에 걸려 잘리는 것으로 추정. 100% 확정하려면
  ARM9 디스어셈블리가 필요 — 범위 밖으로 판단, 보류.
- ⚠️ **미해결로 남은 별개 이상**: や/ゆ/よ 자리(0x0314/0x0318/0x031C로 추정)는 클리핑만으로
  설명되지 않음 — や 자리엔 클리핑이 필요없는 ㅓ계열인데도 우리가 칠한 것과 다른 음절(머 대신 버,
  서 대신 어)이 나타났고, よ 자리는 아예 페인트 안 된 것처럼 원본 그대로 보임. 이는 이 구간의
  코드 위치가 라벨과 최소 한 칸(2코드) 밀려있을 가능성을 시사하나 확정 못함.
- ⚠️ **せ 위치도 의문점**: v6에서 오버랩 때문에 의도적으로 제외한 `0x0283`(라벨상 せ) 자리인데도
  화면엔 페인트된 글자("치", 클리핑된 "차")가 나타남 — 실제 せ의 진짜 코드가 0x0283이 아니라
  0x0284(우리가 "차"로 칠함)일 가능성을 시사. せ/ぜ 라벨이 서로 밀려있거나 바뀌었을 수 있음, 미확정.

**실용적 결론**: 이건 매핑 공식이나 코드 위치 문제가 아니라 **우리 테스트 글리프를 박스 꽉 채워
그린 것 자체의 아티팩트**다. 실제 로컬라이징용 한글 폰트를 그릴 때 오른쪽에 1~2px 여백을 미리
남기고 그리면(또는 전체를 살짝 왼쪽으로 당겨서 그리면) 이 클리핑을 피할 수 있을 것으로 보임 —
다음 프로토타입에서 이 여백을 적용해 재검증 필요.

## 번역용 일괄 추출/재삽입 파이프라인 (2026-08-05)

`mes_codec.py`의 왕복 검증된 디코더/인코더 위에, 340+개(정확히는 `Script/` 전체 865개 중 dom1/dom3
865개 파일 - dom1 캐릭터 340개 + dom3 등 나머지 포함) `.mes` 파일 전체를 CSV로 일괄 추출하고 번역문을
다시 삽입하는 실전용 스크립트 3개를 신규 구현·검증 완료.

**핵심 설계 결정 — `mes_codec.render()`를 그대로 쓰지 않고 `translate_io.py`를 새로 만든 이유**:
기존 `render()`는 사람이 콘솔에서 훑어보기 위한 디버그용이라 (a) `kind=='blank'`인 22개 코드를 전부
공백 한 칸으로 뭉개고 (b) 아직 정체를 못 밝힌 10개 코드(`0080,0082~0086,009C~009E`,`3131`)를
`?(punct, unresolved)` 같은 여러 글자짜리 디버그 라벨로 그대로 찍는다. 두 경우 다 번역 파이프라인
관점에서는 **비가역적**이다 — 공백 하나가 원래 어떤 blank 코드였는지 복원 불가능하고, 디버그 라벨
문자열은 애초에 실제 글리프가 아니라서 글자 단위로 되돌릴 방법이 없다. 그래서 `translate_io.py`는
번역 목적 전용 규칙을 따로 둠: **"font_map_full.json에서 kind가 full/half이고 char가 정확히 1글자인
코드"만 리터럴 문자로 렌더링, 나머지 전부(ctrl + blank 22개 + 미해결 10개)는 예외 없이 `<HEX>`
플레이스홀더로 명시**. 번역자는 이 플레이스홀더를 문장 어디에 두든 그대로 복사만 하면 됨.

- `translate_io.py`: `tokens_to_text()`(u16 리스트→텍스트, 위 규칙), `text_to_tokens()`(역변환,
  매핑 없는 문자를 만나면 `ValueError`), `validate_placeholders()`(원문/번역문의 `<HEX>` 플레이스홀더
  **개수**가 정확히 일치하는지 검사 — 순서는 강제하지 않음, 번역 특성상 어순이 바뀔 수 있어 개수만
  엄격히 검증).
- `mes_translate_extract.py`: `unpack/data/Script/*.mes` 865개 전체를 스캔해 `find_dialogue_blocks()`로
  진짜 대사 블록만 골라 `analysis/translation_export.csv`로 출력 (컬럼: `file,block,n_tokens,source,
  translation`). 실행 결과 **847개 파일에서 13,302개 대사 블록** 추출 확인.
- `mes_translate_reinsert.py`: 번역문이 채워진 CSV를 읽어 파일별로 원본 `.mes`를 다시 로드하고
  (CSV의 start/end가 아니라 **매번 원본에서 새로 `find_dialogue_blocks()`를 계산**해서 블록 경계를
  구함 — 번역으로 블록 길이가 달라져도 같은 파일 내 다음 블록 위치가 안 꼬임), 블록별로
  `validate_placeholders()` 통과 여부를 확인한 뒤 `text_to_tokens()`로 재인코드, `mes_codec.dump_values()`
  로 LZ10 재압축해 저장. **한 블록이라도 플레이스홀더 검증에 실패하거나 매핑 안 된 문자가 있으면 그
  파일 전체를 건너뛰고(부분 파일 저장 안 함) 사유를 출력, 다른 파일은 계속 처리.** 빈 번역 셀은
  원문으로 자동 폴백하므로 부분 번역 CSV도 항상 유효한 출력을 냄. **안전을 위해 `unpack/data/Script/`를
  절대 직접 덮어쓰지 않고 `analysis/translated_output/Script/`에 새로 씀** — 실제 게임에 반영하려면
  이 결과물을 `unpack/`으로 복사 후 `NitroPacker pack`을 수동으로 돌리는 별도 단계가 필요(의도적으로
  자동화 안 함).

**검증 완료 (2026-08-05)**:
1. ✅ 번역문 전부 빈 채로(=원문 그대로) 847개 파일 전체 추출→재삽입 실행 → **847개 파일 모두
   디코드 값이 원본과 완전히 동일**(`load_values()` 리스트 단위 비교, mismatch 0건).
2. ✅ 리터럴 글자 재배열(어순 변경) 성공 케이스 — `dom1Athena_L01.mes` 블록0의 리터럴 문자만
   순서를 바꿔도(멀티셋 동일) 정상 통과.
3. ✅ 매핑 안 된 문자(예: 한글처럼 아직 폰트에 코드가 없는 문자) 삽입 시 `ValueError`로 정확히
   잡혀서 해당 파일이 스킵되고 사유가 출력됨.
4. ✅ 플레이스홀더 누락(`<7714>` 삭제) → "missing placeholders" 에러로 정확히 잡힘.
5. ✅ 플레이스홀더 추가(`<9999>` 삽입) → "unexpected/extra placeholders" 에러로 정확히 잡힘.

**주의할 점 / 아직 안 된 것**:
- 지금은 폰트에 한글 코드가 확정 배정되지 않은 상태라(위 v5/v6 프로브는 임시 테스트용 오버라이트일
  뿐 `font_map_full.json`에 영구 반영된 게 아님), 실제 한글 번역문을 넣으면 `text_to_tokens()`가
  전부 "no font code assigned" 에러를 낼 것임 — **이건 파이프라인 버그가 아니라 정상 동작**. 한글
  타일+코드가 확정되고 `font_map_full.json`에 반영된 이후에야 실제 번역 삽입이 가능함.
  - 공백(단어 사이 띄어쓰기)도 마찬가지: 현재 어떤 코드도 `char==' '`로 매핑돼 있지 않음(일본어라
    띄어쓰기 자체가 없었던 것으로 보임). 한글 번역엔 띄어쓰기가 필요하므로, 한글 타일 작업 시
    미사용 코드 슬롯 하나를 공백으로 명시적으로 배정해둬야 함.
- `translated_output/`에 쓰기 전 파일별 검증 실패를 전부 모아 리포트하지만, 재실행할 때마다 CSV
  전체를 다시 읽고 847개 파일을 통째로 재생성함(증분 처리 아님) — 13,302행 규모에선 수초 내로
  끝나서 문제 없음.

## 그래픽 리소스 추출/삽입 검증 (2026-08-05)

폰트/텍스트 말고 실제 배경·초상화·UI 이미지 리소스도 같은 방식(추출→수정→재삽입→NitroPacker
리팩)이 통하는지 확인. `unpack/data/`에 `*.nbfc`(타일)/`*.nbfp`(팔레트)/`*.nbfs`(스크린맵) 세트가
30여 종 존재(`hoshi0001~0059` 홀수 30개, `00_ATHENA_0`, `_CAP_B_00` 등). Font_DOM.nbfc/nbfp와
확장자는 같지만 이 파일들은 **개별적으로 LZ10 압축돼 있다는 점이 다름** (Font_DOM.nbfc/nbfp는
압축 안 된 raw였음 — 아마 폰트는 항상 상주라 압축 안 하고, 배경 이미지들은 개별 파일 단위로
LZ10 압축해 저장하는 것으로 보임).

**포맷 확정**:
- `.nbfc`: LZ10 압축 해제 후 8bpp 8x8 타일 flat array (64바이트/타일, Font_DOM과 동일 타일 포맷).
- `.nbfp`: LZ10 압축 해제 후 256 x RGB555(2바이트) = 512바이트 팔레트 (Font_DOM.nbfp와 동일 구조).
- `.nbfs`: LZ10 압축 해제 후 u16 스크린맵 엔트리 배열. 엔트리 포맷은 표준 NDS BG 스크린 엔트리와
  동일: bits0-9=타일 인덱스, bit10=수평반전, bit11=수직반전, bits12-15=팔레트뱅크(4bpp 전용, 이
  포맷에서는 관측된 값이 전부 0이라 8bpp 단일 팔레트 사용으로 확인). 768개 엔트리 = 32x24 타일
  = 256x192px, NDS 화면 한 장 전체 크기와 정확히 일치.
- 즉 폰트처럼 "코드값=타일인덱스 직접" 방식이 아니라, **타일시트(적은 수의 유니크 타일) + 스크린맵
  (타일 재사용 배치도)**의 표준적인 타일맵 압축 구조 — 같은 무늬가 반복되는 배경/이펙트에 유리함.
- `decode_bg_image.py`로 세 파일을 합쳐 PNG로 렌더링 검증 완료:
  - `hoshi0001~0059`: 별 모양이 30프레임에 걸쳐 회전/변형하는 반짝임 이펙트 애니메이션으로 확인
    (타일 18~81개, 녹색=팔레트 인덱스0, 그 외 대부분 검정 — 사실상 2색 실루엣 마스크에 가까움).
  - `00_ATHENA_0.nbfc/.nbfp/.nbfs`: 아테나 캐릭터 초상화(173타일)로 정상 렌더링 확인, 배경은
    녹색(크로마키용 백드롭 색상으로 추정, 실제 게임에서는 다른 레이어와 합성되며 투명 처리될
    가능성이 높음 — 지금은 검증 안 함).
  - `_CAP_B_00.nbfc/.nbfp/.nbfs`: 캐릭터 선택 카드 UI(199타일) — 초상화, 스탯 게이지, "リュウ"
    캐릭터명 라벨까지 전부 선명하게 렌더링됨.
- **삽입(재인코딩→리팩) 왕복 검증 완료**: `hoshi0001.nbfc`를 압축 해제→타일2(스크린맵에서 736/768
  칸을 차지하는 지배적 채우기 타일)를 녹색/검정 체커보드 패턴으로 덮어씀→`lz10.compress()`로
  재압축(재압축 직후 `lz10.decompress()`로 원하는 수정 바이트와 정확히 일치하는지 자체 검증)→
  `unpack/data/hoshi0001.nbfc`에 저장→`NitroPacker pack`으로 `hoshi_test.nds` 빌드→
  `NitroPacker unpack`으로 재검증. **압축 해제한 값이 우리가 만든 수정 바이트와 완전히 일치**
  (바이트 단위 비교, mismatch 0), `decode_bg_image.py`로 렌더링해서 체커보드 무늬가 화면 전체에
  실제로 반영된 것도 시각적으로 확인. 텍스트/폰트 파이프라인과 완전히 동일한 안전성으로 그래픽
  리소스도 추출·수정·재삽입이 가능함이 실증됨. 테스트 후 `unpack/data/hoshi0001.nbfc`는
  원본으로 정확히 복원함(diff 0줄, 백업은 `hoshi0001.nbfc.orig_backup`에 보관).

**한글화에 대한 함의**:
- 그래픽 쪽 로컬라이징(예: 일본어가 그림으로 박혀 있는 UI 텍스트, 로고, 카드의 캐릭터명 라벨 등)도
  이 파이프라인으로 안전하게 교체 가능함이 확인됨 — 폰트 코드 문제와 별개로, "그림으로 그려진 일본어"
  가 있다면 타일만 다시 그려 넣으면 됨(스크린맵 구조상 텍스트가 있는 이미지는 보통 타일 재사용이
  적어 새 텍스트 타일을 추가할 공간이 필요할 수 있음 — 지금은 기존 타일 개수를 유지한 채 픽셀만
  바꾸는 시나리오만 검증함, Font_DOM에서처럼 파일 끝에 타일을 늘리는 시나리오는 이번엔 테스트 안 함).
- `_CAP_B_00`처럼 캐릭터명이 이미지 텍스트로 박힌 UI가 존재한다는 것 자체가, `.mes` 스크립트
  번역만으로는 완전한 한글화가 안 되고 이런 그래픽 UI 요소들도 별도로 찾아서 교체해야 함을 시사함
  — 전체 리소스에서 이런 "그림 속 텍스트"가 몇 개나 더 있는지는 아직 전수 조사 안 함.
- `unpack/data/`의 918개 `.bin` 파일은 이번 세션에서 다루지 않음 — 배경/맵/기타 리소스일 가능성이
  높으나 확장자만으론 포맷 특정 안 됨, 필요 시 매직바이트/크기 패턴으로 추가 조사 필요.

## 코드→타일 변환 공식 확정 + 전체 매핑표 완성 (2026-08-05)

이전까지 `decode_value`(bank==0 half-width, bank!=0이면 `v+33>=NUM_TILES`로 ctrl/full 판정)는
"이 코드가 글자인가 제어코드인가"만 판정했을 뿐, **실제 `Font_DOM.nbfc` 상의 타일 인덱스가 코드값과
정확히 같다는 가정**(`decode_value`의 `('full', v)` 리턴값을 그대로 타일 인덱스로 씀)에 의존하고
있었다. v5/v6 프로브에서 이 가정이 미묘하게 안 맞는 정황(や/ゆ/よ 자리 불일치, せ 위치 의문)이
발견되면서, 코드값 자체가 아니라 **별도의 변환 공식이 필요하다**는 게 이번에 확정됨.

**확정된 공식**: `real_tile_index = (code >> 8) * 64 + (code & 0xFF)` (bank≠0인 경우만 해당).
何(0x2008)→2056, 計(0x3F04)→4036, 時(0x5902)→5698 세 글자로 역산해 도출했고, "8:30" 대사
그라운드트루스 블록(`dom1OP_0701_1.mes[3796:3830]`, 34토큰) 전체에 모순 없이 적용됨을 확인.
`analysis/probe_830_korean.py`로 21개 고유 글자를 한글 음절로 실제 리페인트해 melonDS 게임
테스트(`analysis/probe_830_korean.nds`)로 검증 — 테스트 대상 씬뿐 아니라 전혀 다른 씬(아테나
잔소리 장면)에서도 겹치는 글자가 자동으로 한글로 바뀌어 나타나, **폰트 타일이 게임 전체에서
공유됨**(씬별로 다르지 않음, 즉 어떤 글자를 리페인트하면 그 글자를 쓰는 모든 대사가 다같이 바뀜)도
함께 확인됨. 반폭(half-width) 숫자/콜론(8,3,0,：)은 이 공식과 무관한 기존 별도 주소 체계
그대로이며 건드리지 않음.

**전체 매핑표**: `analysis/scan_all_codes.py`로 게임 내 모든 `.mes`(883개 파일)를 `find_dialogue_blocks()`
기준으로 스캔해 실제 대사 블록에서 쓰이는 고유 코드 1,612개를 추출, 공식을 적용해
`analysis/full_code_tile_map.json` 생성(코드hex → {count, kind, real_tile}). 1,591개가
"full"(유효한 real_tile 보유), 나머지는 half/ctrl, 예외는 `0x6E5C`(줄바꿈/블록종료 제어)와
`0xFFFF`(41회 등장하는 특수 종결 코드) 단 2개뿐. 최상위 빈도 39개 코드를 pristine `Font_DOM.nbfc`에서
렌더링해 전부 깨끗한 히라가나/기호로 정확히 나타남을 확인 — 공식이 게임 전체에 일반화됨이 사실상
확정됨. real_tile 기준 고유 타일 수는 1,583개(코드 1,591개 중 8개는 동일 타일을 가리키는 구조적
별칭, bank-2/low+128 관계로 설명됨 — 오류 아님). 이 맵은 "이 타일은 이미 다른 대사가 쓰고 있으니
한글 신규 글리프 배정 시 피해야 한다"는 충돌 검사의 기준표로 쓰임(아래 오프닝 장면 작업 참고).

## 오프닝 장면 완역 파이프라인 실기 검증 + 블록 길이 고정 필수 발견 (2026-08-05)

지금까지는 "1글자→1타일" 식으로 원문과 정확히 같은 글자 수만 치환하는 테스트만 했음. 이번엔 실제
자연스러운 한국어 문장으로 번역하고 `.mes` 토큰 스트림 자체를 재작성(대사 블록 길이가 원문과 달라져도
되는지)하는 것까지 포함한 전체 파이프라인을 `unpack/data/Script/dom1OP_0701_1.mes`(대지가 늦잠 자다
지각 위기에 처하는 오프닝 기상 장면)로 검증.

**대상 범위**: `find_dialogue_blocks()`로 경계가 확실한 7개 블록 — (3682,3687) ……ん…… /
(3710,3729) う、ううん……もう食べられないよ…… / (3758,3766) ……。ああっ！？ /
(3796,3830) 時計時計……って もう８：３０じゃねーか！ / (3860,3882) あ、アテナか？ちょっと待て、
いま開けるから / (3988,3999) 出るって、まだパジャマじゃない。 / (4066,4071) おっとっと.
(3690~3709 구간은 주인공 이름으로 추정되는 저신뢰 글자 2개가 있고 자동 블록 검출도 못 잡아 스코프
제외, 4092 이후 94토큰 블록도 전사 미완료라 제외.)

**신규 타일 할당 전략**: 필요한 한글 음절(약 38개) + 공백 마커를 `full_code_tile_map.json` 기준
게임 전체가 **한 번도 쓰지 않는** 타일 주소(tile 10000번대부터 탐색, bank≈156 — 실사용 bank 범위
1~208 안쪽이라 "엔진이 특별 취급하는 예약 영역"일 가능성도 확인해서 배제함)에 새로 할당해
`Font_DOM.nbfc`를 리페인트(`analysis/opening_scene_font.py`). 기존 문장부호(…！？。)와 반각
숫자(8,3,0)는 파일에 이미 쓰이는 코드를 그대로 재사용. 이렇게 하면 다른 미번역 일본어 대사가 쓰는
타일과 절대 안 겹쳐서, 이 씬만 번역해도 게임 전역의 다른 대사에 영향이 안 감(위 "폰트 타일 공유"
발견과 대칭되는 안전장치).

**1차 시도 — 실패, 실기에서 치명적 버그 발견**: 7개 블록을 자연스러운 길이로(원문과 다른 토큰 수로,
전체 6948→6935 토큰) 재작성 → `NitroPacker pack`/`unpack`/`diff -rq`로 **왕복 바이트 무결성은
완전히 통과**(`.DS_Store`/프로젝트 json 이름 차이 외 0줄 diff) → 하지만 **실제 melonDS 실행에서
버그 발생**: 가장 많이 줄어든 블록(34→24 토큰, -10)에서 스피커 이름이 엉뚱하게(「アンディ」) 바뀌고
텍스트 앞부분이 깨진 문자로 나오다가, 그 대사에서 게임이 멈춰 진행이 안 됨. **핵심 교훈**:
`diff -rq` 왕복 검증은 "NitroPacker가 파일 바이트를 그대로 보존하는가"만 확인할 뿐, 파일 내부(또는
게임 엔진의 다른 어딘가)에 **대사 블록 길이에 의존하는 구조**(오프셋 포인터/페이지네이션 프리컴퓨트
등, 정확한 메커니즘은 ARM9 디스어셈블리 없이는 미상)가 있는지는 전혀 검증 못 함 — 이번 실전에서
처음 드러난 리스크.

**2차 시도 — 성공, 사용자 실기 확인**: 번역 문구는 유지하되 **각 블록의 총 토큰 개수를 원본과
정확히 동일하게** 맞춤 — 짧아진 블록은 끝에 안 보이는 공백(SP/blank) 타일로 패딩, 유일하게
원문(11토큰)보다 길어졌던 블록 하나만 "나간다며 아직잠옷잖아"로 표현을 축약해 정확히 11토큰으로
맞춤. 전체 파일 토큰 수가 6948로 원본과 완전히 동일하게 재빌드 → pack/unpack 왕복 검증 통과 →
**melonDS 재테스트 결과 정상 통과 확인**(사용자: "좋아 잘 넘어가는거 같아"). 빌드물:
`/tmp/opening_scene_korean_v2.nds`. 관련 스크립트: `analysis/opening_scene_font.py`(타일 할당+
리페인트, 결과 `analysis/opening_scene_glyphs.json`), `analysis/rewrite_opening_scene.py`(.mes
재작성 — 블록별 `assert len(new_content) <= old_len`으로 원문 초과 방지 + 부족분은 SP 패딩으로
길이를 원본과 강제로 맞춤, 원본 백업 `analysis/dom1OP_0701_1.mes.orig_backup`).

**⚠️ 앞으로 반드시 지켜야 할 규칙**: 이 게임의 `.mes` 스크립트(혹은 이를 참조하는 다른 구조)는
대사 블록 길이가 고정되어 있다고 가정하는 것으로 강하게 의심됨. **모든 향후 번역은 각 대사 블록의
토큰 개수를 원본과 정확히 동일하게 유지**해야 함(자연스러운 번역 + 부족분은 보이지 않는 패딩,
초과하면 표현 축약). 위 "번역용 일괄 추출/재삽입 파이프라인"(`translate_io.py`,
`mes_translate_reinsert.py`) 절에서 언급한 "블록 길이가 달라져도 다음 블록 위치가 안 꼬인다"는
설계는 **패키징 레벨에서는 여전히 맞지만, 게임 실행 레벨에서는 위험하다**는 것이 이번에 추가로
확인된 것 — 향후 `mes_translate_reinsert.py`로 일괄 재삽입할 때도 번역문 길이(placeholder 개수뿐
아니라 전체 토큰 수)를 원문과 맞추는 검증 단계를 추가해야 함(**아직 미구현, TODO**). `diff -rq`
왕복 검증은 필요조건일 뿐 충분조건이 아니므로, 항상 melonDS 실기 테스트를 최종 확인 단계로 삼을 것.

**다음 단계**: 이 표준 절차(미사용 타일 신규 할당 + 블록 길이 고정)를 다른 씬으로 확장 — 아직 손대지
않은 `dom1OP_0701_1.mes`의 나머지 구간(이름 추정 2글자, 94토큰 블록)과 다른 스크립트 파일(아테나
장면 등)이 후보.

## `mes_translate_reinsert.py` 블록 길이(토큰 수) 검증 추가 완료 (2026-08-06)

이전 세션 TODO였던 항목(남은 선택 항목 #4) 해결. 오프닝 장면 실기 테스트로 "대사 블록의 토큰 수가
원문과 달라지면 게임이 멈춘다"가 확인됐는데, 그동안 `mes_translate_reinsert.py`는 `<HEX>` 플레이스홀더
**개수**만 검증하고 블록 전체 토�큰 수는 검증하지 않아 일괄 재삽입 시 이 버그를 그대로 재현할 위험이
있었음.

- `build_file()`에서 블록별로 원본에서 새로 계산한 `(s, e)` 경계 기준 `expected_len = e - s`를 구하고,
  `text_to_tokens(dst_text)` 결과 길이와 정확히 일치하는지 검사하도록 추가. 불일치 시 플레이스홀더
  오류와 동일하게 해당 블록을 `problems`에 기록 → 파일 전체 스킵(부분 저장 안 함), 사유에 원본/번역
  토큰 수와 방향(짧음/김)을 명시.
- **의도적으로 자동 패딩은 넣지 않음** — 패딩용 blank/filler 코드는 폰트/씬마다 새로 할당해야 하는
  결정(`opening_scene_font.py`에서 한 것처럼)이라 이 범용 일괄 파이프라인의 책임 밖으로 봄. 검증만
  하고, 실제 길이 맞추기(패딩 또는 표현 축약)는 번역자/후속 스크립트가 담당.
- **검증**: 빈 번역(=원문 그대로) 847개 파일 재실행 → 이전과 동일하게 0건 스킵 확인(회귀 없음).
  `dom1Athena_L01.mes` 블록0(원본 10토큰)에 대해 (a) 3토큰짜리 번역 → "shorter" 메시지로 정확히
  스킵, (b) 12토큰짜리 번역 → "longer" 메시지로 정확히 스킵, (c) 같은 10글자를 순서만 뒤집은 번역 →
  정상 통과, 세 케이스 모두 의도대로 동작 확인.

## analysis/ 폴더 정리 (2026-08-06)

103MB → 2.4MB로 정리. 초기 탐색 단계 중복 스크립트, 코드→타일 공식 역산용 1회성 비트 이분탐색/
그라운드트루스 프로브 스크립트, 1회성 디버그 시각화 스크립트, 대용량 melonDS 테스트 빌드
(`probe_*.nds/.sav`, 결론은 이미 이 문서에 기록됨), 이미 해결된 이슈의 렌더링 이미지, 빈 번역
상태의 재생성 가능한 `translated_output/`을 삭제. 상세 목록은 삭제 당시 대화 기록 참고.

**주의 — 백업 두 개는 `analysis/backups/`로 이동해 보존, 삭제하지 않음**: 정리 시점에
`unpack/data/Font_DOM.nbfc`와 `unpack/data/Script/dom1OP_0701_1.mes`가 각각의
`.orig_backup`과 달랐음 — 즉 **오프닝 장면 한글화 실험이 되돌려지지 않고 `unpack/`에 그대로
남아있는 상태**(위 "오프닝 장면 완역 파이프라인" 절의 2차 시도 결과물)였음. 이 두 백업이 해당
두 파일의 유일한 일본어 원본 복원 지점이라 삭제하지 않고 `analysis/backups/`로 옮김:
- `analysis/backups/Font_DOM.nbfc.orig_backup`
- `analysis/backups/dom1OP_0701_1.mes.orig_backup`

`rewrite_opening_scene.py`의 `BACKUP` 상수와 `paint_hiragana_range_korean_v6.py`의
`nbfc_backup`/`fm_backup` 경로도 `analysis/backups/` 아래를 가리키도록 함께 갱신함(다음
실행부터 새 백업도 이 폴더에 쌓임). 다른 3개 대사 백업(`dom1OP_0701_2/3/4.mes.orig_backup`)과
`dom1Athena_L01.mes.orig_backup`, `font_map_full.json.pre_hiragana_probe(_v6)_bak`은
`unpack/`이 이미 원본과 동일함을 `cmp`로 확인 후 삭제함.

## `find_dialogue_blocks()` 치명적 버그 발견/수정 + v8 최종 알고리즘 확정 (2026-08-06)

사용자가 "CSV 대사가 전부 주인공 대사뿐이다(아테나가 같이 나오는 씬에도 아테나 대사가 없다)"고
지적 — 조사 결과 실제 파서 버그였음. 기존 로직은 `0x0002`를 정방향으로 찾고 다음 `0x0000`까지를
블록으로 간주했는데, 헤더(타이밍/좌표 카운터)에 우연히 등장하는 가짜 `0x0002`/`0x0000`에 걸려
그 사이 진짜 대사 블록을 통째로 건너뛰었음. 1차 수정: 종결부 `0x6E5C 0x6E5C 0x0000` 트리플렛을
역방향 기준점으로 삼는 방식으로 교체(17,687블록으로 증가).

이어서 남아있던 미해결 구간("でヤンス" 대사, `0x0002` 마커 없음)을 추적한 결과 더 근본적인 문제를
발견: 블록 헤더의 마지막 정수는 `0x0002` 고정값이 아니라 `0x0003/0x17/0x18` 등 다양한 값을 갖는
필드였고, 종결 마커도 `0x6E5C 0x6E5C 0x0000`이 아니라 **`0x6E5C 0x6E5C` 두 토큰만으로 결정**(뒤
토큰은 임의)임을 확인. **v8 최종 알고리즘**: (1) `0x6E5C 0x6E5C` 연속 등장을 경계로 삼고,
(2) 경계 사이에서 `font_map_full.json`(`CODES`)의 `kind=='full'`로 확인된 첫 토큰을 콘텐츠
시작점으로 삼으며(고정 마커 값에 의존 안 함), (3) 시작점 바로 앞에 붙은 `kind=='half'` 토큰(반각
숫자 등)까지 포함해 시작점을 당김. 결과: 38,123블록(거의 2배 회수). 부수 오염 2건도 함께 정리:
(a) 캐릭터/의상/표정 테스트 메뉴(`0x0087` 구분자, 73건)와 "다음 페이지"로 끝나는 사운드 테스트
메뉴(37건)를 `mes_translate_extract.py`의 `is_debug_menu_block()`에서 필터링, (b) 모든 대사 끝에
항상 붙던 한자 "糠"이 텍스트가 아니라 페이지송출/입력대기 제어코드(`0x485C`)였음을 발견 —
`build_font_map_v2.py`의 `FULL_CHARS`에서 `"485C":"糠"` 제거, `<485C>` HEX 폴백으로 정리.

**Why**: `0x0002`라는 특정 값에 의존하는 한 "일부만 맞는 휴리스틱"에 머무를 수밖에 없었음 — 이
게임 포맷은 헤더 마지막 필드도 종결 마커 뒤 토큰도 가변 필드였고, corpus 전체 실측 코드 분류
(`CODES`)를 신뢰하는 것이 유일하게 일반화되는 방법이었음.

## 화자(speaker) ID 필드 확정 + CSV speaker 컬럼 추가 (2026-08-06)

사용자 제안("스크립트 파일명에 캐릭터 이름이 있으니 그걸로 교차검증하면 될 것 같다")으로 확정:
`find_dialogue_blocks()`가 반환하는 `(start, end)`에서 `values[start-1]`(콘텐츠 시작 직전, 가변
헤더의 마지막 정수)이 화자/라인타입 ID임을 파일명 기반 교차검증(865개 파일을 캐릭터별로 그룹핑해
헤더값 분포 비교)으로 확인. `0x2`는 모든 파일에서 압도적 1위(주인공), 이름 있는 캐릭터 24명은
각자 고유 ID를 보유. `dom1Mai_L02.mes`로 화자 전환 지점과 ID 전환 지점이 정확히 일치함을 결정적
검증. 예외 2개: `0x0`=선택지(대사가 아니라 옵션 2개 이어붙은 형태), `0xffff`=화자없는 효과음/
내레이션. 결과물: `analysis/speaker_map.py`(`SPEAKER_NAMES` 24명 + `SPECIAL_NAMES`,
`speaker_of()`), `mes_translate_extract.py`에 `speaker` 컬럼 추가(미확인 ID는 `UNKNOWN_0x..`로
보존). UNKNOWN 비율 14.3%(5,460행) — 자기 파일 없는 조연급 캐릭터로 추정, 후속 조사 후보로 남김.

## "화자ID 자리 역감사"로 0x412류 숨은 전각 글자 4개 추가 확정 (2026-08-06)

`full_code_tile_map.json` 스캔은 "이미 콘텐츠로 인식된 코드만 재확인 가능"한 자기참조 사각지대가
있음(정확히 `0x412`=♪가 숨어있던 원리 — corpus 전체 66회 실사용에도 스캐너가 v8 재작성 이전
스냅샷이라 못 잡았던 회귀 버그였고, 재스캔으로 우선 수정함). 이 사각지대를 정면 감사하기 위해
`find_dialogue_blocks()`가 찾은 모든 블록의 `values[start-1]`(화자ID 자리)을 `decode_value()`
기준 순수 산술 분류해, `full`/`half`(진짜 글자 모양)로 나오는 값을 전부 추출(진짜 화자ID는 전부
`ctrl` 범위 정수여야 하므로 이상 신호). 30개 후보 중 4개(`0x0402`,`0x0410`,`0x031E`,`0x010C`)가
새 후보였고, 문맥·타일 시각 확인(`temp/render_tilestrip.py`)으로 3개 확정: `0x0410`(타일272)=`◎`,
`0x031E`(타일222)=`×`(둘 다 `0xFFFF` 직후·짧은 의성어 직전에서 `ピンポーン`/`ブー`와 짝을 이루는
일본식 정답/오답 표시), `0x0402`(타일258)=`<`(이미 확정된 `0282`=`<`의 구조적 별칭, bank+2/low-128
관계로 동일 타일 — 짝 `0x0404`=`>`와 함께 `<アイントリガー>` 같은 KOF풍 필살기 이름을 감쌈).
`0x010C`(타일76="S")는 여러 캐릭터 파일에 걸쳐 완전히 동일한 헤더+"藤堂です"(공용 전화 통화 씬)
뒤에서만 반복돼 재사용 씬ID 필드로 판단, 라벨 없이 보류. `build_font_map_v2.py`에
`MANUAL_FULL_CODES` 딕셔너리 신설(콘텐츠에 한 번도 안 잡힌 코드를 강제로 `kind='full'` 주입).
재빌드 후 블록 수 38,154 그대로 유지(파서 경계는 이미 맞았고 라벨만 비어있던 케이스).

**Why**: 재스캔만으로는 "한 번도 콘텐츠로 인식된 적 없는 코드"를 원리적으로 절대 발견할 수 없음
(스캐너가 `find_dialogue_blocks()`→`CODES` 순환 의존) — 화자ID 자리 값의 순수 산술 타당성 검사가
이런 류의 숨은 버그를 잡는 유일한 일반적 방법.

**How to apply**: `find_dialogue_blocks()`/`CODES` 판정 로직을 바꿀 때마다 (1) `scan_all_codes_v8.py`
류로 `full_code_tile_map.json` 재생성 → (2) `build_font_map_v2.py`로 `font_map_full.json` 재빌드 →
(3) `mes_translate_extract.py`로 `translation_export.csv` 재생성을 한 세트로 실행할 것. 추가로
모든 블록의 `values[start-1]`을 `decode_value()`로 직접 검사해 "화자ID치고 너무 글자처럼 생긴 값"이
남아있는지 주기적으로 재점검할 것.

## 주인공 이름 변수 마커 `0x505C`(+`0x3131`/`0x3232`) 확정 (2026-08-06)

사용자가 CSV의 `<505C><3232>` 조합을 보고 "사용자가 입력한 주인공 이름이니 따로 표기하고 유지되게
구분해달라"고 요청. corpus 전체 스캔으로 확정: `0x505C`는 2786회 등장하며 99.7%(2777회)가 바로
다음 토큰으로 `0x3131`(1036회) 또는 `0x3232`(1741회)를 동반하고, 이 두 코드는 **corpus 전체에서
이 위치 외에는 단 한 번도 등장하지 않음**(standalone 0건) — 완전 전용 쌍. 접미사 없는 나머지 9건도
문맥 확인 결과 전부 진짜 이름 자리였음("<505C>君、遅くなって"="~군, 늦어서 미안",
"どうも<505C>です"="저, ~입니다" 등) — 즉 `0x505C` 자체가 핵심 마커이고 `0x3131`/`0x3232`는 선택적
접미(의미 미상: 존칭/격변화 후보이나 미확정). `0x505C`의 real_tile(5212)은 실제로 그리면 정상
한자 "咲"이 나오지만(`temp/render_tilestrip.py 145c`로 시각 확인), `<485C>`가 "糠" 타일을
제어코드로 재활용했던 것과 같은 트릭 — 이 자리는 게임 엔진이 가로채 플레이어 입력 이름으로
치환하는 자리. 접미사 타일(3232→3250, 3131→3185)은 렌더링해보면 완성된 글자가 아니라 깨진 모양이라
애초에 문자로 쓰일 의도가 아니었음도 확인.

**구현**: `translate_io.py`에 `NAME_VAR_PREFIX=0x505C`, `NAME_VAR_SUFFIXES={0x3131,0x3232}` 추가.
`tokens_to_text()`가 이 조합을 감지해 일반 `<HEX>` 폴백 대신 `<이름>`(접미사 없음) /
`<이름:3131>` / `<이름:3232>`(접미사 값을 원본 그대로 보존, 의미는 해독 안 함)로 렌더링.
`text_to_tokens()`에 역변환 추가, `validate_placeholders()`도 이 마커를 카운트에 포함시켜 번역자가
실수로 빠뜨리거나 접미사를 바꿔치기하면 검증 실패하도록 함(순서는 강제 안 함 — 한국어 어순상 이름
위치 이동은 허용). 재생성 결과 블록 수 38,154 그대로 유지, raw `<505C>`/`<3131>`/`<3232>` 누출
0건, 왕복 변환 정상 확인(합계 2786 = corpus 전체 등장 횟수와 정확히 일치).

**Why**: 이 마커는 절대 번역하면 안 되고(플레이어가 실제 입력한 이름이 들어갈 자리), 동시에 블록
길이 고정 제약 때문에 토큰이 하나라도 빠지거나 추가되면 안 되는 이중으로 위험한 자리 — 일반
`<HEX>` 폴백과 똑같이 취급하면 번역자가 "뭔지 몰라서 지우거나 다른 미해결 코드처럼 취급"할 위험이
있어 명확한 한글 라벨(`<이름>`)로 구분해야 한다는 요청이 정확한 실무적 필요였음.

**How to apply**: 번역 파일에서 `<이름>`/`<이름:XXXX>` 마커를 만나면 절대 삭제·복제하지 말고
자연스러운 한국어 어순에 맞는 위치로만 옮길 것. `mes_translate_reinsert.py`는 이미
`validate_placeholders()`를 호출하므로 이 마커 관련 실수는 재삽입 전에 자동으로 걸러짐.

## CSV 편집→재삽입 파이프라인 실전 테스트 + `mes_translate_reinsert.py` 치명적 버그 발견/수정 (2026-08-06)

사용자 요청: "`dom1OP_0701_1.mes` 27번 블록부터 대략 20개(27-46)를 export해서 번역 컬럼을 채우고
실제로 삽입까지 되는지 테스트해보자." CSV 스키마(`file,block,n_tokens,speaker,source,translation`)로
19개 신규 블록 + 이미 2026-08-05에 번역된 7개 블록(30,32,34,36,38,41,43, 그대로 유지) 총 20개 구간을
다뤘다. 테스트 산출물은 전부 `temp/`에 저장(`test_block27_drafts.py`, `build_block27_glyphs.py`,
`build_block27_csv.py`, `run_block27_reinsert.py`, `test_block27_46.csv`, `test_output/Script/`).

**버그 발견**: `mes_translate_reinsert.py`의 `build_file()`이 `mc.find_dialogue_blocks(values)`의
**미필터링** 결과(디버그 메뉴 블록 포함)를 CSV 행 수와 비교하고 있었는데, `mes_translate_extract.py`는
CSV 번호를 매길 때 `is_debug_menu_block()`으로 디버그 블록을 **제외**하고 번호를 매김. 이 파일은
전체 232개 블록 중 113개가 디버그 메뉴(인덱스 2~104, 106~113, 120~121)라서 실제 대사는 119개뿐인데,
reinsert는 232 vs 119로 항상 개수 불일치 판정을 내려 **이 파일을 포함해 디버그 메뉴가 있는 모든
파일을 무조건 스킵**하고 있었다(자동 스킵이라 조용히 묻힐 위험이 있었음 - 전체 코퍼스 재삽입 시
얼마나 많은 파일이 영향받는지는 미확인이나 구조상 디버그 메뉴가 있는 모든 파일에 해당).
`analysis/mes_translate_reinsert.py`를 수정: `mes_translate_extract`의 `is_debug_menu_block()`을
import해서 재삽입 시에도 동일하게 필터링한 블록 리스트로 CSV와 비교하도록 고침(디버그 블록은
필터링만 되고 `values`에는 그대로 남아있으므로 `out.extend(values[last:s])` 구간 복사에서 자동으로
그대로 보존됨).

**신규 한글 폰트 코드 최초로 `font_map_full.json`에 정식 등록**: 그동안 2026-08-05 오프닝 장면
실험은 `temp/opening_scene_glyphs.json`이라는 별도 파일 + 수동 `K()` 룩업으로 `mc.CODES`/
`translate_io.py`를 우회했었음(그래서 CSV 파이프라인은 이 39개 음절을 전혀 몰랐음). 이번에
`temp/build_block27_glyphs.py`가 (1) 기존 39개 오프닝 장면 글리프 + `__SPACE__`(→ 공백 " " 문자로
등록, 앞으로 모든 블록 길이 패딩에 재사용 가능한 공용 blank 글리프)를 `font_map_full.json`의
`codes`에 정식 등록하고, (2) 신규 필요 음절 72개를 `full_code_tile_map.json` 기준 미사용 타일에
새로 렌더링(`AppleSDGothicNeo.ttc`, 기존 `opening_scene_font.py`와 동일한 4분할 16x16 렌더링 방식)
+ `unpack/data/Font_DOM.nbfc`에 기록 + `font_map_full.json`에도 등록(3517→3591 코드).
결과: `translate_io.text_to_tokens()`/`tokens_to_text()`가 이 111개 한글 음절 전부를 다른 기존
글자와 완전히 동일하게 다룸 — 더 이상 임시 스크립트로 우회할 필요 없음. 신규 페인팅 전
`unpack/data/Font_DOM.nbfc`·`analysis/font_map_full.json`·`unpack/data/Script/dom1OP_0701_1.mes`
백업을 `temp/backups_before_block27_test/`에 저장해둠.

**검증 결과**: 번역 채운 11개 블록(27,29,33,35,37,39,40,42,44,45,46 — 나머지 9개는 이미 번역됐거나
순수 구두점이라 CSV에서 빈칸으로 두고 원문 폴백에 맡김) 전부 `validate_placeholders()` +
`text_to_tokens()` 실제 토큰 수가 원본 `n_tokens`와 **정확히 일치**하는 것 확인 후,
버그 수정된 `mes_translate_reinsert.py` 로직으로 `temp/test_output/Script/dom1OP_0701_1.mes` 생성
— 전체 토큰 수(6948) 원본과 동일, 재로딩·재디코딩해서 블록 27~46 전부 의도한 한글 텍스트로
정상 출력됨을 확인(부수 효과로 기존 오프닝 실험 블록 30/32/34/36/38/41/43도 이번에 처음으로
`<9C10>` 같은 raw hex가 아니라 정상 한글 문장으로 렌더링됨 — CODES 등록 전에는 CSV 파이프라인
관점에서 미해독 코드였을 뿐). 새 타일 하나(딩, tile 13390)는 `render_tilestrip.py`로 시각 확인,
글리프 깨짐 없음.

**주의(다음 세션 참고)**: 이번 결과물은 전부 `temp/test_output/`에만 있고 실제 `unpack/data/Script/`
파일은 건드리지 않음(테스트이므로 의도적으로 분리) — 실제 반영하려면 별도로 복사하는 명시적 단계가
필요함. 블록 40/45는 패딩 공백이 많이 들어감(각각 61토큰 중 17개, 95토큰 중 다수) — 파이프라인
동작 검증이 목적이라 자연스러운 줄바꿈 배치까지는 다듬지 않았음, 실제 대사로 확정할 땐 재작업 필요.
`mes_translate_reinsert.py`의 필터 버그 수정은 이 파일 하나만이 아니라 **전체 코퍼스 재삽입에
영향을 주는 수정**이므로, 다음에 `translation_export.csv` 전체를 재삽입할 때 이 수정이 반드시
적용된 상태여야 함(이미 적용됨, 별도 조치 불필요 — 그냥 기록 목적).

## 테스트 결과 실제 ROM 빌드 + 향후 정식 작업 방식으로 확정 (2026-08-06)

위 CSV 편집→재삽입 테스트 결과(`temp/test_output/Script/dom1OP_0701_1.mes`)를 사용자가 "정상반영
되었는지 확인해보게 nds 롬으로 말아달라"고 요청. 절차: (1) 테스트 전 원본 `.mes`를
`temp/backups_before_block27_test/dom1OP_0701_1.mes.bak2`로 재백업, (2) `temp/test_output/Script/
dom1OP_0701_1.mes`를 `unpack/data/Script/dom1OP_0701_1.mes`(작업 카피)에 덮어쓰기 — `Font_DOM.nbfc`는
이미 이전 단계에서 `unpack/`에 반영돼 있었으므로 추가 작업 불필요, (3)
`./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r temp/dom_block27_test.nds`로 ROM 빌드, (4)
`./NitroPacker unpack -r temp/dom_block27_test.nds -o temp/verify_block27 -n VERIFY`로 재언팩 후
`Script/dom1OP_0701_1.mes`·`Font_DOM.nbfc` 둘 다 `unpack/`과 `diff -q` 완전 일치 확인(왕복 무결성
OK). 빌드물: `/Users/hmc/Documents/mds/NitroPacker/temp/dom_block27_test.nds`(16,789,504바이트).
사용자가 melonDS로 실행해서 "잘되는거 같아"라고 확인함.

**⚠️ 향후 한글패치 작업의 정식 워크플로우로 확정**: 사용자가 이 테스트를 근거로 "앞으로 한글패치
작업은 CSV의 한글번역 칼럼을 읽어서 해당 대사를 교체하고 롬에 적용하는 것으로 한다"고 명시적으로
확정함. 즉 앞으로 이 프로젝트의 표준 작업 방식은:

1. `translation_export.csv`(또는 특정 파일/블록 범위만 다루는 소규모 테스트 CSV)의 `translation`
   컬럼에 한글 번역을 채운다(빈 칸은 원문 폴백).
2. 번역에 새 한글 음절이 필요하면 먼저 `font_map_full.json`에 정식 등록 + `Font_DOM.nbfc`에 페인팅
   (이 세션에서 확립한 `build_block27_glyphs.py` 방식 - 미사용 타일에 할당, `font_map_full.json`의
   `codes`에 등록해야 `translate_io.py`가 우회 없이 처리 가능).
3. `mes_translate_reinsert.py`(디버그블록 필터 버그 수정 완료 상태)로 CSV → `.mes` 재삽입.
4. 재삽입 결과를 `unpack/data/Script/`(작업 카피)에 반영.
5. `./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r <output>.nds`로 ROM 빌드.
6. (선택) `unpack`으로 재언팩해 `diff -q`로 왕복 무결성 확인 — 필요조건일 뿐 충분조건 아니므로
   최종 확인은 항상 실기/melonDS 실행으로 할 것([[project_dom_hangulization]] 2026-08-05 길이 고정
   버그 발견 사례 참고).

**Why**: 이 세션에서 실제로 CSV 편집 → 폰트 등록 → 재삽입 → 패킹 → melonDS 확인까지 전체
파이프라인이 한 번의 실전 테스트로 정상 동작함이 검증됐고(부수적으로 corpus-wide 버그까지 발견/수정),
더 이상 씬별 1회성 우회 스크립트(`opening_scene_font.py`/`rewrite_opening_scene.py`식 접근)를 쓸
필요가 없어짐 — CSV 기반 파이프라인이 유일한 정식 경로로 격상됨.

## `tool.md` 기반 Node.js 웹 툴(`webtool/`) 신규 개발 및 엔드투엔드 검증 (2026-08-06)

사용자 요청(`tool.md`)에 따라 지금까지 사람이 손으로 순서대로 실행하던 파이썬 CLI 파이프라인
(`mes_translate_extract.py`→CSV 수정→`mes_translate_reinsert.py`→`NitroPacker pack`)을 하나의
크로스플랫폼 Node.js 웹 GUI로 통합했다. 신규 디렉터리 `webtool/`(`analysis/`·`unpack/`·`temp/`와
같은 층, 리포 루트 기준)에 Express 5 백엔드 + 번들러 없는 순수 HTML/JS 프론트엔드로 구현.

**구조**:
- `webtool/server/lib/`: `lz10.js`(`analysis/lz10.py` 포팅), `mesCodec.js`(`load_values`/
  `dump_values`/`find_dialogue_blocks` 포팅, `font_map_full.json`은 `../../analysis/font_map_full.json`을
  상대경로로 직접 읽어 중복 없음), `translateIo.js`(`tokens_to_text`/`text_to_tokens`/
  `validate_placeholders` + `0x505C` 이름마커 처리 1:1 포팅), `speakerMap.js`, `debugFilter.js`
  (`is_debug_menu_block`), `project.js`(워크스페이스 프로젝트 상태 read/write + NitroPacker
  바이너리 `execFile` 헬퍼), `pipeline.js`(extract/save/reinsert 파이프라인 로직, csv-parse·
  csv-stringify 사용).
- `webtool/server/routes/`: `project.js`(언팩/목록/상태), `csv.js`(추출/파일목록/조회/저장+검증),
  `build.js`(재삽입/패킹/다운로드), `files.js`(이미지 탐색용 트리/원본 스트리밍, 경로탈출 방지).
- `webtool/public/`: `index.html`+`app.js`+`style.css` — 4개 탭(프로젝트/번역 CSV/이미지 탐색/빌드).
- `webtool/workspace/<project>/`: 툴 전용 새 워크스페이스(기존 `unpack/`·`unpack_origin/`은 건드리지
  않음) — `unpack/`(NitroPacker 언팩 결과, 이후 불변)/`translation_export.csv`(세이브 파일)/
  `build/`(재삽입 결과, `unpack/` 통째 복사 후 번역 파일만 덮어씀)/`output/`(패킹된 .nds).
- NitroPacker 실행 파일(`/Users/hmc/Documents/mds/NitroPacker/NitroPacker`, 컴파일된 arm64 바이너리)은
  `child_process.execFile`로 그대로 호출 — 재구현하지 않음.

**범위 제외**(요청사항 문구 "현재 그래픽작업은 하지 않았지만"에 따라): 신규 한글 글리프 페인팅 기능
없음(번역은 `font_map_full.json`에 이미 등록된 문자만 가능, 미등록 문자는 저장/재삽입 시 명확한
오류로 표시). 이미지 탐색은 일반 폴더 트리 + 표준 이미지 포맷(png/jpg/bmp/gif) 미리보기만 제공,
NDS 그래픽 포맷(nbfc/nbfp/nbfs) 디코딩은 하지 않음.

**엔드투엔드 검증 결과** (실제 `Days of Memories.nds`로 `webtool/workspace/webtooltest/`에 테스트):
- 언팩: 865개 `.mes` 파일 확인.
- CSV 추출: 865개 파일 스캔, 853개 파일에 대사 블록 존재, 총 38,154개 블록, 341개 디버그메뉴
  블록 스킵 — 기존 파이썬 파이프라인의 확정 수치(대사 38,154개, `dom1OP_0701_1.mes` 119개 실제
  블록)와 정확히 일치, JS 포팅이 파이썬 원본과 동일하게 동작함을 확인.
- 저장 검증: `dom1OP_0701_1.mes` 27번 블록에 기존 검증된 번역("♪딩동~♪ <485C>", 7토큰)을 저장 →
  OK 배지 확인. 플레이스홀더 누락(`<485C>` 제거) 및 미등록 한자(`龘`) 사용 시 각각 정확한 오류
  메시지 반환 확인.
- 재삽입: 853개 파일 전부 작성, 0개 스킵, 문제 없음.
- 패킹 → 다운로드: 16,789,504바이트 .nds 생성(기존 수동 테스트 빌드 `temp/dom_block27_test.nds`와
  동일 크기), 다운로드 엔드포인트가 스트리밍한 파일이 서버 산출물과 바이트 단위로 동일함을 `cmp`로 확인.
- 왕복 무결성: 빌드된 ROM을 `temp/webtool_verify_unpack`에 재언팩 → `diff -rq`로 워크스페이스의
  `unpack/`과 비교 시 **대사 블록이 있던 853개 `.mes` 파일 전부가 바이트 단위로 다르게 나타남** —
  이는 버그가 아니라 `mes_translate_reinsert.py`와 동일한 기존 동작(번역 여부와 무관하게 재삽입
  대상 파일 전부를 `dump_values()`로 재압축하며, LZ10 압축은 비트 단위로 유일하지 않으므로 내용이
  같아도 압축 바이트는 달라짐). `mc.load_values()`로 재언팩본과 워크스페이스본의 토큰 배열을 직접
  비교해 완전히 동일함을 확인(예: `dom1Athena_L01.mes`, 694토큰 일치) — 무손실 왕복이 유지됨을
  토큰 레벨에서 검증. 27번 블록 내용도 재언팩본에서 "♪딩동~♪ <485C>"(7토큰)로 정확히 확인.
- 병합 로직: CSV를 두 번째로 재추출해도 27번 블록의 번역("♪딩동~♪ <485C>")이 그대로 유지됨을 확인
  (`translatedCount: 1` 유지) — `(file, block, n_tokens, source)` 키 매칭 병합이 정상 동작.
- 이미지 탐색: `/api/files/tree` 루트 조회 정상(`data/`, `overlay/` 등), 경로 탈출 시도
  (`dir=../../../etc`)는 정규화 검사로 차단됨을 확인.
- melonDS 실행 확인은 이번 툴 자체의 검증 범위 밖(사용자가 직접 실행) — 나머지 전 과정은 툴이
  자동화.

**Why**: `tool.md`가 요구한 5가지 항목(크로스플랫폼 웹 툴, 언팩+CSV 자동 생성, CSV 스키마+저장/읽기,
이미지 폴더 미리보기, CSV 기반 재삽입+ROM 빌드)을 전부 구현하고 실제 ROM으로 끝까지 검증했다. 기존
파이썬 CLI 스크립트(`analysis/mes_codec.py` 등)는 그대로 두고 로직만 JS로 포팅했으므로 두 경로
모두 유지 가능하지만, 앞으로는 이 웹 툴이 [[project_dom_hangulization]]에 기록된 "정식 한글패치
워크플로우"(CSV 번역 컬럼 → 재삽입 → NitroPacker pack → melonDS 확인)의 1차 인터페이스가 된다.

**How to apply**: `cd webtool && npm install && node server/index.js` 실행 후
`http://localhost:4000` 접속. 롬 경로는 서버가 접근 가능한 절대경로로 입력. 프로젝트는
`webtool/workspace/<name>/`에 격리되므로 여러 번역 시도를 병렬로 두거나 실패한 시도를 워크스페이스
째로 삭제해도 `unpack/`·`unpack_origin/`·`analysis/` 원본에는 영향 없음.

## `webtool` 언팩 API의 ROM 경로 검증 미흡 버그 수정 (2026-08-06)

사용자가 실제로 툴을 실행해보다가 언팩 시 알아보기 힘든 .NET 예외
(`System.UnauthorizedAccessException: ... Permission denied`,
`NdsProjectFile.Create` 내부 `File.ReadAllBytes` 단계)가 발생한다고 보고. 원인을 재현해보니
`server/routes/project.js`의 `POST /unpack`에서 `romPath`를 `fs.existsSync(romPath)`로만
검증하고 있어서, 사용자가 실수로 `.nds` 파일이 아니라 폴더 경로(`webtool/workspace/test`)를
입력해도 통과되어버렸고, NitroPacker CLI가 디렉터리를 파일로 열려다 위 예외로 죽었다.
실제로 `webtool/workspace/test/unpack/`이 텅 빈 채로 남아있던 것도 이 실패한 시도의 잔해였음
(정리 완료, `project-state.json`이 없어 `listProjects()`에는 애초에 노출되지 않는 상태였음).

수정: `fs.existsSync(romPath) || !fs.statSync(romPath).isFile()` 체크를 추가해 폴더가
입력되면 "ROM 파일을 찾을 수 없습니다(폴더가 아닌 .nds 파일 경로를 입력하세요)"라는 명확한
한글 오류를 반환하도록 함. 서버 재시작 후 폴더 경로/정상 ROM 경로 양쪽 모두 curl로 재검증 완료
(폴더 입력 시 새 오류 메시지 정상 반환, 기존 `webtooltest` 프로젝트 상태 조회는 그대로 정상 동작
— 회귀 없음).

**Why**: 사용자 입력 실수를 서버가 방치하면 원인을 알 수 없는 외부 CLI 스택트레이스가 그대로
노출되어 디버깅이 어려워짐. 파일시스템 경계에서의 입력 검증은 "발생할 수 있는 실패"이므로
방어적으로 처리하는 것이 맞는 경우.

**How to apply**: 앞으로 이 툴에서 언팩 관련 오류가 나면 먼저 입력한 ROM 경로가 실제 `.nds`
파일(디렉터리 아님)을 정확히 가리키는지부터 확인. 서버는 `nohup ... &`로 백그라운드 실행 시
Bash 도구의 셸 종료와 함께 죽는 경우가 있었음 — 세션 내에서 서버를 계속 띄워두려면 Bash의
`run_in_background: true` 옵션을 사용할 것(`nohup`/`disown`만으로는 불충분했음).

## `webtool` 언팩 UI를 경로 타이핑 → 드래그앤드롭/파일선택 업로드로 변경 (2026-08-06)

사용자가 매번 서버 파일 경로를 타이핑하는 게 번거롭다며 브라우저 "열기" 대화상자나
드래그앤드롭으로 경로를 읽어오도록 요청. 브라우저 JS(`File`/드래그앤드롭 API 포함)는 보안상
로컬 파일의 절대경로를 노출하지 않으므로("파일 열기"든 드롭이든 동일하게 `File` 객체의 이름과
내용만 얻을 수 있음), "경로만 읽어오기"는 애초에 불가능하다고 판단. 대신 선택/드롭한 ROM 파일
자체를 서버로 업로드하고, 서버가 그 사본을 `workspace/<project>/original.nds`에 저장한 뒤
그 경로로 NitroPacker unpack을 실행하는 방식으로 변경.

- 백엔드: `POST /api/project/unpack`을 JSON(`{romPath, projectName}`)에서
  `multipart/form-data`(`romFile` 파일 필드 + `projectName` 텍스트 필드)로 변경.
  `multer`(memoryStorage, 512MB 제한) 추가. `.nds` 확장자 아니면 명확한 한글 오류 반환.
  `project-state.json`에 `romFileName`(표시용 원본 파일명)과 `romPath`(서버에 저장된 사본
  경로)를 함께 기록.
- 프론트: ROM 경로 텍스트 입력을 제거하고 클릭(파일 열기 다이얼로그 트리거) + 드래그앤드롭이
  모두 되는 드롭존(`#rom-dropzone`)으로 교체. 선택 후 `FormData`로 업로드.
- 검증: curl로 실제 ROM(`Days of Memories.nds`)을 multipart 업로드해 언팩 성공(865개 .mes
  파일, 기존 알려진 수치와 일치) 확인. `.nds`가 아닌 파일 업로드 시 오류 반환, 파일 누락 시
  오류 반환 확인. 검증용으로 만든 `uploadtest` 프로젝트는 삭제.

**Why**: `tool.md`의 "어느 플랫폼에서도 작업이 가능하도록"라는 요구사항과도 더 잘 맞음 —
서버와 브라우저가 다른 기기여도(예: 같은 네트워크의 다른 기기에서 웹 툴에 접속) 업로드
방식은 그대로 동작하지만, 서버 로컬 경로 입력 방식은 브라우저를 실행하는 기기의 파일
경로를 알 수 없으면 애초에 불가능했다.

**How to apply**: 이제 프로젝트를 새로 만들 때는 프로젝트 이름 입력 후 드롭존에 `.nds`
파일을 드래그하거나 클릭해서 선택하면 됨. 업로드된 ROM 사본은 프로젝트별
`workspace/<name>/original.nds`에 남아있으므로 재언팩이 필요하면 재사용 가능. 이 변경
전에 만들어진 `webtooltest` 같은 기존 프로젝트는 `romFileName`이 없어 프로젝트 목록에
파일명 대신 물음표(`?`)로 표시될 수 있음 — 기능상 문제는 아님(재언팩하지 않는 한
`romPath`/`romFileName`은 조회용일 뿐 재사용되지 않음).

## `webtool` 시작/멈춤 스크립트 `tool.sh` 추가 (2026-08-06)

매번 `node server/index.js`를 직접 실행/종료하는 게 번거롭다는 요청으로 `webtool/tool.sh`
추가. `start`/`stop`/`restart`/`status` 서브커맨드 지원, PID는 `webtool/.server.pid`,
로그는 `webtool/server.log`에 기록(`webtool/.gitignore`에 둘 다 추가). `nohup ... &` +
`disown`으로 백그라운드 실행 후 부모 프로세스가 종료돼도 살아있는지(`PPID`가 `1`로
재부모화되는지) 확인 완료 — 정상적으로 데몬화됨.

**Why**: Claude Code의 Bash 도구로 `nohup ... &`만 실행하면 그 도구 호출(서브셸)이 끝나는
순간 자식 프로세스도 같이 죽는 특이 케이스가 있었음(이전 세션에서 발견,
`run_in_background: true` 옵션으로 우회). 이는 에이전트 환경의 샌드박싱 특성이고, 사용자가
자기 터미널에서 직접 스크립트를 실행할 때는 `disown`이 잡 테이블에서 제거하므로 터미널을
닫아도 정상적으로 살아남는다 — 실제로 `ps`로 `PPID=1`(init에 재부모화)임을 확인해 검증함.

**How to apply**: `cd webtool && ./tool.sh start|stop|restart|status`로 사용. 이미 실행
중이면 `start`가 안전하게 안내만 하고 종료(중복 기동 방지), `stop`은 PID 파일 기준으로
종료. 서버 재시작해도 `workspace/`의 프로젝트 데이터(업로드된 ROM, CSV, unpack/build
결과)는 그대로 유지됨 — 실제로 사용자가 브라우저에서 만든 `dom` 프로젝트가 재시작 후에도
남아있는 것으로 확인.

## 남은 선택 항목 (급하지 않음)

1. `common.mes` QA 문자열에서 발견된 저신뢰 한자/가나 후보
   (`0500,0502,0504,0506`,`0414~041E`)를 실제 대사 문맥에서 교차검증해 확정 시도.
2. ctrl 코드 짝 관계를 더 넓혀서(현재 ratio≥0.8, count≥10만 봄) 전체 710개 중 몇 %가
   짝짓기 패턴에 해당하는지 커버리지 통계 내보기.
3. `.mes` 파일 크기가 원본보다 커지는 경우의 repack 왕복 테스트 (더미 긴 텍스트로).
4. 대사 블록 길이 고정을 요구하는 정확한 메커니즘(오프셋 테이블/페이지네이션 등) 역공학 —
   ARM9 디스어셈블리가 필요해 보류 중. 규명되면 length-lock 제약 없이 자유 길이 번역이 가능해질
   수도 있음.
5. 오프닝 장면에서 검증한 표준 절차(미사용 타일 신규 할당 + 블록 길이 고정)를 다른 씬으로 확장 —
   `dom1OP_0701_1.mes`의 나머지 구간(이름 추정 2글자, 94토큰 블록 미전사)과 아테나 장면 등 다른
   스크립트 파일이 후보.

## 재현 방법

**`analysis/` 루트 — 최종 파이프라인 (현재 유지보수 대상)**:

```bash
cd "/Users/hmc/Documents/mds/NitroPacker/analysis"
python3 mes_codec.py <path.mes>    # 임의 .mes 파일 디코드 + find_dialogue_blocks() v8로 대사 블록 추출
python3 lz10.py                    # LZ10 압축기 자체 round-trip 검증
python3 build_font_map_v2.py       # full_code_tile_map.json + FULL_CHARS/HALF_CHARS/MANUAL_FULL_CODES -> font_map_full.json 재빌드
python3 mes_translate_extract.py   # Script/ 전체(unpack_origin 기준) -> analysis/translation_export.csv (speaker 컬럼 포함, 일괄 번역용 추출)
python3 mes_translate_reinsert.py  # 번역 채운 CSV -> translated_output/Script/*.mes (재삽입, placeholder+블록길이 검증 포함)
# speaker_map.py, translate_io.py는 위 두 스크립트가 import해서 쓰는 모듈(직접 실행 대상 아님)

# find_dialogue_blocks()/decode_value()를 고치면 반드시 이 순서로 재생성 (하나라도 건너뛰면
# 0x412류 회귀 버그 재발):
cd /Users/hmc/Documents/mds/NitroPacker
PYTHONPATH=analysis python3 temp/scan_all_codes_v8.py  # unpack_origin 전체 스캔 -> analysis/full_code_tile_map.json 재생성 (1단계)
cd analysis
python3 build_font_map_v2.py       # -> font_map_full.json 재빌드 (2단계)
python3 mes_translate_extract.py   # -> translation_export.csv 재생성 (3단계)
```

**`temp/`(프로젝트 루트, `analysis/`의 하위 폴더가 아님) — 조사/검증용 1회성 스크립트 (결과물도
여기 유지)**. 여기 스크립트들은 `analysis/`의 `mes_codec.py` 등을 import하므로 `PYTHONPATH=analysis`
지정 필수:

```bash
cd /Users/hmc/Documents/mds/NitroPacker
PYTHONPATH=analysis python3 temp/render_tilestrip.py 3131   # 임의 hex 타일 인덱스 주변 타일시트 렌더링 -> temp/tilestrip_<hex>.png (신규 글자 확정 전 시각 검증 필수)
PYTHONPATH=analysis python3 temp/scan_all_codes_v8.py       # 위 참고 - full_code_tile_map.json 1단계 재생성
PYTHONPATH=analysis python3 temp/decode_bg_image.py <basename> [out.png]  # nbfc+nbfp+nbfs 그래픽 리소스 -> PNG (예: hoshi0001, 00_ATHENA_0, _CAP_B_00)
PYTHONPATH=analysis python3 temp/opening_scene_font.py      # 오프닝 장면용 한글 음절 -> 미사용 타일 신규 할당 + Font_DOM.nbfc 리페인트 (unpack/ 대상, 실험적)
PYTHONPATH=analysis python3 temp/rewrite_opening_scene.py   # dom1OP_0701_1.mes 7개 블록 한글 번역으로 재작성 (블록 길이 원본과 동일하게 고정, 실험적)
# build_full_font_map.py, render_batches.py, render_ambiguous.py, ctrl_analysis.py,
# ctrl_pair_detection.py, analyze_0306.py, decode_common_mes.py, paint_hiragana_range_korean_v6.py,
# scan_all_codes.py(v8 이전 버전) 도 temp/에 남아있음 - 각자 도출 당시 1회성 조사 목적이었고
# 지금은 위 최종 파이프라인으로 대체됨(참고용으로만 남겨둠, 재실행 결과가 최신 CODES를 반영 안 할 수 있음)
```

**리팩 파이프라인** (NitroPacker 자체 pack 명령 활용, 이미 동작 확인됨):

```bash
cd /Users/hmc/Documents/mds/NitroPacker
./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r out.nds
./NitroPacker unpack -r out.nds -o verify_dir -p VERIFY   # 왕복 검증용
```

## `webtool` 이미지 탐색 탭이 아무 파일도 못 읽던 문제 수정 (2026-08-06)

사용자 보고: "이미지 탐색의 경우 아무파일도 못읽고 있어". `GET /api/files/tree`가 디렉터리/파일
목록 자체는 정상적으로 반환하는 것을 curl로 먼저 확인(목록 버그 아님). 실제 원인은 `raw`/`tree`
라우트가 표준 이미지 확장자(`.png/.jpg/.jpeg/.bmp/.gif`)만 `isImage: true`로 판정했는데, 이
ROM의 그래픽 리소스는 전부 NDS 네이티브 `.nbfc`(타일)+`.nbfp`(팔레트)+`.nbfs`(스크린맵) 3파일
세트로 저장되어 있어 표준 이미지 파일이 통째로 하나도 없었기 때문.

이미 `temp/decode_bg_image.py`(2026-08-05 "그래픽 리소스 추출/삽입 검증" 항목에서 실제 ROM으로
검증 완료된 디코더)가 있었으므로 재설계하지 않고 그대로 JS로 포팅:
- `webtool/server/lib/nbfcImage.js` 신규 — `decodeTilemapPng(nbfcBuf, nbfpBuf, nbfsBuf)`.
  LZ10 압축 해제(`lz10.js` 재사용) 후 8bpp 8x8 타일 + RGB555 팔레트 + 스크린맵(bits0-9 타일
  인덱스, bit10 hflip, bit11 vflip)을 그대로 조합, `pngjs`로 PNG 인코딩.
- `webtool/server/routes/files.js` 수정 — `/tree`에서 `.nbfc` 파일 중 같은 폴더에 짝이 되는
  `.nbfp`+`.nbfs`가 모두 존재하는 것만 `isImage: true`로 표시. `/raw`에서 `.nbfc` 요청 시
  짝 파일들을 읽어 `nbfcImage.decodeTilemapPng`로 즉석 디코딩해 PNG로 응답.
- `pngjs` 패키지 신규 설치.

**Why**: 게임 그래픽 자체가 표준 이미지 포맷이 아닌 NDS 전용 타일 포맷이라 브라우저가 원본 바이트를
직접 렌더링할 수 없다 — 서버에서 디코딩해 PNG로 변환해 내려줘야 브라우저 `<img>`로 미리보기 가능.

**How to apply**: `.nbfcn`/`.nbfpn`("n" 접미사) 변형은 포맷 차이가 아직 검증되지 않아 이번
수정 범위에서 제외(미리보기 미지원으로 남음) — 나중에 그 포맷도 검증되면 `hasNbfcSiblings`/
`raw` 라우트에 조건 추가 필요. 검증: curl로 `.nbfc`+짝파일이 있는 항목이 `isImage: true`로
바뀌는 것 확인, 디코딩된 PNG 2개를 다운로드해 file(1)로 포맷 확인 후 하나(`ATHENA_0.png`)를
Read 툴로 직접 시각 확인 — 초록 배경의 캐릭터 초상화가 정상 렌더링됨.

## `webtool` 번역 CSV 탭 개선: 컬럼 폭/다운로드 버튼/dom1·2·3 탭 분리 (2026-08-06)

사용자 요청 3가지를 한 번에 처리:

1. **원문/번역 컬럼 폭 불균형**: `#csv-table`에 `table-layout: fixed` + `<colgroup>`으로 원문·번역
   컬럼을 동일 폭(각 32%)으로 고정, `word-break`/`overflow-wrap`으로 긴 텍스트가 컬럼을
   넘치지 않고 줄바꿈되게 수정(`webtool/public/index.html`, `style.css`).
2. **CSV 저장(다운로드) 버튼**: `GET /api/csv/download?name=`(`webtool/server/routes/csv.js`)이
   `proj.csvPath(name)`(마스터 CSV)를 `res.download()`로 그대로 스트리밍. 프론트는 프로젝트
   선택 시 `<a>` 링크의 `href`를 갱신해두는 방식(별도 fetch+blob 불필요, 브라우저 기본
   다운로드 동작 활용) — `updateCsvDownloadLink()`.
3. **dom1/dom2/dom3 탭 분리**: 파일이 너무 많아(800개 이상) 플랫 드롭다운 사용성이 나빠 게임
   루트 접두사 기준 서브탭 추가. 실제 `GET /api/csv/files` 결과(853개, 디버그 전용 등 일부
   파일은 대사 블록 0개라 제외된 걸로 보임 — `data/Script/` 전체 865개보다 적음)로 접두사별
   개수를 세어보니 dom1=330, dom2=259, dom3=264, 기타=0으로 **이번에도 깨끗하게 3분할**됨을
   확인. 백엔드 변경 없이 프론트에서 이미 받아온 전체 목록을 `state.allFiles`에 캐시해두고
   활성 탭에 맞게 `Array.prototype.startsWith`로 필터링만 함(`renderFileSelect()`).

**Why**: 원문 대비 번역 컬럼이 좁으면 실제 입력 내용을 보면서 편집하기 어렵고, CSV를 텍스트
편집기로 대량 수정하려는 사용자에게는 다운로드 경로가 꼭 필요하며, 800개+ 파일을 플랫 드롭다운
하나에 넣으면 원하는 파일을 찾기 힘들다.

**How to apply**: dom 접두사 분할은 이 프로젝트의 `data/Script/` 디렉터리에 한해 성립하는
전수 확인된 규칙(코드 자체에 하드코딩하지 않고 `startsWith` 문자열 비교로 구현했으므로, 만약
접두사 체계가 다른 새 파일이 추가되면 그 파일은 세 탭 어디에도 나타나지 않게 됨 — 향후 그런
파일이 생기면 "기타" 탭 추가 필요). 검증: curl로 `/api/csv/files`·`/api/csv/download` 정상
동작 확인, 접두사별 개수 재검증(330/259/264/기타 0, 합계 853 일치). CSS/탭 클릭 상호작용의
실제 브라우저 렌더링은 직접 확인 불가 — 코드 리뷰와 API 레벨 검증만 수행함.

## `webtool` 번역 저장 UX 개선(미저장 경고/플로팅 저장버튼/원문복사) + `.bin` 배경 이미지 디코딩 추가 (2026-08-06)

사용자 질문 "번역 입력하면 CSV에 실시간 저장되는거냐"에 답변: **아니오**, 기존 구조는 "저장"
버튼을 눌러야만 서버에 반영되고 그 전까지는 브라우저 메모리에만 있음. 이를 보완하기 위해:

- **미저장 변경 감지**: `translation-input` textarea에 `input` 이벤트 위임 리스너를 달아
  `state.dirty`를 세우고(`markDirty()`), 저장 성공 시/파일 새로 불러올 때 해제(`clearDirty()`).
  파일 드롭다운 변경, dom1/2/3 서브탭 전환, 최상위 탭 이탈(번역 CSV 탭에서 다른 탭으로) 시
  `confirm()`으로 사용자에게 확인받고 취소하면 원래 선택/탭을 유지. `beforeunload`에도 걸어
  새로고침/닫기 시에도 경고.
- **플로팅 저장 버튼**: 기존 `#btn-save-csv`를 `position: fixed`로 화면 우측 하단에 고정 —
  테이블이 길어 스크롤해도 항상 보임. 미저장 상태면 빨간색(`.dirty` 클래스)으로 강조.
- **원문 복사 버튼**: 번역 컬럼 각 행에 "원문 복사" 버튼 추가, 클릭 시 해당 행의 원문
  (`tr.dataset.source`, HTML 이스케이프 이전 원본 문자열)을 번역 textarea에 그대로 채움
  (부분만 수정하고 싶을 때 원문 기반으로 편집 시작할 수 있게).
- **저장 직후 파일 점프 방지**: `renderFileSelect()`가 현재 편집 중인 파일이 그 그룹에 여전히
  있으면 다시 로드하지 않고 드롭다운 표시(번역 개수)만 갱신하도록 수정 — 원래는 저장할 때마다
  무조건 그룹의 첫 파일로 튀었음.
- **"상태" 컬럼 용도**: 저장 시 서버가 반환하는 행별 검증 결과(`report[]`) — placeholder
  불일치, 미등록 문자, 토큰 수 불일치 등을 "OK" 또는 에러 메시지로 표시하는 배지. 검증 실패해도
  저장 자체는 막지 않음(작업 중 임시 저장 허용, `pipeline.saveFile` 참고).

**Why**: 실시간 저장이 아니므로 사용자가 모르고 파일/탭을 이동하면 작업 내용이 조용히 사라질 수
있음 — 명시적 경고와 항상 보이는 저장 버튼으로 데이터 손실을 방지.

---

사용자 지적: "이미지 중 상당수가 `.bin` 파일인데 `.nbfc`는 일부라 실질적으로 소용없다" — 실제로
`data/`의 그래픽 관련 `.bin`은 917개 중 169개(`*_bg_*c.bin` 67 + `*_bg_*s.bin` 57 + `*_p_*.bin`
45, 나머지 `.bin`은 `arm9/arm7/overlay/banner` 등 ROM 시스템 바이너리로 이미지 아님)이며, 기존
`.nbfc/.nbfp/.nbfs` 전용 판정 로직은 이 169개를 전혀 인식하지 못했음.

**조사 결과**: `.bin` 배경 그래픽도 `.nbfc/.nbfp/.nbfs`와 **바이트 단위로 완전히 동일한 포맷**
(LZ10 압축 + 8bpp 8x8 타일 / RGB555 팔레트 / u16 스크린맵)이며, 파일명 규칙만 다름:
- `<A>_bg_<B>c.bin` = 타일(구 `.nbfc`에 해당)
- `<A>_bg_<B>s.bin` = 스크린맵(구 `.nbfs`)
- `<A>_p_<B>.bin` = 팔레트(구 `.nbfp`, 접두사 `_p_`로 구분되고 이름에 별도 접미사 없음)
(`title02_bg_title02c.bin`+`title02_bg_title02s.bin`+`title02_p_title02.bin`으로 실제 LZ10
해제 후 타일 283개/스크린 1024엔트리/팔레트 256색으로 확인, `title02_bg_title02c.bin` 디코딩
결과가 "Days of Memories 2" 타이틀 로고로 정상 렌더링됨을 시각적으로 확인).

**적용 범위**: `*_bg_*c.bin` 67개 중 짝(`s.bin`+`p_*.bin`)이 모두 존재하는 36개는 이번 수정으로
정상 디코딩/미리보기 가능. 나머지 31개(캐릭터 이름표 등, 예: `leona_bg_namec.bin`)는 타일
파일만 있고 스크린맵/팔레트가 별도로 없음 — 다른 캐릭터의 스크린/팔레트를 재사용하는 것으로
추정되나(예: `athena_bg_namec.bin`은 자기 짝 `athena_bg_names.bin`/`athena_p_name.bin`을 온전히
갖고 있음) 어떤 파일을 공유하는지 확인된 규칙이 없어 **추측으로 잘못된 이미지를 보여주지 않도록
디코딩 대상에서 제외**하고 명확한 오류 메시지(`짝이 되는 스크린/팔레트 파일이 없어 디코딩할 수
없습니다`)를 반환하도록 함.

- `webtool/server/routes/files.js`: `BIN_TILE_RE`(`^(.+)_bg_(.+)c\.bin$`)로 `<A>`/`<B>` 파싱,
  `binSiblingNames()`/`hasBinSiblings()`로 짝 존재 확인. `/tree`에서 `.bin` 타일 파일 중 짝이
  완전한 것만 `isImage: true`. `/raw`에서 `.bin` 요청 시 같은 폴더의 스크린/팔레트를 찾아
  기존 `nbfcImage.decodeTilemapPng()`(확장자 무관, 버퍼만 받음)로 디코딩.

**How to apply**: 31개의 짝 없는 캐릭터 이름표(`_bg_namec.bin`류) 디코딩이 필요해지면, 어느
캐릭터가 어느 공용 스크린/팔레트를 쓰는지 먼저 실제 게임 화면(멜론DS 등)에서 확인하거나 ROM의
오버레이 코드(이름표를 그리는 루틴)를 분석해 매핑 규칙을 검증한 뒤 `binSiblingNames()`를
확장할 것 — 검증 없이 예: "athena 걸 재사용" 같은 가정을 코드에 넣지 말 것(시각적으로 확인해보니
entries=39개로 정사각형이 안 맞아떨어져 최소한 이 특정 조합은 틀린 매핑이었음).

## 완성형 한글 2,350자 전체 등록 시도 → 코드 주소체계의 하드 캡 발견(2026-08-06)

**질문**: 완성형 음절(2,350자) 전체를 폰트에 넣을 수 있는가. 사용자가 "1,000자 단위로 크게 한
번에" 단계적으로 늘려보자고 결정하여 `temp/add_wanseong_batch.py`로 1차 1,000자 배치를 실행함.

**치명적 발견**: 폰트 코드는 u16 값이고 `formula.description`대로 `bank=(v>>8)&0xFF, low=v&0xFF`
이며, `real_tile = bank*64 + low`(구 "tile==code" 가정은 틀렸고 이후 타일 오버랩 버그로 수정된
공식, `build_font_map_v2.py`/`full_code_tile_map.json`에서 이미 CONFIRMED로 표기됨 — block27
테스트로 melonDS 실기검증까지 끝난 공식). 이 식에서 **bank는 v의 상위 바이트라 물리적으로 0~255
범위를 못 벗어난다** → 주소 지정 가능한 최대 real_tile은 `255*64+63 = 16383`, 즉 **전체 코드
체계가 표현 가능한 타일은 최대 16,384개뿐**(0번 밴드는 half-width/ctrl 전용이라 실사용 가능
밴드는 1~255).

- 원본 `Font_DOM.nbfc`가 이미 14,336타일(밴드 0~223) 전부를 차지하고 있어서, 새로 추가 가능한
  타일은 딱 `16,384 - 14,336 = 2,048개`(32개 밴드) 뿐. 이번 세션에서 세운 "2×2 블록당 글자 1개,
  밴드(64타일)당 16글자" 규칙으로 계산하면 **신규 확장으로 넣을 수 있는 글자 수는 최대
  32밴드 × 16 = 512자**.
- 여기에 기존 14,336타일 예산 안에서 재활용 가능한 빈 2×2 슬롯이 약 200개 있음(스캔 확인,
  `used_tiles`가 3,591개 코드 기준).
- 따라서 **앞으로 절대 넘을 수 없는 총합 상한은 대략 512 + 200 = 약 712자 신규 등록**,
  기존에 이미 등록된 111자(오프닝 씬)+block27 신규분 등 합쳐 현재 109자 기존 등록분을 더해도
  **총 대략 820자 안팎이 이 코드 체계의 물리적 한계** — 완성형 전체 2,350자의 약 35%밖에 안 됨.
  1,000자 단위 배치는커녕 애초에 1,000자도 다 못 들어감.

**버그 재현 경위**: `temp/add_wanseong_batch.py` 1차 실행(1,000자 목표)이 이 bank≤255 상한을
체크하지 않고 계속 새 64타일 밴드를 파일 끝에 이어붙여서, 배치 뒷부분 글자들(예: "뻐" 이후)의
`tile_to_code()` 결과가 `bank>255`가 되어 u16 범위(0xFFFF)를 초과하는 깨진 코드값을 만들어냄
(예: tile=17544 → bank=274 → code=70152=0x11208, 실제로는 0xFFFF로 랩되면서 완전히 다른 기존
코드와 충돌). **즉시 롤백함**: `unpack/data/Font_DOM.nbfc`·`analysis/font_map_full.json`을
`temp/backups_wanseong_stage1/*.pre_stage1`에서 복원, `diff -q`로 원상복구 확인 완료. 실제
ROM 빌드/melonDS 테스트는 이 버그가 있는 상태로는 진행하지 않았음(사전에 코드값 점검하다가
발견).

**남은 선택지** (사용자 결정 필요, 아직 진행 안 함):
1. **~700자 서브셋으로 타협.** 번역 대본 실사용 빈도 기준으로 상위 약 700자만 골라 이 한도
   안에서 커버 — 코드 체계를 안 건드리는 가장 안전한 경로.
2. **기존 일본어 글리프 전체를 밀어버리고 재사용.** 완역이 목표라면 번역 후에는 기존
   가나/한자 타일이 전혀 필요 없어지므로, 기존 14,336타일 전체(밴드 0~223)까지 포함해
   `real_tile` 재배정 대상으로 삼으면 이론상 최대 `16,384/4 = 4,096`개 슬롯 확보 가능 → 완성형
   2,350자 전체가 들어갈 수 있음. 단, 이건 "빈 슬롯에 새 글자 추가"가 아니라 **모든 대사 스크립트의
   코드 재매핑**(현재 정식 워크플로우 전체가 "코드값은 그대로 두고 char만 새로 등록"하는 전제 위에
   있음)이 필요한 훨씬 큰 아키텍처 변경 — 아직 설계/검증 전혀 안 됨.
3. **자모 조합형으로 전환.** 완성형 대신 초성/중성/종성 낱자(약 68자, 유니코드 조합형 원리)만
   타일로 만들고 게임 렌더러가 이를 조합해서 그리게 하는 방식 — 코드 슬롯은 절약되지만 게임
   엔진이 애초에 가변폭/합성 렌더링을 지원하는지 확인이 안 됨(오히려 렌더링 엔진 자체를 건드려야
   할 가능성이 커서 리스크가 가장 큼, ARM9 패치 영역).

**How to apply**: 완성형 한글 "전체"를 요구하는 다음 요청이 오면, 이 문서의 "812" 언저리
상한 계산과 옵션 1/2/3을 다시 꺼내서 어느 방향으로 갈지부터 사용자와 정할 것 — 임의로 옵션 하나를
골라 진행하지 말 것.

## 완성형 한글 2,350자 전체 등록 완료 — 옵션 2(기존 일본어 글리프 재도색) 실행(2026-08-06)

사용자가 옵션 2로 결정("일단 한글폰트를 다 그려서 맵핑테이블을 만들어놓고 작업하자 오리지널
파일은 따로 분리해놨으니까 언제든 참조 가능") — `unpack_origin/`에 원본이 그대로 보존되어 있으니
`unpack/`은 바로 파괴적으로 수정해도 된다는 판단. 번역 프로토타입(0% 진행) 상태에서 바로 적용할지
사용자에게 재확인하려던 시도는 사용자가 툴콜 자체를 거부하고 직접 지시했음 — 이 프로젝트에서는
애매하면 직접 결정을 내리고 알려주는 쪽을 선호함.

**작업 스크립트**: `temp/build_full_wanseong_font.py` (신규 작성, 1회 실행, 배치 불필요 —
리페인트는 파일을 키우지 않으므로 `add_wanseong_batch.py`처럼 단계적으로 나눌 이유가 없음).

**리페인트 대상 분류** (기존 3,591개 코드 중 `kind=='full'`인 3,463개를 문자 유니코드 범위로
분류):
- 이미 등록된 한글 109자 → 그대로 유지
- 한자 1,363 + 가나(히라가나 79+가타카나 80) + CJK구두점 9 = **tier1 1,531개** → 번역 완료 후
  절대 안 쓰이는 게 확실한 일본어 전용 코드라 안전하게 리페인트 대상
- 숫자/로마자/문장부호/기호 등 37개("keep") → 번역 후에도 계속 쓰이므로 **절대 건드리지 않음**
- `char` 필드가 단일 문자로 안 잡히는(=한 번도 수동 식별 안 된) 1,782개 → **tier2**, 정체불명이지만
  `real_tile` 공식상 분명 실재하는 글리프 타일이라 사용 안 하면 낭비. 단 4개의 센티널 코드
  (`485C`/`505C`/`3131`/`3232` — 이름변수·페이지송 마커, [[project_dom_hangulization]] 참고)는
  tier2에서도 명시적으로 제외해서 `char` 필드를 절대 건드리지 않음(값 자체로 하드코딩된 분기라
  `char`를 세팅해도 로직상 안 깨지지만, 메타데이터 혼동 방지 차원에서 원칙적으로 보존).

**할당 결과** (필요 2,241자 = 2,350 − 기존 109):
- 기존 빈 2×2 슬롯 200개 (순수 추가, 기존 코드 건드림 없음)
- tier1 리페인트 1,531개 (전량 소진)
- tier2 리페인트 510개 (1,782개 중 일부만 사용)
- 신규 타일 추가 0개 — **tier1 전체 소진만으로 2,241자 중 1,731자를 이미 커버**해서 신규
  확장(옵션1 검토 당시의 최대 512자 한도)까지 갈 필요조차 없었음. 사전 낙관적 계산(tier1+free+
  new≈2,243)보다 실제로는 tier2를 조금만 써도 충분했음(tier2 1,782개 중 510개만 사용, 1,272개
  여유분 남음).

**결과**: `unpack/data/Font_DOM.nbfc`는 917,504바이트(14,336타일) **그대로 크기 불변** —
리페인트만으로 완결. `analysis/font_map_full.json` 코드 총수 3,591 → **3,790개**(리페인트는
코드 개수를 안 늘리지만 free-slot 200개분은 신규 코드라 증가). 리페인트된 코드는 원래 문자를
`orig_char` 필드에 보존(예: 감사 목적/향후 원본 참조용), `char`를 새 한글로 교체, `count`는 0으로
리셋. 신규 매핑 전체는 `temp/wanseong_full_glyphs.json`에 문자별 `{tile, code, code_hex, source}`
로 기록(`source`는 `free`/`repaint_tier1`/`repaint_tier2` 중 하나).

**검증**: `translate_io.text_to_tokens`/`tokens_to_text`로 완성형 2,350자 전체 왕복 인코딩 확인 —
`missing from _CHAR_TO_CODE: 0`. 센티널 4개 코드의 `char` 필드는 그대로 미설정 확인
(`485C`/`505C`/`3131`/`3232` 전부 `char` 키 없음, `real_tile`만 유지).
주의: KS X 1001 완성형 2,350자 집합에는 유니코드 한글 음절 블록의 마지막 글자인 '힣'(U+D7A3)이
**포함되지 않음** (EUC-KR로 인코딩 자체가 안 되는 문자 — 완성형은 최빈도 2,350자만 담은 부분집합이라
전체 11,172 현대 한글 음절 중 일부가 원천적으로 빠짐). 검증 스크립트 작성 시 이 점 주의.

백업: `temp/backups_wanseong_full/Font_DOM.nbfc.pre_full`,
`temp/backups_wanseong_full/font_map_full.json.pre_full` (이번 리페인트 작업 직전 상태).

**How to apply**: 이제 완성형 한글 2,350자 전부가 폰트/코드 맵에 등록되어 번역 파이프라인에서
바로 쓸 수 있음. 다음 단계는 (1) 실제 번역 진행 중 미번역 대사가 화면에 뜨면 리페인트된 옛
일본어 타일 자리에 깨진/엉뚱한 한글이 표시된다는 점을 감안해서 QA 순서를 정할 것(전체 번역이
끝나기 전에는 "일본어 참조용 화면"으로 못 씀 — 대신 `unpack_origin/`이 유일한 참조 소스),
(2) melonDS로 실제 렌더링 검증(예: `dom1Athena_L01.mes` 같은 기존 테스트 블록에 새로 등록된
한글 여러 개를 섞어 넣고 타일이 올바르게 그려지는지 확인) 아직 미실시.

## 폰트 리페인트 이후 첫 실전 재삽입 — 미번역 폴백 인코딩 붕괴 발견/수정 + ROM 빌드(2026-08-06)

사용자가 `translation_export.csv`에 번역 몇 줄(주로 `dom1OP_0701_1.mes`, block 26~36)을 채우고
"롬에 적용해서 확인해보자"고 요청. `mes_translate_reinsert.py` 최초 실행 시 **851/853 파일이
스킵**되는 대형 회귀 발견.

**원인 1 (근본 원인)**: `build_file()`이 미번역 블록도 `dst_text = src_text` 폴백 후
`tio.text_to_tokens(dst_text)`로 **매번 재인코딩**하는 구조였음. 위 절("완성형 한글 2,350자 전체
등록 완료")에서 `kind=='full'`인 한자/가나 코드 **1,531개를 전량** 한글로 리페인트했기 때문에,
그 코드들이 예전에 매핑하던 일본어 문자는 이제 `_CHAR_TO_CODE`에 존재하지 않음 — 미번역 원문
전체가 인코딩 자체가 불가능해진 것.
  - **수정**: `mes_translate_reinsert.py`의 `build_file()`에서 번역이 빈 칸이거나 원문과 동일하면
    `text_to_tokens`로 재인코딩하지 않고 **원본 토큰 슬라이스(`values[s:e]`)를 그대로 복사**하도록
    변경. 이렇게 하면 폰트 맵이 어떻게 바뀌든 미번역 대사는 항상 안전하게 보존됨(화면에는 이제
    일본어 대신 한글 모양으로 깨져 보이지만, 게임 자체는 이전과 동일하게 정상 재생됨).

**원인 2**: 남은 5개 파일은 `is_debug_menu_block()`이 `tokens_to_text(values).endswith("次のページ")`
로 디버그 메뉴 블록을 걸러내던 방식이었는데, 리페인트로 그 특정 가나/한자 코드들이 이제 다른(한글)
문자로 디코딩되어 더 이상 문자열이 일치하지 않음 → 예전엔 디버그 메뉴로 분류되던 블록이 갑자기
"진짜 대사"로 재분류되어 CSV 행 수(추출 시점, 리페인트 이전)와 어긋남.
  - **수정**: 이 검사를 디코딩된 텍스트 비교 대신, 리페인트 이전 폰트맵으로 미리 구해둔
    "次のページ" 5글자의 **원시 토큰 값 튜플**(`0x5904, 0xB0A, 0x1102, 0x21E, 0xE1E`)과
    `values`의 마지막 5개 토큰을 직접 비교하도록 변경(`_NEXT_PAGE_TOKENS` 상수, `mes_translate_extract.py`).
    이제 폰트맵이 어떻게 바뀌어도 이 판별은 항상 원본 .mes 토큰 스트림 자체(불변)만 보고 동작함.

**남은 문제(진짜 번역 블록)**: 위 두 수정 후에도 `dom1OP_0701_1.mes`의 실제로 번역을 채운 6개
블록(27,29,32,34,35,36)이 "토큰 수 부족"으로 스킵됨(예: 원문 7토큰인데 번역이 5토큰) — 이는
`mes_translate_reinsert.py`가 의도적으로 자동 패딩을 하지 않기 때문(위 "블록 길이 검증 추가" 절
참고). **기존 컨벤션(`rewrite_opening_scene.py`)대로 각 번역문 끝(`<485C>` 직전)에 공백(" ") 문자를
부족한 토큰 수만큼 채워 넣어** 정확히 원본과 동일한 토큰 수를 맞춤(공백 글리프 코드
`A002`/`real_tile 10242`, kind=`full`, 이미 등록되어 있음 — 1토큰=글자 1개 소비).

**결과**: `mes_translate_reinsert.py` 재실행 → **853/853 파일 스킵 없이 전부 재삽입 성공**.
`analysis/translated_output/Script/*.mes`를 `unpack/data/Script/`에 복사 →
`./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r temp/dom_full_wanseong_test.nds`로 ROM 빌드
(16,789,504바이트) → `./NitroPacker unpack`으로 재언팩해 `dom1OP_0701_1.mes`·`Font_DOM.nbfc` 둘 다
`unpack/`과 `diff -q` 완전 일치 확인(왕복 무결성 OK). melonDS 실기 확인은 사용자 몫(아직 미실시).

**How to apply**: 앞으로 `translation_export.csv`에 새 번역을 채울 때는 (1) 토큰 수가 원문과
정확히 같아야 하며 부족하면 `<485C>`/`<6E5C>` 등 제어 코드 직전에 공백을 채워 맞출 것(자동 패딩
없음, 수동 확인 필수 — `PYTHONPATH=analysis python3 -c` 한 줄로 `translate_io.text_to_tokens(trn)`
길이를 CSV의 `n_tokens`와 비교하는 방식이 빠름), (2) `mes_translate_reinsert.py` 실행 → 스킵 0건
확인 → `unpack/data/Script/`에 복사 → `NitroPacker pack`으로 ROM 빌드가 정식 절차. 위 두 건의
버그 수정(미번역 폴백 원본 토큰 그대로 복사, 디버그 메뉴 판별을 원시 토큰 기준으로 전환)은
이후 재삽입 전부에 항상 적용된 상태여야 함(이미 코드에 반영 완료).

## 완성형 전체 리페인트 롤백 → 한자 전용 리페인트 + 자모빈도 서브셋으로 전환(2026-08-06)

melonDS로 `temp/dom_full_wanseong_test.nds`를 실행해본 사용자가 "특수문자랑 구두점등등이 제대로
안나오는 문제가 있다"고 보고. 원인 조사 결과 위 "완성형 한글 2,350자 전체 등록" 절의 tier2(정체불명
1,782개 중 510개 리페인트) 단계에 **타일 공유 충돌**이 있었음이 확정됨: `font_map_full.json`의
모든 `full`/`half` 코드를 `real_tile` 기준으로 묶어보면, 동일한 `real_tile`을 가리키는 서로 다른
코드가 다수 존재하고(원본 ROM이 애초에 타일 픽셀 데이터를 코드 간에 재사용) 이 중 **13건**이
"keep"(절대 건드리면 안 되는 숫자/로마자/문장부호, 예: `0x0306`='…', `0x0312`=')', `0x0508`='A',
`0x0500`='6', `0x0600`='M' 등)과 tier2(리페인트 대상)가 같은 타일을 공유하는 경우였음. tier2
분류가 "이 코드 자신의 `char`만" 보고 판정했지, 그 코드의 `real_tile`을 다른 카테고리가 이미
쓰고 있는지는 검사하지 않아서, tier2를 리페인트하면서 그 타일을 공유하던 keep 문자의 픽셀 데이터가
함께 덮어써짐 — `font_map_full.json` 메타데이터는 keep 문자 그대로인데 실제 화면에는 리페인트된
한글 모양이 나오는 방식으로 깨짐(사용자가 본 "특수문자 안나옴" 증상의 정체).

**사용자 결정**: "반각렌더링과 특수문자 부분은 유지하고 한자영역만 한글로 바꾸자, 완성형 전부
넣지 말고 일상적으로 사용하는 글자만 넣어서 한자영역만 한글로 교체하는 방식으로 하자." →
AskUserQuestion으로 "일상적으로 사용하는 글자" 선정 방법을 확인, **자모(초성/중성/종성) 빈도 기반
추정**을 선택함(KS X 1001 순서 그대로 자르기, 수동 리스트 제공 등 다른 옵션 대신).

**롤백**: `unpack/data/Font_DOM.nbfc`·`analysis/font_map_full.json`을
`temp/backups_wanseong_full/*.pre_full`(완전 리페인트 이전 원본 상태)로 복원, `diff -q`로 원상복구
확인. 완전 리페인트 상태 자체도 나중에 참고할 수 있게
`temp/backups_wanseong_full/*.pre_full_v2_backup_before_rollback`로 별도 보존. **`unpack/data/Script/*.mes`
(번역 CSV 반영분)는 롤백하지 않음** — 이 파일들의 실제 번역 내용(사용자가 CSV에 직접 채운 대사)은
폰트 이슈와 무관하게 유효한 작업물이므로, 새 폰트맵이 준비되는 대로 재삽입(`mes_translate_reinsert.py`)
을 다시 돌려서 최신 상태로 맞추는 쪽을 택함(전체 rebuild 방식, 파일 단위 되돌리기 아님).

**새 스크립트**: `temp/build_kanji_only_hangul.py`. 핵심 차이:
1. **안전한 리페인트 대상 = "한자로만 분류되는" 코드가 아니라 "그 real_tile을 오직 한자 카테고리
   코드만 쓰는" 코드.** 모든 `full`/`half` 코드를 `real_tile`별로 묶어 카테고리 집합을 구하고
   (`{hangul,hiragana,katakana,kanji,cjk_punct,keep,sentinel,unresolved}`), 그 집합이 정확히
   `{kanji}` 하나뿐인 코드만 후보로 채택. 이번 세션 실측: 한자 분류 코드 1,363개 중 안전한 건
   1,362개(단 1개, `real_tile 1876`=`0x1D14`(遠)가 `unresolved` 코드 `0x1B94`와 타일을 공유해서
   자동 제외됨 — 렌더링으로 원본 한자 '遠'이 그대로 남아있음을 시각 확인, `temp/tilestrip_0754.png`).
   이번엔 hiragana/katakana/cjk_punct/unresolved/keep 카테고리는 **전혀** 건드리지 않음(이전
   시도의 tier1/tier2 개념 자체를 폐기) — 이 카테고리들이 얽힌 25개 타일 충돌(예: `0x0306`='…'+
   `0186`unresolved, `0x0508`='A'+2개 unresolved 등)은 손대지 않은 카테고리이므로 자동으로 안전.
2. **자모빈도 스코어링**: `CHOSEONG`(19)/`JUNGSEONG`(21)/`JONGSEONG`(28, 받침없음 포함) 각각에
   국어 일반 지식 기반 근사 가중치(외부 코퍼스 없음, 이번 세션에서 상식적으로 구성 — `ANALYSIS_NOTES.md`에
   투명하게 기록해두는 근사치이지 확정 통계 아님)를 부여, `freq_score(ch) = CHO_W×JUNG_W×JONG_W`로
   완성형 2,350자 중 미등록 글자를 정렬해 상위 N개 채택.
3. **실사용 우선순위 보정**: `translation_export.csv`에 이미 채워진 실제 번역문에서 쓰인 완성형
   글자를 스캔(`used_translation_chars()`)해 자모빈도 순위와 무관하게 **최우선으로** 배정. 순수
   통계 추정보다 이미 작성된 실제 대사가 우선이어야 한다는 판단 — 1차 실행 후 `translation_export.csv`
   검증에서 '콰'/'몇' 2글자가 자모빈도 순위에서 밀려 누락된 게 실제로 발견되어 이 보정을 추가함.

**실행 결과**: 안전한 한자 타일 1,362개 + 기존 빈 2×2 슬롯 200개 = 총 예산 1,562자. CSV 실사용
7글자(우낫웅젠큰콰몇) 전량 우선 배정 후 나머지는 자모빈도 순, 예산 전량 소진(리페인트 1,362 +
free 200). 완성형 총 등록 수 109(기존) + 1,562(신규) = **1,671 / 2,350자**(71.1%, 이전 시도의
2,350/2,350 100%에서 후퇴했지만 특수문자/구두점 파손 위험이 0으로 낮아짐 — 사용자가 명시적으로
선택한 트레이드오프). `Font_DOM.nbfc` 파일 크기 917,504바이트 그대로(리페인트+기존 빈 슬롯만 사용,
신규 타일 확장 없음).

**검증**:
- 타일 충돌 재스캔: 리페인트된 1,362개 코드 중 keep/hiragana/katakana/cjk_punct/unresolved와 겹치는
  건 0건(설계상 애초에 배제됨). 기존에 존재하던 25개 타일 충돌(전부 unresolved와 다른 카테고리 간)은
  이번 스크립트가 손대지 않는 카테고리들끼리의 문제라 그대로 남아있지만 영향 없음.
- `temp/render_tilestrip.py`로 시각 확인: tile 198('…', 타일 인덱스 0xc6)이 여전히 말줄임표
  점 3개로 정상 렌더링(리페인트 대상에서 제외된 keep 카테고리 확인), tile 328('A', 0x148)도 정상,
  tile 1876(한자 '遠', 위 1번 항목)도 원본 그대로 유지, 리페인트된 한자 타일 하나(코드 `0x121E`,
  원래 '愛', 이제 '눅')도 메타데이터·픽셀 모두 정상 반영 확인(저해상도 특성상 다소 흐릿하지만
  기존 검증된 렌더링 파이프라인과 동일).
- `translate_io.text_to_tokens`/`tokens_to_text`로 신규 1,562자 전체 왕복 인코딩 일치 확인.
- `translation_export.csv`의 기존 번역 7개 행에서 쓰인 완성형 글자 전량이 새 폰트맵에 존재함을
  재확인(missing chars: 없음).
- `mes_translate_reinsert.py` 재실행 → 853/853 파일 스킵 없이 재삽입 성공 → `unpack/data/Script/`에
  반영 → `./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r temp/dom_kanji_only_test.nds`로 ROM
  빌드(16,789,504바이트) → `NitroPacker unpack`으로 재언팩해 `Script/` 853개 파일 전부와
  `Font_DOM.nbfc` 모두 `unpack/`과 `diff -q` 완전 일치 확인(왕복 무결성 OK). melonDS 실기 확인은
  사용자 몫(아직 미실시).

**백업**: 한자전용 리페인트 직전 상태 = `temp/backups_wanseong_full/*.pre_full`(완전 리페인트
이전과 동일한 원본, 재사용). 완전 리페인트(폐기된 접근) 상태 = `temp/backups_wanseong_full/
*.pre_full_v2_backup_before_rollback`. 신규 매핑 전체는 `temp/kanji_only_hangul_glyphs.json`에
문자별 `{tile, code, code_hex, source}`(`source`는 `free`/`repaint_kanji`)로 기록.

**Why**: 리페인트 대상 판정을 "이 코드 자신의 분류"만으로 하면 원본 ROM의 타일 재사용 구조를
놓친다 — 안전 여부는 코드 단위가 아니라 **타일(real_tile) 단위**로 판단해야 하고, 그 타일을
공유하는 모든 코드의 카테고리가 전부 리페인트-안전 카테고리여야 한다는 게 이번에 확정된 원칙.
또한 "일상적으로 쓰이는 글자"는 순수 통계 추정(자모빈도)만으로는 실제 작성 중인 번역과 어긋날
수 있으므로, 이미 확정된 실사용 데이터(번역 CSV)가 있으면 그게 항상 통계적 추정보다 우선해야 함.

**How to apply**: 앞으로 폰트에 새 코드를 리페인트/추가할 때는 반드시 (1) `real_tile` 기준으로
그룹핑해 그 타일을 공유하는 **모든** 코드의 카테고리를 확인하고 카테고리 집합이 단일 안전
카테고리일 때만 리페인트, (2) `translation_export.csv`에 이미 쓰인 글자가 있으면 그 글자들을
최우선 배정 후 나머지 예산을 통계적 방법(자모빈도 등)으로 채울 것. 1,671자로도 부족한 번역이
계속 나오면(신규 완성형 글자가 CSV에 계속 추가되는데 예산이 이미 소진된 경우) 이 스크립트를
재실행해서 최신 CSV 기준으로 우선순위를 다시 매기는 것 외에는 방법이 없음(예산 자체가 물리적
한계 — 안전 한자 타일 1,362개+빈 슬롯 200개가 상한) — 이 경우 사용자에게 알리고 소진된 예산
안에서 어떤 글자를 뺄지 다시 결정받아야 함.

## 한글 글리프 굵기 보정 + 반각 공백/콜론 우선순위 수정(2026-08-06)

`temp/dom_kanji_only_test.nds`를 melonDS로 확인한 사용자 피드백: "한글이 너무 굵게 그려져서
가독성이 좋지 않다. 원본 히라가나/가타가나 참고해서 적절한 굵기로 그려라. 반각 렌더링은
유지해야 하는데 공백(' ')이나 ':' 같은 게 반각이 아니라서 간격이 너무 넓어 보인다."

**원인 1 — 글리프가 굵게 나오는 이유**: `temp/measure_glyph_weight.py`로 실측한 결과, 원본
히라가나/가타카나 타일은 전경 픽셀 비율(16×16 중 켜진 비율)이 평균 **9.7~12.1%**이고, 켜진
픽셀은 예외 없이 램프 최댓값(raw byte **15**, 안티에일리어싱 없는 완전한 이진 렌더링)만 사용함.
반면 기존 `build_kanji_only_hangul.py`의 `glyph_tiles()`는 13pt로 16×16 캔버스에 직접 렌더링 후
15단계 램프 중 **가장 가까운 값**으로 매핑하는 방식이라 안티에일리어싱된 가장자리가 그대로
중간 회색조로 남았고(전경 픽셀 평균 램프값 **6.31/15**), 게다가 전경 비율도 **28.2%**로 원본의
2.3배 이상 — 이 두 가지(중간톤 잔존 + 과도한 커버리지)가 합쳐져 "뭉개진 듯 굵게" 보이는 원인이었음.

**수정 1**: `temp/repaint_hangul_thin.py` 신규 작성 — 기존 `font_map_full.json`의 코드/문자 배정은
그대로 두고(재선정 없음), `temp/kanji_only_hangul_glyphs.json` 매니페스트에 있는 1,562개 글리프의
**픽셀만** 다시 그림. 방식: (1) 8배 슈퍼샘플(128×128 캔버스, 폰트 크기도 11pt×8=88pt로 비례
확대 — 처음엔 이 배율 적용을 빠뜨려 전부 빈 타일로 그려지는 실수가 있었고, 최댓값이 30~40/255에
불과한 것으로 발견해 즉시 수정함), (2) LANCZOS로 16×16 다운샘플, (3) 임계값 110/255로 완전
이진화해서 raw byte 15(on)/0(off)만 사용 — 원본과 동일한 무안티에일리어싱 인코딩.
슈퍼샘플을 쓴 이유: 16×16 캔버스에 직접 11~13pt로 렌더링해보면 특정 폰트 크기(예: 12pt)에서
FreeType 힌팅이 깨져 '가' 같은 간단한 글자조차 획 대부분이 사라지는 현상을 발견했음(`temp/
experiment_glyph_weight.py`, `temp/render_weight_candidates.py`로 확인) — 슈퍼샘플 후 다운샘플하면
이 힌팅 드롭아웃이 사라짐(`temp/experiment_supersample.py`로 검증, 드롭아웃 0건).
크기/임계값 조합은 `temp/render_supersample_candidates.py`로 히라가나/가타카나 원본과 나란히
시각 비교해서 11pt+threshold110(평균 커버리지 13.4%, 원본 평균과 근접)으로 확정.
재실행 후 재측정: `hangul_new: avg_nonzero_fraction=0.134, avg_nonzero_ramp=15.00` (원본
히라가나 0.121/가타카나 0.097과 유사한 굵기, 램프값도 원본과 동일하게 순수 이진).
`temp/tilestrip_0042.png`(자유슬롯 '우'), `temp/tilestrip_049e.png`(리페인트 '눅')로 시각 확인 —
이전의 뭉개진 느낌 없이 주변 한자/로마자와 비슷한 두께로 깔끔하게 렌더링됨.

**원인 2 — 공백/콜론이 반각이 아니게 나오는 이유**: `translate_io.py`의 `_CHAR_TO_CODE`는
`font_map_full.json`을 순회하며 `setdefault`로 문자→코드를 만드는데, 같은 문자가 반각(half)과
전각(full) 두 코드에 모두 등록된 경우(예: `':'` → 전각 `0x0283` vs 반각 `0x0084`, 숫자 `5`/`6`/`7`,
`A`/`B`) JSON 딕셔너리에 **먼저 나오는 쪽**이 그냥 이겨버림 — 우연히 전각 쪽이 먼저 나와 있어서
`:`가 항상 전각(16px폭)으로 인코딩되고 있었음. 공백 `' '`은 원래 전각 한정(`0xA002`, 이전 세션에
어떤 스크립트가 빈 이미지를 렌더링해 새로 추가한 16px짜리 필러 타일)이라 아예 반각 대응이
font_map에 없었음 — `temp/probe_half_space.py`로 라벨 없는 반각 코드 23개를 전부 렌더링해서
찾아본 결과 `0x00A4`(count=2, 완전히 빈 타일)가 실제 반각 공백 타일로 확인됨.

**수정 2**: (1) `analysis/font_map_full.json`의 `0x00A4` 엔트리에 `"char": " "` 추가.
(2) `translate_io.py`의 `_CHAR_TO_CODE` 생성 로직을 2-pass로 변경 — half kind를 먼저 채우고
그 다음 full kind로 나머지만 채움(`setdefault`라 이미 채워진 문자는 덮어쓰지 않음). 결과:
`text_to_tokens(' ')` → `0x00A4`(반각), `text_to_tokens(':')` → `0x0084`(반각)로 정상화됨(직접
검증: `벌써8:30잖아!` → `...0x99, 0x84, 0x94, 0x91...`, `딩~동  ` 끝의 공백 2개 → `0xa4, 0xa4`).

**재실행**: `mes_translate_reinsert.py` 재실행(853/853 성공, 0 스킵) → `unpack/data/Script/`에
반영 → `./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r temp/dom_kanji_thin_test.nds`로 신규
테스트 ROM 빌드(16,789,504바이트) → `NitroPacker unpack`으로 재언팩해 `Script/`·`Font_DOM.nbfc`
모두 `unpack/`과 완전 일치 확인(왕복 무결성 OK). `font_map_full.json`의 글자→코드 배정 자체는
이전 세션과 동일(1,671/2,350 wanseong 등록 수 변동 없음) — 이번 수정은 픽셀 렌더링 방식과
공백/콜론 코드 우선순위만 바꾼 것. melonDS 실기 확인은 사용자 몫(아직 미실시).

**Why**: NDS 폰트 타일은 원래 완전 이진(그레이스케일 램프 미사용) 렌더링이므로, 새로 그리는
글리프도 안티에일리어싱 없이 이진으로 맞춰야 원본과 동일한 "굵기 느낌"이 난다 — 그레이스케일
안티에일리어싱은 작은 16×16 비트맵 폰트에서는 오히려 획이 번져 보이게 만듦. 또한 문자→코드
매핑에서 반각/전각이 공존하는 경우 어느 쪽이 이기느냐는 순전히 JSON 딕셔너리 순회 순서라는
우연에 좌우되면 안 되고, 명시적으로 반각 우선 정책을 코드에 박아둬야 함(한국어 표기 관습상
숫자/콜론/공백은 반각이 자연스러움).

**How to apply**: 앞으로 폰트에 새 글리프를 그릴 때는 항상 (1) 슈퍼샘플 렌더링 후 다운샘플+
이진 임계값 방식을 쓸 것(직접 저해상도 렌더링은 폰트 크기별 힌팅 드롭아웃 위험이 있음),
(2) 임계값/크기는 실제 원본 히라가나/가타카나 타일의 평균 커버리지(9.7~12%)를 기준점으로 시각
비교해서 정할 것. 문자→코드 매핑에 반각/전각 중복이 있는 문자를 새로 발견하면 항상 반각을
우선하도록 `translate_io.py`의 2-pass 로직이 이미 처리하므로 별도 대응 불필요 — 다만 새로운
반각 미해결(라벨 없는) 코드에 실제 문자를 배정할 때는 `temp/probe_half_space.py` 패턴대로 먼저
렌더링해서 눈으로 확인 후 `font_map_full.json`에 `char` 필드를 추가할 것.

## `build_kanji_only_hangul.py` 타일 안전성 검사 버그 3종 발견 및 수정 (2026-08-06)

**사용자 피드백(스크린샷 2장)**: "글씨 크기가 뒤죽박죽이네 특수문자는 이상하게 출력되고". 첫 화면
에서 대사 중 일부 글자가 박스/밑줄처럼 이상하게 겹쳐 보이고, 두 번째 화면에서 "몇시지!?",
"8:30잖아!" 같은 특수문자·숫자 줄에서 글리프가 깨져 보임.

**원인 조사**: "한자 전용 리페인트" 스크립트(`temp/build_kanji_only_hangul.py`)의 타일 충돌
안전성 검사 로직 자체에 버그 3개가 있었음 — 굵기 보정(이전 절)과는 무관하게, **애초에 캐릭터
선택/타일 배정 단계에서부터 실제 사용 중인 다른 코드의 타일을 침범**하고 있었음. 검증 스크립트로
`font_map_full.json`의 모든 `full`(2×2 블록)/`half`(수직 2타일 스택, 주소는 `real_tile`이 아니라
`mes_codec.decode_value`와 동일하게 `low=code&0xFF; tile=2*low-14`로 재계산해야 함) 코드의 실제
타일 풋프린트를 코드별로 전수 대조한 결과, 1,562자 배정 중 **13건의 실질 충돌**을 확인:

1. **`tile_cats` 등록이 "주 타일"(`real_tile` 자체) 하나만 태그하고 나머지 풋프린트
   (`+1`/`+32`/`+33`, 혹은 half의 `+32`)는 태그하지 않음.** 원본 폰트는 서로 다른 코드의 2×2
   블록이 모서리에서 맞닿아 타일을 공유하는 경우가 실제로 있는데(의도된 패킹), 이 공유가
   `tile_cats`에서 보이지 않아 "한자 단독 타일"로 오판됨. 실제로 `잭`(신규 배정)이 극도로 자주
   쓰이는(count 1037/1745) 이름 변수 센티널 코드 `3131`/`3232`(`translate_io.py`의
   `NAME_VAR_SUFFIXES`)의 블록 모서리 타일을 덮어씀.
2. **동일한 `real_tile`을 공유하는 서로 다른 두 코드(원본에 존재하는 별칭 관계, 예:
   `575C`(과)와 `581C`(댓) 둘 다 타일 5660)를 구분하지 못함.** 카테고리 집합만 보면 둘 다
   `{"kanji"}`라 안전 판정을 통과해서 **둘 다 독립적으로 같은 물리 타일에 배정**됨 → 나중에
   그려진 쪽이 이전 것을 덮어써서 한쪽 코드는 엉뚱한 글자를 보여주게 됨.
3. **"빈 슬롯" 탐색(`free_slots`)이 `full` kind의 `real_tile` 필드만 보고 `half` kind는 아예
   고려하지 않음** (`half`는 `real_tile`이 항상 `null`이라 존재 자체가 스캔에서 누락). 실사용
   중인(count 46/43) 반각 특수문자 코드 `008F`/`009F`의 실제 타일(272, 304)이 "빈 슬롯"으로
   오판되어 신규 배정된 `익`에 덮어씌워짐 — 스크린샷의 "특수문자가 이상하게 출력" 증상의
   직접적 원인.

**수정**: `temp/build_kanji_only_hangul.py`에 `footprint(k, v)` 헬퍼를 추가해 코드 하나가 실제로
차지하는 전체 타일 집합(`full`=4타일, `half`=2타일, half 주소는 재계산)을 반환하도록 통일하고:
- `tile_cats`뿐 아니라 `tile_touch`(타일별로 그 타일을 건드리는 코드 집합)도 함께 기록.
- 한자 후보 채택 조건을 "후보 코드의 풋프린트에 속한 **모든** 타일에서 카테고리가 `{kanji}`이고
  그 타일을 건드리는 코드가 오직 후보 자신뿐"으로 강화 — 모서리 공유든 완전 별칭 공유든 전부
  걸러짐.
- `free_slots`의 `used_tiles` 계산도 `footprint()`를 통해 `full`+`half` 코드를 모두 반영하도록
  변경.

**재실행**: `temp/backups_wanseong_full/*.pre_full`(한자전용 리페인트 이전, 완전 리페인트 롤백
직후와 동일한 상태)로 `Font_DOM.nbfc`/`font_map_full.json`을 복원한 뒤, 수정된
`build_kanji_only_hangul.py` 재실행:
- 안전 배정 예산: 1,559자(한자 전용 타일 1,359개 + 빈 슬롯 200개; 버그 수정 전 1,561→1,559로
  소폭 감소 — 안전성 우선). 충돌 재검증 스크립트로 **0건** 확인.
- **주의**: 이 복원으로 `font_map_full.json`의 `00A4`(반각 공백) `char` 라벨이 초기화되므로
  다시 추가함. `translate_io.py`의 반각 우선 `_CHAR_TO_CODE` 2-pass 로직은 스크립트 파일 자체라
  복원 영향 없음(정상 확인).
- `temp/repaint_hangul_thin.py`(굵기 보정, 11pt+8×슈퍼샘플+threshold 110)를 새 매니페스트
  (1,559자)에 재실행 → `hangul_new: avg_nonzero_fraction=0.134, avg_nonzero_ramp=15.00` (정상,
  이전 절과 동일 수치).
- `mes_translate_reinsert.py` 재실행(853/853 성공, 0 스킵) → `unpack/data/Script/`에 복사 →
  `./NitroPacker pack` → `temp/dom_kanji_thin_v2_test.nds`. Unpack 왕복 검증(`Script/`,
  `Font_DOM.nbfc` 모두 diff 없음) 통과. 추가로 `008F`/`009F`/`3131`/`3232` 타일이
  `unpack_origin`과 바이트 단위로 완전히 동일함을 직접 확인(더 이상 침범 없음).

**Why**: 타일 하나를 "안전"으로 판정하려면 그 코드가 실제로 그리는 모든 물리 타일 각각에 대해
"이 타일을 건드리는 코드가 나뿐인가"를 확인해야 하며, `real_tile` 필드 하나만 비교하거나 카테고리
집합만 보는 것은 (a) 다른 코드의 풋프린트 모서리와 겹치는 경우, (b) 동일 타일을 가리키는 별칭
코드가 둘 이상 있는 경우, (c) `real_tile`이 애초에 기록되지 않는 `half` kind를 놓친다. 세 경우
모두 "카테고리는 한자뿐"이라는 조건만으로는 걸러지지 않고, 실사용 코드(특히 매우 자주 등장하는
센티널이나 반각 특수문자)를 조용히 파괴한다.

**How to apply**: 폰트 타일 재사용/충돌 안전성을 확인하는 코드를 새로 짤 때는 항상 (1) 대상
코드의 "주소 하나"가 아니라 "실제로 그려지는 전체 타일 집합"을 명시적으로 계산하고(kind별 공식이
다름 — `full`은 2×2, `half`는 `2*low-14` 기준 수직 2타일), (2) 그 집합의 모든 타일 각각에 대해
"단독 소유"를 확인할 것(카테고리 일치만으로는 부족). 이 프로젝트에서 폰트/타일 관련 스크립트를
수정하거나 새로 만들 때마다 이 세 가지 실패 패턴(모서리 공유 누락, 별칭 중복, half 타일 누락)을
체크리스트로 재확인할 것. 백업에서 복원한 뒤에는 그 백업 시점 **이후에** 별도로 적용한 다른 수정
(예: `00A4` 공백 라벨)이 되돌아가지 않았는지 항상 다시 확인.

## `0x00A4` 반각 공백 오식별 — 대사 사이 틱 마크(작은 점/따옴표) 버그 근본 원인 및 수정 (2026-08-06)

위 3종 버그 수정 후 새 테스트 롬(`dom_kanji_thin_v2_test.nds`)을 전달했으나 사용자가 스크린샷
3장과 함께 "별로 달라진게 없는데"라고 재차 보고 — 대사 중 음절 사이/뒤에 작은 틱/따옴표 마크가
계속 나타남(예: "이젠'더'못'먹어……'·'·", "벌써8:30잖아!'·'·'·'·'·"). 이는 이전에 고친 3종
타일 충돌 버그와는 **별개의 원인**이었음.

**조사 과정**: `translation_export.csv`에서 스크린샷과 일치하는 행(예: `dom1OP_0701_1.mes`
block=32, "이젠 더 못 먹어……  " — 트레일링 스페이스 2개가 블록 길이 고정용 패딩)을 찾고,
`mes_translate_extract.py`의 block 인덱싱 로직(`enumerate(real_blocks)`, `real_blocks`는
`find_dialogue_blocks` 결과에서 `is_debug_menu_block` 제외)을 그대로 재현해 실제 `.mes` 파일의
토큰 시퀀스를 직접 디코딩함 → 공백 문자가 전부 토큰 `0xA4`(코드 `00A4`)로 인코딩되고 있음을 확인.

**근본 원인**: `00A4`를 반각 공백으로 확정한 근거(`temp/probe_half_space.py` 렌더링 결과,
"완전히 빈 타일"이라던 이전 세션 판정)가 애초에 틀렸음. `Days of Memories.nds`(원본 리테일 롬)를
`temp/verify_pristine/`으로 새로 언팩해서 직접 타일 바이트를 확인한 결과, `00A4`의 실제 타일
(주소 314/346, `half_tile_addr` 공식 `2*low-14` 그대로)은 순정 상태에서도 완전히 비어있지 않고
314번 타일에 픽셀 7개짜리 작은 흔적 도형이 있었음(346은 비어있음) — 이게 화면에 찍히는 틱
마크의 정체. 게다가 `tile_touch` 매핑으로 확인해보니 314/346 타일은 `00A4` 전용이 아니라
`0094`('3' 숫자 반각), `041A`, `00B4`, `051A` 등 **완전히 무관한 다른 코드들의 2×2 블록 모서리와
겹쳐 공유되는 타일**이었음 — 즉 `00A4`는 "빈 칸"이 아니라 이 폰트 시트가 타일을 극도로 빽빽하게
모서리 공유 방식으로 재사용하는 과정에서 우연히 걸린 반각 주소 하나에 불과했고, 그 위치의 실제
내용은 디지트 '3' 등 다른 글자 모서리의 조각 픽셀이었을 뿐임. 반각 주소 공간(bank=0,
`low`∈[0x80,0xFF], 128개) 전체를 스캔해봐도 순정 롬에는 **완전히 비어있는(모든 픽셀 0) 반각
타일이 단 하나도 없음** — 이 폰트 시트 자체가 여백 없이 빽빽하게 짜여 있기 때문. 따라서 "이미
존재하는 진짜 빈 칸"을 찾는 접근 자체가 애초에 불가능했고, 반드시 하나를 새로 만들어(다른 코드
타일을 덮어써서) 확보해야 했음.

**해결**: 어느 반각 주소를 덮어써도 그 물리 타일을 공유하는 다른 코드가 반드시 존재하므로,
"현재 번역이 끝난 실제 스크립트 파일들에서 그 다른 코드가 정말 한 번도 쓰이지 않는가"를 기준으로
가장 안전한 후보를 골랐음(기존 `font_map_full.json`의 `count` 필드는 번역 전 일본어 원문 기준
통계라 이미 stale — `unpack/data/Script/*.mes`를 직접 전수 스캔해서 **현재** 사용 빈도를 다시
집계해야 함). 그 결과 `00CA`(타일 390/422, 완전히 `0606`의 2×2 블록 안에 포함됨)를 확정 —
`0606`은 현재 번역된 스크립트 전체에서 등장 횟수 0회(완전한 데드 코드)라 이 타일을 지워도 실제
게임에는 전혀 영향이 없음. `00A4`의 잘못된 `"char": " "` 라벨을 제거하고, `00CA`를
`{"kind": "half", "count": 0, "char": " "}`로 신규 등록, 해당 타일(390/422) 픽셀을 0으로 초기화.
`translate_io.py`는 `font_map_full.json`을 순회해 동적으로 `_CHAR_TO_CODE`를 만들므로 코드 수정
없이 `text_to_tokens(' ')` → `0x00CA`로 자동 반영됨(직접 검증 완료).

**재실행**: `mes_translate_reinsert.py` 재실행(853/853 성공) → `unpack/data/Script/`에 복사 →
`./NitroPacker pack` → `temp/dom_kanji_thin_v3_test.nds`. Unpack 왕복 검증(`Script/`,
`Font_DOM.nbfc` diff 없음) 통과. `dom1OP_0701_1.mes` block 32 토큰을 직접 렌더링한
`temp/verify_space_fix_render.png`로 "이젠 더 못 먹어……  " 라인이 공백 위치에 아무 마크 없이
정상 렌더링됨을 시각 확인 — 이전 스크린샷의 틱 마크가 전부 이 공백 위치와 일치했음.

**Why**: 폰트 시트의 "타일이 비어 보인다"는 판단은 반드시 실제 바이트를 직접 읽어 검증해야
하며, 예전에 한 번 확인했다고 기록된 내용도 이후 다른 리페인트/복원 작업으로 상태가 바뀌었을 수
있으므로 재검증 없이 그대로 신뢰하면 안 됨. 또한 이 게임의 폰트 타일 시트는 여백이 전혀 없이
모서리 공유로 빽빽하게 채워져 있어 "완전히 빈 슬롯"이 애초에 존재하지 않을 수 있다 — 이 경우
가장 덜 위험한 후보는 "정적 메타데이터(원본 일본어 corpus 기준 count)"가 아니라 "번역이 끝난
현재 스크립트 전체를 직접 재스캔한 실사용 빈도"로 골라야 한다.

**How to apply**: 어떤 폰트 코드를 "비어있다/안전하다"고 결론 내리기 전에 항상 (1) 진짜 순정
원본 롬(`Days of Memories.nds`를 새로 언팩)에서 해당 타일의 raw 바이트를 직접 읽어 전부 0인지
확인하고, (2) 그 타일을 공유하는 다른 코드가 있는지 `tile_touch`로 확인하고, (3) 있다면 그
코드의 안전성 판단 기준은 번역 전 `count`가 아니라 **현재 시점의 `unpack/data/Script/*.mes`
전체를 직접 스캔한 실사용 여부**로 삼을 것 — 텍스트가 이미 전량 번역/재인코딩된 프로젝트에서는
번역 전 통계가 더 이상 유효한 안전성 근거가 아니다.

## `add_wanseong_batch.py` 산출 109자 굵은 글리프 미적용 버그 발견/수정 + v4 빌드 (2026-08-06)

`dom_kanji_thin_v3_test.nds`(위 `00A4`→`00CA` 공백 수정 반영분)를 melonDS로 테스트한 사용자가
스크린샷 4장과 함께 "글자크기가 여전히 다르고 뭉개져서 잘 알아볼수없는 글자도 많아 공백은
이상하게 깨져 나오고"라고 재차 보고 — v3에서 고쳤다고 생각한 두 문제(글자 굵기 불일치, 공백
깨짐)가 여전히 남아있다는 피드백.

**조사 방법 교정**: `mc.find_dialogue_blocks(values)`는 콘텐츠 자체가 아니라 `(start,end)` 인덱스
튜플 리스트를 반환하고(슬라이싱 `values[s:e]` 필요), `mte.is_debug_menu_block()`은 이미 슬라이스된
토큰 리스트 1개 인자만 받는다(`(values, block)` 2인자로 호출하면 `TypeError`) — 지난 세션에 확립된
API를 다시 잘못 호출해 한 차례 헤맴, 시그니처 재확인 후 정정.

**글자 굵기 불일치 원인 확정**: `font_map_full.json`에서 한글 문자(`'가'<=char<='힣'`)이면서
`kind=='full'`인 항목 1,668개를 전수 조사해, `temp/kanji_only_hangul_glyphs.json`(굵기 보정이
적용된 1,559자 매니페스트)에 없는 **109개**를 발견함. 이 109자는 전부 `temp/add_wanseong_batch.py`
(완성형 2,350자 확장을 위해 별도로 작성된 "단계적 배치 확장" 스크립트, "완성형 한글 2,350자 전체
등록 완료" 절과는 다른 시도 — 그 절은 롤백됨, 이 스크립트는 그 이후 남아있던 별개 산출물)가
등록한 것이었고, 이 스크립트는 `build_kanji_only_hangul.py`와 **동일한 구 굵은/안티에일리어싱
렌더러**(`glyph_tiles()`, 13pt 직접 렌더링+15단계 램프 중 최근접값)를 그대로 재사용하고 있었음 —
`repaint_hangul_thin.py`의 얇은 재도색은 오직 `kanji_only_hangul_glyphs.json` 매니페스트(1,559자)
만 대상으로 했기 때문에, 이후 `add_wanseong_batch.py`가 추가한 109자는 얇은 처리를 한 번도 받지
못한 채로 남아있었음. 이 109자(제,이,어,아,줄,게,더,응,먹,겠,시,벌,써,나,야,잠,깐,만,지,금,열 등)가
정확히 스크린샷에 찍힌 문제 대사("이젠 더 못 먹어……", "아 아톄나야? 잠깐만 지금 열어줄게")의
음절과 일치 — 얇게 그려진 정상 음절과 굵은 미보정 음절이 같은 문장 안에 섞여 나오는 것이
"글자크기가 뒤죽박죽" 증상의 정체였음.

**수정**: 109자 목록을 `temp/wanseong_orphan_glyphs.json`(`{문자: {key, tile}}`)으로 추출 후,
`repaint_hangul_thin.py`와 동일한 알고리즘(11pt, 8배 슈퍼샘플, LANCZOS 다운샘플, threshold 110
이진화)을 쓰는 `temp/repaint_wanseong_orphans_thin.py`를 신규 작성/실행 — `Font_DOM.nbfc`에서 이
109자의 타일 436개(4타일×109자)만 재도색, 파일 크기 불변(917,504바이트) 확인.

**공백 재검증**: `dom1OP_0701_1.mes` block 32를 v8 API로 직접 재디코딩한 결과
`...0x10a,0xca,0x9c1c,0xca,0x9c24,0xca,...` — `00CA` 공백 코드가 정확히 존재하고, `Font_DOM.nbfc`의
타일 390/422(`00CA`의 물리 주소) 둘 다 `nonzero: 0`(완전히 빈 타일)임을 재확인. **v3 수정은
되돌려지지 않았고 여전히 올바르게 적용된 상태** — 이 부분은 새로운 버그가 아니라 아래 "미해결"
항목 참고.

**빌드/검증**: `./NitroPacker pack`으로 `temp/dom_kanji_thin_v4_test.nds`(16,789,504바이트) 빌드 →
`NitroPacker unpack`으로 `temp/verify_unpack5/`에 재언팩해 `Script/`·`Font_DOM.nbfc` 둘 다 원본과
`diff -q`/`diff -rq` 완전 일치 확인(왕복 무결성 OK). 추가로 `Font_DOM.nbfc`+`Font_DOM.nbfp.dec`를
직접 읽어 block 32 토큰을 픽셀 단위로 블릿하는 수동 렌더러(`temp/verify_space_fix_render_v2.png`)로
"우 우 웅…… 이 젠 더 못 먹어…… "가 전부 일관된 얇은 굵기로 렌더링되고 공백 위치에 어떤 마크도
없음을 시각 확인.

**⚠️ 미해결로 남은 사항 — 공백 틱마크가 melonDS 스크린샷에서 여전히 보임**: 토큰 스트림 확인,
타일 바이트 0 확인, 왕복 diff 확인, 픽셀 단위 수동 렌더링까지 모두 "정상적으로 고쳐졌어야 한다"는
결론인데도, 사용자가 보낸 v3 스크린샷에는 `00CA` 공백 위치에 여전히 작은 틱 마크가 보임(예:
"더'못'먹어……'·'·"). 내부 검증 4단계가 전부 동일한 결론을 내는데 실제 에뮬레이터 결과와 어긋나는
원인은 이번 세션에서 규명하지 못함 — 유력한 가설(미확정): (a) 사용자가 v3 스크린샷을 찍을 때
melonDS를 완전히 재시작하지 않고 이전 세션/세이브스테이트를 이어서 썼을 가능성(VRAM/타일 캐시
잔존), (b) `decode_value`/`real_tile` 공식이 놓친 엔진 레벨의 렌더링 차이가 이 특정 코드에서만
드러날 가능성. v4에서는 (b)를 완전히 배제할 수 없으므로, 사용자에게 **melonDS를 완전히 새로
껐다 켜서(이전 세이브/세션 이어받지 않고)** v4를 재확인해달라고 요청 예정 — 그래도 재현되면
근본 원인 재조사 필요.

**Why**: 여러 시점에 서로 다른 스크립트(`build_full_wanseong_font.py`→롤백,
`build_kanji_only_hangul.py`+`repaint_hangul_thin.py`, `add_wanseong_batch.py`)가 제각기 한글
폰트 등록에 손을 댔고, 얇은 렌더링 보정은 그 중 한 스크립트의 매니페스트만 대상으로 삼았기 때문에
그 매니페스트 밖에서 등록된 글자가 조용히 구식 렌더러의 결과물로 남는 사각지대가 생겼음.

**How to apply**: 앞으로 폰트 굵기/렌더링 스타일을 일괄 보정하는 스크립트를 짤 때는 특정 매니페스트
파일이 아니라 `font_map_full.json` 전체에서 해당 카테고리(예: 한글 전체)에 속하는 코드를 다시
스캔해서 대상으로 삼을 것 — 여러 스크립트가 시차를 두고 같은 카테고리에 코드를 추가해온 이력이
있는 만큼, "이 매니페스트에 있는 것만" 가정하면 새로 추가된 항목이 항상 누락될 위험이 있다.

## 공백 틱마크 버그 진짜 근본 원인 발견/수정 + v5 빌드 (2026-08-07)

**재현 보고**: 사용자가 v4(109자 굵은 글리프 수정 반영) 기준 melonDS 스크린샷 4장을 전달하며
"더 알아보기 힘들게 뭉게진거 같아"라고 재차 보고. 스크린샷을 크롭/확대해 직접 확인한 결과
("♪ 딩~ 동ㅈㅈ", "이 젠 ㅈ 뎌 ㅈ 못 ㅈ 먹어…… ㅈ ㅈ", "벌 씨8:30 잖아! ㅈㅈㅈㅈㅈ" 등) —
v3/v4 세션 내내 "미해결"로 남겨뒀던 바로 그 공백 틱마크 버그가 맞았고, 새로운 문제가 아니었음.

**근본 원인**: 지금까지 이 프로젝트 전체에서 써온 half-width 글리프 "footprint" 모델
`{t, t+32}`(공식 `t = 2*low - 14`가 가리키는 타일 + 그 바로 아래 타일, 1열 스택)이 **불완전**했다.
실제 폰트 시트에서 half-width 코드의 타일은 인접한 full-width 라틴 글리프(예: 알파벳 "U" —
타일 314/315/346/347의 2x2 블록)와 물리적으로 같은 2x2 블록을 공유하는 구조로 짜여 있고, 게임
엔진은 half-width 코드를 렌더링할 때도 이 2x2 블록 전체 영역을 스캔/참조하는 것으로 보인다.

`00A4`→`00CA` 공백 재배치 때 zero-out한 것은 half 공식이 가리키는 `{390, 422}`뿐이었음. 그런데
타일 390은 죽은(사용되지 않는) full-kind 코드 `0x0606`(`real_tile: 390`, 전체 corpus 재확인 결과
occurrence 0회, 완전히 안전하게 지울 수 있음이 이번에도 재확인됨)의 2x2 footprint
`{390, 391, 422, 423}` 중 좌측 절반과 겹치는 자리였다. 즉 **타일 391에 그 죽은 코드가 과거에
그렸던 픽셀 잔여물(값 15, 4번째 컬럼 3~6행에 작은 세로선 4픽셀)이 그대로 남아있었고**, 이게
공백을 렌더링할 때마다 화면에 작은 틱마크로 새어나온 것 — 지난 세션에 했던 4단계 내부 검증
(토큰 스트림 확인, half 공식 타일 0 확인, 왕복 diff, 픽셀 단위 수동 렌더러)이 전부 "정상"이라고
결론 내렸던 이유는, 그 검증들이 전부 이 불완전한 `{t, t+32}` 모델만 들여다봤고 타일
391/423(우측 절반)을 한 번도 확인하지 않았기 때문이었다.

**수정**: `Font_DOM.nbfc`에서 타일 391과 423을 64바이트씩 전부 0으로 덮어씀(파일 크기 불변,
917,504바이트 확인). 코드:
```python
for t in (391, 423):
    nbfc[t*64:t*64+64] = bytes(64)
```

**빌드/검증**: `./NitroPacker pack`으로 `temp/dom_kanji_thin_v5_test.nds`(16,789,504바이트) 빌드 →
`temp/verify_unpack6/`에 재언팩해 `Script/`, `Font_DOM.nbfc` 둘 다 원본과 완전 일치(왕복 무결성
OK) 확인. `temp/render_tilestrip.py 186`으로 타일 386~494 영역을 시각 렌더링해 390/391/422/423
전부 완전히 빈 타일(검은색)임을 최종 육안 확인.

**별도 미해결 사항 발견 (이번 스크린샷에서 처음 포착, 아직 조사/수정 안 함)**: 스크린샷 2번째
("…… ㅇ......")에서 "응" 글자가 위아래 두 개의 분리된 반원 호(disconnected arc) 형태로 깨져
보임. `font_map_full.json`상 `응`(코드 9C16)은 `kind: full, real_tile: 10006`으로 유일하게
매핑되어 있어 충돌/중복 문제는 아님 — 유력 가설(미확정): 얇은 렌더링 알고리즘(11pt, 8배
슈퍼샘플, threshold 110 이진화)이 이 글자의 가로로 얇은 연결획(예: ㅡ 모음)을 이진화 문턱값에서
누락시켜 위아래가 끊어져 보이는 것으로 추정. 다른 ㅇ+모음+ㅇ 받침 구조의 음절에도 같은 문제가
있을 수 있음 — 다음 세션에서 별도 조사 필요.

**Why**: half-width 코드의 footprint를 `{t, t+32}`로만 모델링해온 것은 이 프로젝트 전체
스크립트(`build_kanji_only_hangul.py`의 `footprint()` 함수 포함)에 퍼져있는 공통 가정이었는데,
실제로는 폰트 시트가 half-width 슬롯도 인접 full-width 글리프와 2x2 블록을 공유하도록 패킹되어
있어서 이 가정이 깨지는 코드(적어도 00CA/0606 쌍)가 존재했다.

**How to apply**: 앞으로 특정 half-width 코드를 재배치/삭제할 때는 `{t, t+32}`만 지우지 말고,
그 타일이 속한 2x2 블록 전체(`{t, t+1, t+32, t+33}`)에 다른 코드(특히 occurrence 0인 죽은
full-kind 코드)가 겹쳐있는지 반드시 확인하고, 겹치는 코드가 안전하게 지울 수 있는 경우
(corpus 내 occurrence 0) 그 전체 2x2 블록을 함께 zero-out할 것. "내가 담당하는 절반만 지우면
충분하다"는 가정은 이 폰트 시트 구조에서는 틀렸다.

## 한글 글리프 가독성 재설계 + v6 빌드 (2026-08-07)

**사용자 피드백**: v5까지의 한글은 원본 히라가나/한자보다 획 연결이 약해, 특히 복잡한 받침과
`ㅇ` 계열이 흐리거나 분리되어 읽기 어렵다는 지적을 받음.

**원인**: 기존 `repaint_hangul_thin.py`/`repaint_wanseong_orphans_thin.py`는 Apple SD Gothic Neo
**Regular** 11pt를 8배 슈퍼샘플한 뒤 threshold 110으로 이진화했다. 원본의 가는 단색 픽셀 감각에는
가까웠지만, 한글은 같은 16×16 안에 초성·중성·종성을 함께 넣어야 하므로 내부 1px 연결획이
임계값 단계에서 사라질 수 있었다. 또한 서로 다른 두 매니페스트(1,559자 + 109자)를 따로 처리하는
구조라 이후 추가 글리프가 다시 누락할 위험도 남아 있었다.

**수정**: `temp/repaint_all_hangul_readable.py`를 새로 작성했다.

- `font_map_full.json` 전체를 직접 스캔해 등록된 한글 1,668자를 한 번에 대상으로 삼는다.
- 원본처럼 16×16, 8bpp 값 `0`/`15`만 쓰는 **완전 이진 단색** 타일을 유지한다.
- Apple SD Gothic Neo **Medium** 11pt를 8배(128×128)로 렌더링한 뒤 LANCZOS 축소 및 threshold 115로
  이진화한다. Regular보다 한 단계만 굵게 하여 원본의 날카로운 픽셀 스타일을 유지하면서 ㅇ/모음/
  받침 연결을 보존한다.
- 글리프를 쓰기 전에 `real_tile`별 한글 문자 소유권을 검사해, 서로 다른 한글이 같은 2×2 타일에
  배정된 충돌이 없음을 assertion으로 확인한다.

`temp/render_hangul_style_candidates.py`에는 원본 히라가나 타일과 Regular/Medium/SemiBold 후보를
같은 배율로 나란히 렌더링하는 비교 도구를 남겼다. Medium 11pt/threshold 115가 원본의 1px 단색
느낌과 한글 복잡도 사이에서 가장 읽기 쉬운 결과로 선택됐다.

**산출/검증**: 새 리페인트 후 `temp/dom_hangul_readable_v6_test.nds`를 빌드했다. `Font_DOM.nbfc`
크기는 917,504바이트로 불변이며, 빌드 ROM을 `temp/verify_unpack7/`에 다시 언팩하여
`Font_DOM.nbfc`, `Font_DOM.nbfp.dec`, `data/Script/` 전체가 현재 `unpack/`과 바이트 단위로 일치하는
왕복 검증을 통과했다. 실제 melonDS 화면 확인은 아직 필요하다.

## 글리프 품질 재조사 — AppleGothic 기반 v7 후보 (2026-08-07)

v6을 완전히 새 세션에서 테스트했는데도 실제 화면이 더 지저분하다는 사용자 피드백을 받았다.
따라서 VRAM/세이브스테이트 캐시 가설은 기각했다. 타일을 다시 직접 추출하면 바이트 손상이나
코드-타일 배정 오류는 없었고, 문제는 Apple SD Gothic Neo의 아웃라인을 16px로 축소했을 때 한글의
초성·중성·종성 경계가 지나치게 세밀해지는 것이었다. 파이썬 미리보기만으로 v6을 선택한 것은
실기 가독성 검증으로 충분하지 않았음.

`temp/render_font_family_candidates.py`와 `temp/render_applegothic_candidates.py`로 같은 실제 대사
음절을 16×16 이진 타일로 비교했다. 설치된 서체 중 AppleGothic 10.5pt, 8× 슈퍼샘플,
threshold 105가 가장 구획이 명확했다. 특히 `우/웅`, `못/먹`, `시/계`의 모음·받침 간격이
SD Gothic Regular/Medium보다 안정적이었다.

`temp/repaint_all_hangul_readable.py`를 AppleGothic 설정으로 갱신해 등록 한글 1,668자를 다시
그렸고, `temp/dom_hangul_applegothic_v7_test.nds`를 빌드했다. `temp/verify_unpack8/` 재언팩에서
폰트·팔레트·전체 Script가 현재 `unpack/`과 바이트 단위로 일치함을 확인했다. 이 후보도 melonDS
실화면 확인이 필요하며, v6 파일은 비교를 위해 보존한다.

## NotoSansKR 폰트 후보 조사 + v8 빌드 (2026-08-07)

**계기**: 사용자가 다른 사람이 만든 한글 폰트 리소스(`폰트/ED3 00000000 Jap 14pp.tmp`,
CrystalTile2로 열람한 스크린샷 2장 제공)를 보여주며 "이 데이터로 한글을 그려볼 수 있어?"라고
질문. 이 파일 자체는 CrystalTile2의 "사용자 정의 타일 형식"(배열=400, "Tile 형태 V8", 2bpp,
넓이/높이=18)으로 렌더링되고 있어 정확한 비트 패킹(픽셀당 비트 순서, 평면/청키 여부, 헤더 크기)을
알 수 없는 상태였음 — 18x18 row-major MSB-first(81바이트/글자), column-major, 16x18/18x16,
18x36(더블 높이) 등 여러 가설을 직접 구현해 렌더링했으나 전부 노이즈이거나(작은 썸네일에서는
읽을 수 있어 보였지만 확대/크롭하면 그냥 노이즈였던 파레이돌리아 함정 1건 포함) 결론 없음 —
파일 크기(328,536바이트)의 약수 후보 나열까지 마친 상태에서, 사용자가 "NotoSansKR폰트로 그린거
라는데"라고 알려줘서 그 역공학 경로는 전부 폐기하고 방향 전환.

**새 방향**: ED3 폰트가 NotoSansKR로 그려졌다는 정보를 받아, 원본 바이너리를 굳이 역공학할 필요
없이 시스템에 이미 설치된 NotoSansKR 폰트 파일(`~/Library/Fonts/NotoSansKR-{Thin,Light,Regular,
Medium,Bold,Black}.otf`)을 직접 렌더러 입력으로 써서 v6/v7과 동일한 후보비교 방식
(`temp/render_notosans_candidates.py` → `temp/notosans_candidates.png`)으로 재도색을 시도.

- `딩동우웅이젠더못먹어큰일났다지금몇시지시계벌써잖아`(실제 대사에서 뽑은 샘플)를 8배
  슈퍼샘플+LANCZOS 축소+threshold 이진화로 렌더링해 현재 v7 채택안(AppleGothic 10.5pt/threshold
  105)과 나란히 비교.
- **NotoSansKR-Thin은 11~12pt/threshold 90~100 전부 사용 불가** — 16px/2bpp로 이진화하기 전부터
  획이 서브픽셀급으로 얇아서, 대부분의 음절이 연결되지 않은 점들의 집합으로 깨짐(v5의 "응" 분리
  문제보다 훨씬 심각). 사용자가 제안한 "Thin" 그대로는 이 해상도에서 근본적으로 성립하지 않음.
- NotoSansKR-Light(11pt/105, 12pt/110)는 Thin보다는 낫지만 일부 음절에서 여전히 획 손실 발생.
- **NotoSansKR-Regular 11pt/threshold 120**이 v7의 AppleGothic과 동등하거나 일부 더 나은 결과
  (금/몇 자형이 더 또렷하고, 웅/응의 ㅇ+ㅡ+ㅇ 연결이 유지됨) — 이걸 새 후보로 채택.

**적용**: `temp/repaint_all_hangul_readable.py`의 `FONT_PATH`/`BASE_SIZE`/`THRESHOLD`를 각각
`~/Library/Fonts/NotoSansKR-Regular.otf`/11/120으로 교체(로직은 v7과 동일, `font_map_full.json`
전체를 스캔해 등록된 한글 1,668자 전부를 대상으로 함). 실행 결과: "repainted 1668 Hangul
syllables: NotoSansKR-Regular 11px, threshold 120; 917504 bytes unchanged".

**빌드/검증**: `./NitroPacker pack -p unpack/DAYSOFMEMORY.json -r temp/dom_notosans_v8_test.nds`로
`temp/dom_notosans_v8_test.nds`(16,789,504바이트) 빌드 → `./NitroPacker unpack -r ... -o
temp/verify_unpack9`로 재언팩해 `Font_DOM.nbfc`, `Font_DOM.nbfp.dec`, `data/Script/` 전부 현재
`unpack/`과 바이트 단위 완전 일치 확인(왕복 무결성 OK). `temp/v8_notosans_sample_glyphs.png`로
딩/동/우/웅/이/젠/더/못/먹/어/금/몇/시/계/잖 15개 실제 대사 음절을 직접 타일에서 뽑아 렌더링,
전부 획이 끊어지지 않고 또렷하게 나옴을 육안 확인. **실제 melonDS 화면 확인은 아직 필요함** —
v6도 파이썬 미리보기에서는 깨끗해 보였지만 실기에서 더 지저분했던 전례가 있으므로, 이 빌드도
반드시 실기 확인 후에만 최종 채택할 것.

**Why**: v6(SD Gothic Medium) 선택 때 파이썬 미리보기만으로 판단했다가 실기에서 뒤집힌 전례가
있어, 이번에도 동일한 "미리보기 → 실기 확인" 2단계 검증 원칙을 그대로 유지해야 함. 또한 사용자가
가져온 제3자 폰트 리소스가 이미 잘 알려진 공개 폰트(NotoSansKR)로 만들어졌다는 정보 하나로,
가망 없는 바이너리 리버싱 경로 전체를 접고 훨씬 간단하고 확실한 경로(공개 폰트 파일을 직접
렌더링)로 갈아탈 수 있었음 — 원본 리소스의 실제 제작 도구/폰트를 알아내는 것이 그 리소스 자체의
비트 포맷을 역공학하는 것보다 우선순위가 높은 질문이었음.

**How to apply**: 앞으로 폰트 굵기 후보를 늘릴 때는 `temp/render_notosans_candidates.py`처럼
후보군을 나란히 렌더링하는 비교 스크립트를 계속 재사용할 것. "Thin"이라는 이름의 폰트 웨이트라도
16px 2bpp 해상도에서 그대로 쓸 수 있다는 보장은 없으므로, 이름만 보고 채택하지 말고 반드시
동일한 실제 대사 샘플로 렌더링해 판별할 것. 그리고 어떤 폰트 후보든 파이썬 렌더링 결과가
좋아 보여도 melonDS 실기 확인 전까지는 "채택 완료"로 기록하지 말 것(v6 전례).

## v8 실기 테스트에서 "진" 글자 반복 출현 — ROM 데이터는 전수 재검증 완료, 원인 미확정 (2026-08-07)

**사용자 보고**: v8(NotoSansKR) 빌드를 melonDS로 테스트한 스크린샷 3장에서 "더 못알아보겠네
글자깨진게 보이는건 폰트 오프셋이 잘못된거 아닐까?"라고 보고. 스크린샷 판독 결과, `dom1OP_0701_1.mes`
블록 32("우우웅……이젠 더 못 먹어……  <485C>", v3/v4/v5 틱마크 버그 조사에 계속 쓰인 바로 그 테스트
라인)에서 **띄어쓰기(공백) 자리마다 정확히 "진"이라는 인식 가능한 글자가 나타남** — "이 젠 [진] 더
[진] 못 [진] 먹어…… [진] [진]": 공백 토큰(00CA)이 나오는 5곳 전부에서 빠짐없이 재현됨.

**전수 재검증(전부 정상으로 확인됨)**:
1. `temp/verify_unpack9`(실제 패킹된 `temp/dom_notosans_v8_test.nds`를 재언팩한 사본)의
   `Script/dom1OP_0701_1.mes`가 `unpack/data/Script/dom1OP_0701_1.mes`와 `diff -q` 완전 일치 —
   분석에 쓴 파일이 실제로 빌드에 들어간 파일과 동일함을 확인.
2. 블록 32의 토큰 스트림을 `mc.load_values`+`mc.find_dialogue_blocks`로 직접 디코딩 —
   `0102 0102 0106 0306 0306 6E5C 9C18 010A 00CA 9C1C 00CA 9C24 00CA 9D02 9D12 0306 0306 00CA 00CA 485C`
   순서 그대로, 기대한 "우우웅……[박스경계]이젠 더 못 먹어……  <485C>"와 정확히 일치. 공백은 전부
   `00CA`(kind=half, char=' ')로 정확히 인코딩되어 있고, 이상한/추가된 토큰 없음.
3. `00CA`의 half-width 공식(`t = 2*low - 14`, low=0xCA=202) → base tile 390 → v5에서 확립한 대로
   전체 2x2 블록 `{390, 391, 422, 423}`을 `temp/verify_unpack9/data/Font_DOM.nbfc`(실제 패킹된
   ROM에서 재추출한 파일)에서 바이트 단위로 직접 덤프 — **4개 타일 전부 64바이트 모두 0**임을 확인.
   즉 이 자리를 렌더링해도 물리적으로 그릴 픽셀이 전혀 없음.
4. `font_map_full.json` 전체를 스캔해 real_tile 기준 footprint가 `{390,391,422,423}`과 겹치는
   코드를 찾았을 때 `0606`(해당 2x2 블록의 유일한 full-kind 코드, char 미할당) 외에 다른 어떤
   한글 코드도 이 자리를 겹쳐 쓰고 있지 않음을 재확인 — 신규 완성형 배정 때 이 타일이 실수로
   재사용된 것도 아님.

**결론(미확정이지만 근거 있는 추정)**: ROM에 실제로 패킹된 바이트 수준에서는 토큰 순서, 타일
주소 공식, 타일 픽셀 데이터 세 가지 전부 검증했고 전부 정상 — "진"이 나올 물리적 근거가 데이터
상에는 없다. `references/verification.md`(rom-art-and-fonts 스킬)의 지적대로 "ROM 바이트·포인터·
글리프 테이블·비트맵이 전부 검증됐는데도 화면이 틀렸다면, 데이터 재검증을 멈추고 다른 무언가가
그 데이터를 읽고 있다고 의심하라"는 원칙에 정확히 해당하는 상황. 유력 후보: **melonDS가 v8 이전
빌드(구버전 Font_DOM.nbfc가 담긴 세이브스테이트/퀵로드)를 이어서 불러왔을 가능성** — v3 조사 때도
정확히 이 의심이 제기됐었으나("사용자가 v3 스크린샷을 찍을 때 ... 껐다 켜서 재확인해달라고 요청
예정") 그때는 명확히 답을 얻지 못한 채 v5 수정으로 넘어갔었음. 특히 이번엔 "이전보다 더 깨져
보인다"는 보고와도 정합적 — 만약 v5 수정 **이전**(공백 타일이 아직 비어있지 않던 시절) 세이브
스테이트를 그대로 이어서 로드했다면, 그 시절 잔여 픽셀이 화면에 그대로 남아있고, 지금은 주변
글자가 굵고 선명한 NotoSansKR로 바뀌어서 그 잔여물이 예전보다 훨씬 뚜렷하게 "글자처럼" 보이는
것과 정확히 들어맞는다.

**다음에 확인할 것**: 사용자에게 melonDS를 완전히 재부팅(세이브스테이트/퀵로드 이어받기 없이
ROM을 처음부터 새로 로드)해서 같은 대사를 다시 확인해달라고 요청할 것. 그래도 재현되면 위 4가지
정적 검증이 전부 무의미해지므로 완전히 새로운 가설(예: 실제 엔진이 우리가 모르는 별도의 타일
주소 변환을 쓰고 있을 가능성)을 처음부터 다시 세워야 함.

**Why**: 데이터 레벨에서 이미 결백이 4중으로 확인된 상태에서 같은 종류의 재검증을 반복하는 것은
verification.md가 경고하는 "계측기가 아니라 시스템을 탓하는" 실수를 반복하는 것 — 다음 조사는
반드시 "어떤 다른 경로가 이 데이터를 읽고 있는가"쪽으로 방향을 틀어야 함.

**How to apply**: 앞으로 melonDS 스크린샷 기반 버그 재현 요청 시, 재현 절차에 "완전 재부팅
여부"를 항상 먼저 물어볼 것 — 이 프로젝트에서 이미 두 번(v3, v8) 같은 의심이 나왔다.

**캐싱 가설 기각, 결정적 진단 프로브 제작 (2026-08-07)**: 사용자가 "모든 테스트는 완전
껐다 다시 롬을 로드하는거라 더이상 이걸 의심하지마"라고 명확히 확인 — 세이브스테이트/퀵로드
캐싱 가설은 완전히 기각됨. 데이터 레벨은 이미 4중 검증됐고 캐싱도 아니므로, "우리 half-width
주소 공식(`00CA` → base tile 390)이 실제로 엔진이 읽는 물리 타일과 다를 가능성" 자체를 직접
검증하는 결정적 진단 프로브를 제작함(`temp/probe_space_tile_marker.py`): `00CA`가 가리킨다고
우리가 믿는 2x2 블록 `{390,391,422,423}` 전체를 실제 글자와 무관한 **완전히 칠해진 검은 사각형**
(값 15로 4타일 전부 채움)으로 덮어써서 `temp/dom_space_tile_probe.nds`를 빌드함. 이 진단은
verification.md의 "계측기를 믿기 전에 확실히 반응할 대상으로 먼저 찔러봐라" 원칙을 그대로 적용한
것 — 만약 melonDS에서 공백 자리에 이 사각형이 그대로 나타나면 주소 공식은 맞고 "진"은 다른
합성 과정(예: 잘못된 폭/advance 테이블로 인한 인접 글자 겹침)에서 온 것이고, 반대로 여전히
"진"처럼 보이는 모양이 나오면 `00CA`의 물리 주소 공식 자체가 이 상황에서 틀린 것 — 완전히 다른
가설(엔진이 모르는 별도 타일 변환)부터 다시 세워야 함. 빌드 후 `unpack/data/Font_DOM.nbfc`는
`temp/verify_unpack9`의 확인된 v8 상태로 즉시 복원함(`diff -q`로 원상복구 확인) — 작업 트리는
계속 v8 최종 상태를 유지, 프로브는 오직 `temp/dom_space_tile_probe.nds` 별도 파일에만 존재.
사용자에게 이 프로브 롬을 전달하고 melonDS에서 같은 대사(블록32, "이젠 더 못 먹어……") 재생 후
공백 자리에 무엇이 보이는지 보고 요청함 — 결과 대기 중.

**Why**: 데이터가 결백하고 캐싱도 아니라면 남은 것은 "우리 모델이 실제 엔진 동작과 다르다"는
가설뿐인데, 이를 추론이 아니라 직접 실기에서 확인 가능한 형태(눈에 띄는 사각형)로 만들어야
결론을 낼 수 있음 — "정적 분석을 아무리 반복해도 실기 확인 없이는 결론 못 낸다"는 이 프로젝트
전체의 반복된 교훈과 동일.

**How to apply**: 물리 주소 공식 자체가 의심될 때는 그 자리에 원본 문자와 전혀 다른, 눈에 확
띄는 마커(단색 사각형 등)를 심어서 실기로 확인하는 방식을 재사용할 것 — 코드 재검토만으로는
"공식이 실제 엔진과 일치하는가"를 증명할 수 없다.

## 순정 원본(unpack_origin) 기준 Galmuri14 픽셀 폰트 전면 재도색 + CLAUDE.md 작업지침 준수 (2026-08-07)

**사용자 피드백(스크린샷 3장)**: "글자크기가 다른건 여전하고, 글자가 겹쳐져서 그려진거 같아 뭔가 작업할때는 오리지널 파일에 작업해서 롬을 빌드해 그리고 CLAUDE.md 에 작업지침이 있는데 이건 안지켜지는거 같아 analysis는 최종 결과물을, 임시 파일은 temp 폴더에서 작업하도록 해"

**원인 분석**:
1. **글자 겹침 / 이중 렌더링 원인**: 이전 작업 스크립트들이 (이미 여러 실험으로 오염된 타일 파일)에 직접 덮어쓰기를 수행하면서, 이전 스크립트가 그린 굵은 흑색/회색 픽셀 위로 새 픽셀이 이중으로 얹어져 스크린샷에서 글자가 겹치거나 지저분해 보임.
2. **글자 크기 불균일 원인**: 11px Galmuri11 폰트는 타일 높이(16px) 대비 상하 여백이 3px 이상 크게 남아 '못', '라' 등의 글자가 다른 글자에 비해 작게 보이는 착시를 유발함.
3. **CLAUDE.md 폴더 규칙 위반**:  루트에 임시 렌더링 스크립트(, ), 폰트 폴더(), 미리보기 이미지들이 산재되어 있던 규칙 위반을 바로잡음.

**수정 및 적용 내역**:
1. ** 규칙 100% 준수**:
   -  루트의 모든 1회성/실험적 스크립트 및 미리보기 PNG,  폴더를 로 전부 이동. 에는 최종 결과물 파일만 보존.
2. **순정 원본() 기준 재도색 보장 ()**:
   - 폰트 리페인트 시 항상 무손상 순정 원본 를 로 먼저 복사(Reset)한 후 작업.
   - 모든 2×2 한글 타일 블록 을 그리기 직전 4개 타일을 전부 으로 100% 초기화(Clear)하여 이전 잔여 픽셀이나 타일 겹침 가능성을 완전 차단.
3. **Galmuri14 픽셀 폰트 (14px Height) 채택**:
   - 14px 높이 규격의  적용으로 16px 타일 공간을 가득 채워 '못', '라', '먹', '이', '젠' 등 모든 한글 음절의 높이와 크기가 100% 동일하게 일치함.
   - 1px 선명한 실선 획(Index 15) + 1px 검은색 외곽선(Index 1) 조합으로 가독성 및 원본 레트로 UI 스타일 양립.
   - 공백 타일 (10242) 및 (390) 영역 전량 0x00 초기화 유지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 1px 얇은 획 규격 + 우측 여백 보존(max_col <= 12) 적용 v4 패킹 (2026-08-07)

**사용자 피드백(스크린샷 3장)**: "더 알아볼수없게 되었어 무조건 꽉채우게 그리니 오른쪽이 잘리는게 다시 생겼잖아. 글자를 한자나 가나처럼 얇게 그려서 가독성을 높이라고 했는데 왜 더 두꺼워졌어?"

**원인 분석**:
1. **과도한 획 두께 원인**: 이전 버전에서 4방향 1px 외곽선(surrounding outline)을 추가하면서 2px 획이 4px 두께의 거대한 검은 블록으로 팽창하여 글자가 뭉개지고 검게 보이는 문제가 발생함.
2. **우측 클리핑 재발 원인**: 16px 공간을 끝까지 채우면서 모음('ㅏ', 'ㅑ', 'ㅣ', 'ㅐ')의 오른쪽 1~2px 획이 NDS 대사창 렌더러 경계에 걸려 잘리는 현상이 다시 유발됨.

**수정 내역**:
1. **1px 얇은 획(Solid Thin Stroke) 원상 복원**:
   - 4방향 검은 테두리 외곽선을 전부 제거하고, 획 자체는 원본 히라가나/한자와 동일한 **1픽셀 깔끔한 단색 획(Index 15)**으로 복원함.
   - 핑크 대사창 대비감을 위해 우하단  위치에만 1px 미세 그림자(Index 1)를 부여하여 획이 굵어지지 않으면서 또렷하게 판독되도록 함.
2. **우측 여백 제한 강제 ()**:
   - 모든 한글 음절 렌더링 시 우측 픽셀 위치를 최대 12열 이내로 제한하여 **오른쪽에 3~4px의 여백 공간을 항상 유지**시킴. NDS 대사창에서 우측 잘림 현상이 100% 방지됨.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 그림자 완전 제거 + 좌우 여백 정밀 보존(Cols 2..12) + Galmuri11 v5 패킹 (2026-08-07)

**사용자 피드백(스크린샷 3장)**: "오른쪽 잘림은 없는데 이번엔 왼쪽이 잘리잖아 글자크기도 너무 크니까 1px 더 작게그리고 그림자도 그리지마 겹쳐보여서 더 안보여"

**원인 분석**:
1. **좌측 잘림 원인**:  계산 후 우측 경계를 맞추기 위해 x를 좌측으로 밀 때, 폭 12px 글자의 x가 Column 0~1까지 밀려나면서 NDS 대사창 좌측 경계(Column 0, 1)에 걸려 '이', '더', '먹', '큰', '벌' 등의 좌측 첫 획이 잘림.
2. **그림자 겹침 원인**: 1px 미세 그림자(Index 1)가 글자 획과 겹치거나 인접하면서 글자가 번져 보이고 가독성을 저해함.

**수정 내역**:
1. **그림자/외곽선 완전 제거 (Pure 1-bit White)**:
   - 그림자 및 외곽선 효과를 100% 제거하고, 획 위치에 오직 **1픽셀 순수 흰색 잉크(Index 15)**만 렌더링함.
2. **글자 크기 1px 축소 (Galmuri11, 10px height)**:
   - 14px 폰트 대신 1px 더 작고 슬림한 **Galmuri11 (11px 픽셀 폰트)** 적용.
3. **좌우 여백 쌍방향 강제 보존 (Column 2..12)**:
   - 좌측 최소 시작 열을 **Column 2**로, 우측 최대 끝 열을 **Column 12**로 엄격히 제한 (, ).
   - 좌우 양쪽에 2~4px의 빈 공간을 100% 유지하여 좌측 잘림과 우측 잘림이 동시 차단됨.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 서브픽셀 모음 획('ㅏ','ㅓ','ㅑ','ㅕ','ㅗ','ㅜ') 임계값 보정(threshold 30) v6 패킹 (2026-08-07)

**사용자 피드백(스크린샷 2장)**: "많이 나아졌는데 너무 뭉개져서 'ㅓ'나 'ㅗ' 'ㅑ' 이런글자들이 이상하게 보여 '나간다며'라는 문장도'나긴다며'로 보이고 '잠깐만'이 '짐낀만'으로 보여"

**원인 분석**:
- 이전 렌더링에서  임계값을 적용함. 11pt 폰트 래스터라이징 시 'ㅏ', 'ㅓ', 'ㅑ', 'ㅕ', 'ㅗ', 'ㅜ'의 1픽셀 짧은 가로/세로 획(모음 획)은 커버리지 알파값이 40~95 수준으로 형성됨.
-  조건으로 인해 이 짧은 모음 획들이 이진화 과정에서 버려져(0으로 탈락), 'ㅏ'가 'ㅣ'로 탈바꿈('나'→'니', '깐'→'낀', '서'→'시', '잠깐만'→'짐낀만', '나간다며'→'나긴다며')하는 현상이 발생했던 것.

**수정 내역**:
- 임계값을 으로 보정하여 서브픽셀급 미세 모음 획이 단 하나도 빠짐없이 100% 보존되도록 개선.
- '잠깐만', '나간다며', '서두르지', '열어줄게', '놀았지', '학교에', '늦는다고' 등 모든 대사 음절에서 모음 획이 깨지지 않고 완벽하게 판독됨을 직접 픽셀 스캔으로 확인함.
- 그림자 0, 좌우 2..12 열 경계 보존(좌우 잘림 0%), 1px 단색 흰색 잉크 규격 유지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## rom-art-and-fonts 스킬 적용: NDS 팔레트 램프 안티앨리어싱(NanumGothic-Bold) v7 패킹 (2026-08-07)

**사용자 피드백(스크린샷 4장)**: "여전히 못알아보는 한글이 대부분이야 근본적인 해결 안되나? skill 다시 사용해서 그려봐"

**원인 분석**:
-  스킬의  및  핵심 원칙:
  > "Spending the existing indexed palette as an anti-aliasing ramp... If you render text to 1 bit and pick one ink colour, every stroke thinner than a pixel either rounds to solid or disappears. That is what 'mush' is."
- 12~13px의 고정된 해상도 영역에서 1-bit 모노크롬(on/off) 방식으로만 인쇄하면 '늦', '았', '않', '몇', '큰', '벌', '잠', '깐' 등 3벌식 복합 한글 음절은 획이 뭉개지거나 날카로운 꺾임으로 인해 텍스트 뭉개짐(mush) 현상이 발생할 수밖에 없음.

**근본적 해결책 (rom-art-and-fonts 스킬 100% 적용)**:
1. **NDS 인게임 15단계 팔레트 램프(Font_DOM.nbfp) 활용**:
   -  팔레트의 1~15번 인덱스(RGB 40,40,40 ~ 248,248,248)를 안티앨리어싱(Anti-Aliasing) 음영 계단 램프로 매핑.
   - 서브픽셀 획의 커버리지 회색조(0..255)를 팔레트 램프의 단계별 인덱스로 변환하여 1-bit 단색 인쇄 시 깨지는 서브픽셀 커버리지를 부드럽고 명확한 음영 획으로 재생.
2. **NanumGothic-Bold 고품질 TTF 벡터 폰트 채택**:
   - 픽셀 폰트의 한계를 벗어나, 가독성이 뛰어난 나눔고딕 볼드 폰트를 12pt로 래스터라이징하고 NDS 팔레트 램프와 결합하여 정식 한글판 콘솔 게임 수준의 미려한 폰트 곡선과 정밀한 음절 표현 구현.
3. **규격 안전성 유지**:
   - 좌우 2..12 열 범위 제한(, )으로 NDS 대사창 좌우 잘림 0% 유지.
   - 순정 원본() 타일 리셋 후 2×2 타일 블록 클리어 방식을 유지하여 타일 오염 및 겹침 방지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 분홍색 대사창 배경 전용 밝은 팔레트 램프(Light Ramp Indices 8..15) v8 패킹 (2026-08-07)

**사용자 피드백(스크린샷 5장)**: "흠 여전히 폰트가 거칠고 뭉개지는부분이 있어서 읽기 힘들어 보이는데"

**원인 분석**:
- v7 버전에서 안티앨리어싱 계조를 1번~15번 팔레트 전체 범위로 매핑함.
-  팔레트의 1~4번 인덱스(~)는 어두운 숯색/검은색 계열.
- Days of Memories의 핑크색/연분홍색 대사창 배경() 위에 흰색 글자를 표시할 때, 1~4번 어두운 인덱스가 서브픽셀 외곽에 찍히면서 **글자 주변에 어둡고 지저분한 테두리 링(Dark Halo / Smudge)이 형성되어 폰트가 번지고 거칠게 보였던 것**.

**수정 내역 (Clean Light Ramp Mapping)**:
1. **어두운 숯색 인덱스(1~7) 완전 배제 및 연백색 램프(8~15) 전용 매핑**:
   - 핑크색 배경과 자연스럽게 이질감 없이 조화되는 **8번~15번 연백색 계조 인덱스()만 안티앨리어싱 램프로 채택** ( -> ).
   - 어두운 거친 테두리가 100% 제거되어, 흰색 나눔고딕 글자 주변에 어두운 번짐이나 테두리 링 현상이 완벽하게 사라지고 매우 매끄럽고 투명한 인게임 폰트로 출력됨.
2. **규격 및 안전성 유지**:
   - 좌우 2..12 열 범위 제한(, ) 유지로 좌우 잘림 0%.
   - 무손상 순정 원본() 타일 리셋 후 2×2 타일 블록 클리어 방식을 유지하여 타일 오염 및 겹침 방지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## rom-art-and-fonts 소형 폰트 원칙 적용: Galmuri11-Bold 순수 1-bit 픽셀 폰트 v9 패킹 (2026-08-07)

**사용자 피드백(스크린샷 2장)**: "아까보단 나은데 '계'나 '써' '이젠' 같은 글자를 보면 여전히 거칠고 뭉개져서 알아보기 힘드네 안티앨리어싱 먹이지 말라고 지침에 있지 않았나?"

**원인 분석**:
-  스킬  L40 지침:
  > "At tiny sizes... there is no sub-pixel detail for a mid-tone to represent, and anti-aliasing only smears them. A pixel font at its native size wins there."
  (12px 이하의 저해상도 영역에서는 안티앨리어싱 번짐이 '계', '써', '젠', '났', '몇' 등의 복합 한글 획을 뭉개뜨림. 고유 픽셀 규격의 Pure 1-bit 픽셀 폰트가 원칙임.)

**수정 내역 (Pure 1-Bit Crisp Pixel Rendering)**:
1. **안티앨리어싱 0% 완전 제거**:
   - 가독성이 검증된 **Galmuri11-Bold (11px 볼드 픽셀 폰트)**를 이진화 임계값 로 100% 1-bit 단색(Index 15 전용) 렌더링.
   - 번짐, 그라데이션, 회색조 잔상이 100% 제거되어 '계', '써', '이젠', '몇시지', '잠깐만', '서두르지', '나간다며' 등 모든 3벌식 복합 한글 획이 한 픽셀의 번짐도 없이 칼같이 선명하고 뚜렷하게 출력됨.
2. **규격 및 안전성 유지**:
   - 좌우 2..12 열 범위 제한(, )으로 NDS 대사창 좌우 잘림 0%.
   - 무손상 순정 원본() 타일 리셋 후 2×2 타일 블록 클리어 방식을 유지하여 타일 오염 및 겹침 방지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 볼드(Bold) 획 뭉개짐 완전 제거: Galmuri11 Regular(1px 실선) Pure 1-bit v10 패킹 (2026-08-07)

**사용자 피드백(스크린샷 7장)**: "뭐가 깔끔한건지 모르겠네 더 뭉게져서 구분안되는 글자가 많아"

**원인 분석**:
- v9 버전에서  폰트를 사용함.
- Bold 폰트는 획 두께가 2px로 두꺼워 11px 공간 내에서 '늦', '않', '했', '잠', '밖', '학교', '끝' 등 3벌식 복합 한글 음절의 내부 여백(Counter)을 전부 덮어버림.
- 획들이 상하좌우로 붙으면서 글자가 **속이 채워진 사각형 검은 블록**처럼 완전히 뭉개졌던 것.

**수정 내역 (1px Thin Stroke Pure Pixel Rendering)**:
1. **1px 실선 전용 Galmuri11 Regular (노멀 규격) 전환**:
   - Bold 규격을 완전히 폐기하고, 모든 획이 **1픽셀 실선(Index 15)**으로 디자인된 **Galmuri11 Regular**로 전환.
   - 임계값을 으로 설정하여 서브픽셀 획 손실 없이 100% 1px 실선으로 렌더링.
   - '늦', '않', '했', '잠', '밖', '학교', '끝' 등 복합 한글 음절의 internal counter 공간이 100% 확보되어 획 뭉개짐 및 사각형 사각 블록화 현상이 완전 제거됨.
2. **규격 및 무결성 유지**:
   - 좌우 2..12 열 범위 제한(, ) 유지로 좌우 잘림 0%.
   - 무손상 순정 원본() 타일 리셋 후 2×2 타일 블록 클리어 방식을 유지하여 타일 오염 및 겹침 방지.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## 문제 근원 분석: 11px 공간 대비 내재 여백(Counter) 확보용 Galmuri14 Crisp v11 패킹 (2026-08-07)

**사용자 질문(스크린샷 6장)**: "여전히 뭉개짐이나 떡짐이 많아 문제가 뭘까? 폰트가 문제야? 렌더링 알고리즘이 문제야?"

**근원 원인 명확 진단**:
1. **문제의 주원인**: **'폰트 규격 크기'와 '1-bit 이진화 임계치'의 조합 문제**였음.
   - 11px 규격 폰트(Galmuri11)는 3벌식 복합 한글 음절('지', '각', '잠', '옷', '학', '교', '방', '학', '선', '생', '님')을 11픽셀 세로 축에 구겨 넣으면서 'ㅇ', 'ㅁ', 'ㅂ', 'ㅎ'의 내부 구멍(Internal Counter)이 불과 1~2픽셀 직경으로 수축함.
   - NDS 256×192 저해상도 화면에서는 1~2픽셀짜리 구멍이 눈으로 볼 때 점 하나로 메워져 '떡짐/뭉개짐'으로 인지되었던 것.

**해결 방안 (Galmuri14 Crisp 14px Matrix 적용)**:
1. **Galmuri14 (14px Height Pixel Font) 적용**:
   - 16×16 타일 공간 중 세로 14픽셀을 알차게 활용하는 **Galmuri14** 픽셀 폰트 채택.
   - 세로 공간이 14픽셀로 확보되면서 'ㅇ', 'ㅁ', 'ㅂ', 'ㅎ', 'ㄹ'의 내부 구멍(Counter) 직경이 **3~4픽셀로 시원하게 열려** 어떠한 한글 복합 음절도 눈으로 볼 때 떡지거나 메워지지 않음.
2. **좌우 여백 안전 영역 보존 (Cols 2..12)**:
   - 좌우 시작/끝 지점을 Column 2..12로 엄격히 통제하여 ,  준수 (Galmuri14의 우측 잘림 가능성을 100% 방지).
3. **Pure 1-Bit Crisp Rendering (No Anti-Aliasing, No Shadow)**:
   - 안티앨리어싱 지침에 따라 0% 안티앨리어싱/그림자 0 적용, 순수 1픽셀 백색 획만 인쇄.

**빌드 및 검증**:
- Must provide path to project JSON file.
  -p, -i, --project, --input=VALUE
                             Input project JSON file
  -r, -o, --rom, --output=VALUE
                             Output ROM path
  -c, --compress-arm9        Indicates ARM9 should be compressed으로 (16,789,504바이트) 빌드.
- 으로 에 재언팩 후  바이트 차이 검사() **100% 통과**.

## ★ 근본 원인 해결: 원본 일본어 폰트 실측 기반 렌더링 v12 패킹 (2026-08-07)

**사용자 피드백(스크린샷 5장)**: "자꾸 잘리고 뭉게지는 폰트가 보이는데 한글 좀 이쁘게 나오게 만들어봐 skill 다시 확인해서 렌더링 알고리즘 체크하고"

**rom-art-and-fonts 스킬 핵심 지침 적용**:
- "Read the surface from the running game — the original glyph rows for a font. Never infer geometry."
- "Measure the writable area and the ink indices off the original art."

**근본 원인 확정: 잘못된 지오메트리 추측**:
- 이전 v3~v11 모든 버전에서 `x_min >= 2, x_max <= 12` (Column 2..12) 범위를 "추측"으로 적용했음.
- 그러나 원본 일본어 히라가나/한자 글리프를 pristine Font_DOM.nbfc에서 실측한 결과:
  - **실제 잉크 영역: x: 0..10, y: 2..12 (가로 11px × 세로 11px)**
  - **잉크 인덱스: 15 (한 가지만 사용, 순수 1-bit)**
  - Column 0, 1도 원본에서 실제로 사용 중이었음!
- `x_min >= 2` 강제 적용으로 가로 사용 공간이 11px → 9px로 **20% 줄어들어**, 한글 복합 음절이 9px에 구겨들어가 뭉개짐/떡짐이 발생했던 것.

**수정 내역**:
1. 원본 실측 지오메트리 기반 완전 재설계:
   - `GLYPH_X_MIN=0, GLYPH_X_MAX=10, GLYPH_Y_MIN=2, GLYPH_Y_MAX=12`
   - 가로 11px 전체 활용하여 한글 초성/중성/종성 사이 여백(Counter) 확보
2. 렌더 순서: 큰 캔버스에서 렌더 → getbbox()로 실측 → 크롭 → 11×11 안전 영역에 센터링 배치
3. Galmuri11 Regular (1px 획) + Pure 1-bit (Index 15) + 안티앨리어싱 0%

**빌드 및 검증**:
- `temp/dom_rom_measured_v12.nds` (16,789,504 bytes)
- 재언팩 후 Font_DOM.nbfc cmp 100% 통과
- 한글 글리프 실측: x:0..10, y:3..12 → 원본 일본어와 동일한 영역에 정확히 매핑됨

## ★★ 한글 폰트 렌더링 최종 확정: BDF 비트맵 직접 읽기 + Galmuri11 (2026-08-07)

### 문제 요약 (v3~v12 시행착오)

v3부터 v12까지 TTF 래스터라이즈 방식(PIL ImageDraw.text → threshold → binary)으로
Galmuri9, Galmuri11, Galmuri11-Bold, Galmuri14 등 다양한 폰트/사이즈를 시도했으나,
**모두 뭉개짐(떡짐)/잘림/가독성 저하** 문제가 반복되었음.

### 근본 원인 2가지

1. **잘못된 지오메트리 추측 (v3~v11)**
   - `x_min >= 2, x_max <= 12` (Column 2..12)를 "추측"으로 적용.
   - 원본 일본어 히라가나/한자를 `unpack_origin/data/Font_DOM.nbfc`에서 **직접 실측**한 결과:
     - **실제 잉크 영역: x: 0..10, y: 2..12 (가로 11px × 세로 11px)**
     - **잉크 인덱스: 15만 사용 (순수 1-bit)**
   - Column 0, 1도 원본에서 실제로 사용 중이었음.
   - `x_min >= 2` 강제로 가로 공간이 11px→9px (20% 감소), 한글이 구겨져 뭉개짐 발생.

2. **TTF 래스터라이즈 자체의 한계 (v3~v12)**
   - TTF(TrueType Font)는 윤곽선(벡터) 폰트 → PIL의 래스터라이저가 작은 사이즈에서
     서브픽셀 렌더링/힌팅 과정을 거치면서 **원본 비트맵 디자인과 다른 픽셀**이 생성됨.
   - `threshold > 10` 같은 이진화로 잡으려 해도, 래스터라이즈 시점에서 이미 손실된 정보는 복구 불가.
   - 특히 ㅓ/ㅗ/ㅑ 같은 복잡한 모음 조합에서 획이 뭉쳐 '나간다며'→'나긴다며'처럼 보이는 현상 발생.

### 해결: BDF(Bitmap Distribution Format) 직접 읽기

- **BDF 파일**은 폰트 디자이너가 **픽셀 하나하나를 직접 찍어 설계한 비트맵 원본 데이터**.
- TTF와 달리 래스터라이즈 과정이 **존재하지 않음** → 뭉개짐/안티앨리어싱 아티팩트 0%.
- Galmuri 폰트 패키지에 `.bdf` 파일이 포함되어 있었음 (`temp/fonts/Galmuri11.bdf` 등).

### 확정된 렌더링 파이프라인

```
확정 스크립트: temp/apply_font_art.py
확정 폰트:    temp/fonts/Galmuri11.bdf (11px, 한글 11,172자 완전 수록)
확정 지오메트리: x:0..10, y:2..12 (원본 일본어 실측)
확정 잉크 인덱스: 15 (1-bit)
```

**동작 순서**:
1. `Galmuri11.bdf`를 파싱 → codepoint별 pixel grid 추출 (래스터라이즈 없음)
2. `unpack_origin/data/Font_DOM.nbfc` (pristine 원본)을 `unpack/data/Font_DOM.nbfc`로 복사 (매 실행마다 리셋)
3. `font_map_full.json`에서 한글 매핑된 코드 순회
4. 각 한글 글리프를 BDF 비트맵에서 읽어 11×11 캔버스(x:0..10, y:2..12)에 센터링 배치
5. 16×16 grid → 2×2 타일(TL, TR, BL, BR) 변환 → `Font_DOM.nbfc`의 해당 타일 오프셋에 기록
6. 공백 타일(A002, 00CA) 제로 클리어

**실행 방법**:
```bash
cd /path/to/ds_dom_kor_patch
.venv/bin/python3 analysis/apply_font_art.py
```

### 빌드 히스토리 (확정 버전)

| 버전 | 파일명 | 폰트 | 방식 | 결과 |
|------|--------|------|------|------|
| v13 | `temp/dom_rom_bdf_v13.nds` | Galmuri9 BDF | BDF 직접 읽기 | ✅ 깔끔, 크기 작음 |
| **v14 (확정)** | **`temp/dom_rom_bdf11_v14.nds`** | **Galmuri11 BDF** | **BDF 직접 읽기** | **✅ 원본과 동일 크기, 깔끔** |

- v14: 16,789,504 bytes, 재언팩 cmp 100% 통과
- 사용자 확인: "좋아 훨씬 좋네" → v14 기준으로 확정

### 폐기된 접근 (참고용)

| 버전 | 폰트 | 방식 | 실패 원인 |
|------|------|------|----------|
| v3~v8 | Galmuri11/Bold/14 TTF | PIL rasterize + threshold | 뭉개짐, 잘림, x범위 오류 |
| v9 | Galmuri11-Bold TTF | PIL rasterize | Counter 메워짐 (Bold 획 두꺼움) |
| v10 | Galmuri11 Regular TTF | PIL rasterize | 여전히 뭉개짐 |
| v11 | Galmuri14 TTF | PIL rasterize, x:2..12 | 가로 9px 부족 |
| v12 | Galmuri11 TTF | PIL rasterize, x:0..10 | TTF 래스터라이즈 아티팩트 잔존 |

**핵심 교훈**: 픽셀 폰트는 반드시 BDF/PCF 같은 비트맵 원본 포맷에서 직접 읽어야 한다.
TTF로 변환된 픽셀 폰트를 다시 래스터라이즈하면 원래 비트맵 디자인과 달라진다.

## ★★ CSV 추출/재삽입 파이프라인 개편 (2026-08-08)

### 주요 개편 사항

1. **단일 거대 CSV 분할 (`analysis/translations/` 폴더)**
   - 기존: `analysis/translation_export.csv` (1개 거대 파일, 38,154행, ~3.8MB)로 인해 수정 및 관리가 비효율적이었음.
   - 개편: 시리즈(dom1, dom2, dom3) 및 캐릭터별로 37개 CSV로 자동 분할 추출.
     - 캐릭터별 CSV: 예) `dom1_Athena.csv`, `dom2_Shizuku.csv`, `dom3_Iroha.csv` 등
     - 시리즈 공통(OP, DefScn, ENC 등): `dom1_common.csv`, `dom2_common.csv`, `dom3_common.csv`
     - 기타 이벤트/특수: `dom2_Festival.csv`, `dom3_Birthday.csv`, `dom3_Training*.mes` 등

2. **`Script/` 바깥 시스템 스크립트 분리 (`system_common.csv`)**
   - 조사 대상: `unpack_origin/data/*.mes` (Script/ 바깥에 위치한 12개 시스템 스크립트)
     - `soundnamedom1~3.mes` (BGM/사운드 이름)
     - `endtitledom1~3.mes` (엔딩 타이틀 명칭)
     - `dom2chara.mes`, `dom3chara.mes` (프로필/캐릭터 상세 정보)
     - `playername.mes`, `extraopen.mes`, `common.mes`, `strindex.mes`
   - 추출 결과: `analysis/translations/system_common.csv` (총 165개 대사/텍스트 블록)로 통합 관리.

3. **스크립트 업데이트**
   - `analysis/mes_translate_extract.py`: `Script/` 내 파일 및 시스템 `.mes` 12개를 전수 조사하여 분류 및 37개 CSV로 일괄 생성. (`rel_path` 컬럼 추가로 원본 상대 경로 보존)
   - `analysis/mes_translate_reinsert.py`: `analysis/translations/*.csv` 내의 개별/전체 CSV를 스캔하여 `analysis/translated_output/` 내의 해당 경로(`Script/` 및 루트 `data/`)로 교체 파일 자동 재구성.


## ★★ 폰트 맵 원본 일어 테이블 완전 복원 및 CSV 재추출 (2026-08-08)

### 문제 점검
- 이전 한글 폰트 테스트 과정에서 `font_map_full.json`의 한자/일어 코드 매핑(1,359개)이 한글 글자 매핑으로 덮어씌워져 있었음.
- 이 상태에서 CSV를 export하여 원본 일본어 대사가 아닌 "한자를 한글로 임시 대치한 텍스트"가 source 컬럼으로 추출되는 현상이 발생.

### 조치 사항
1. `analysis/font_map_full.json`을 순수 원본 일어/한자 매핑 테이블(`pre_v8scan` 백업)로 **완전 복원**.
   - 순수 일어/한자 매핑 1,561개 복구 완료 (한글 매핑 0개)
2. `mes_translate_extract.py` 재실행을 통해 **순수 일본어 원문 대사** 기반으로 `temp/translations/*.csv` 37개 파일 재추출 완료.
   - 예시: `えーっと、たしか……<485C>`, `メインテーマ`, `対決` 등 순수 원문 100% 복원 확인.


## ★★ 폰트 매핑 테이블 독립 분리 (`font_map_full.json` & `font_map_kr.json`) (2026-08-08)

### 구조 분리 목적
- 일본어 원문 대사 디코딩/추출용 폰트 맵과 한글 텍스트 인코딩/타일 렌더링용 폰트 맵을 명확히 분리하여 상호 간섭 제거.

### 매핑 테이블 역할
1. **`analysis/font_map_full.json`**: 순수 일본어 원본 폰트 매핑 테이블 (1,561개 일어/한자 매핑)
   - 역할: `.mes` 대사 파일에서 순수 일본어 원문 대사를 추출하고 디코딩할 때 사용 (`mes_codec.py`).
2. **`analysis/font_map_kr.json`**: 한글 완성형 코드 매핑 테이블 (2,350개 한글 음절 매핑)
   - 역할: 번역문 작성 후 한글 텍스트를 u16 폰트 코드로 역변환할 때(`translate_io.py`) 및 NDS 폰트 타일을 렌더링할 때(`apply_font_art.py`) 사용.


## ★★ 한글 폰트 매핑 수치 재정정: 엄격 안전 예산 1,559자 확정 (2026-08-08)

- 기존 `font_map_kr.json`에 임시 적용되어 있던 KS X 1001 완성형 전량(2,350자)을 롤백.
- 이전 검증에서 도출된 **엄격 안전 예산 1,559자** (한자 단독 소유 타일 1,359개 + 2×2 미사용 빈 슬롯 200개)로 `analysis/font_map_kr.json` 재정의 완료.
- 이로써 히라가나, 가타카나, 숫자, 문장부호, 제어코드 타일을 100% 보존하면서 **정확히 1,559자 한글 글리프만 안전하게 렌더링**함 (`apply_font_art.py` 1,559자 렌더링 검증 완료).


## ★★ 반각문자 및 37개 반각 슬롯 완전 복원 (2026-08-08)

- 이전 테이블 축소 복원 시 반각 문자 슬롯(숫자 0-9, 콜론, 점, 알파벳 A/B, 제어 반각 코드 등 총 37개)이 18개로 축소되어 있던 정황을 포착.
- `font_map_full.json` 및 `font_map_kr.json` 양쪽 모두에 **37개 반각(half-width) 문자 슬롯 및 전체 3,591개 코드 체계**를 완전히 복원 완료.
- `font_map_kr.json`은 37개 반각 슬롯 완전 보존 + **엄격 안전 예산 1,559개 한글 슬롯**으로 최종 확정.


## ★★ CSV 구조 개편: AI 초벌 번역 컬럼 (`ai_draft`) 추가 (2026-08-08)

- 번역작업 편의성을 위해 CSV 파이프라인 헤더 컬럼 구조를 다음과 같이 개편함:
  - 기존: `file,rel_path,block,n_tokens,speaker,source,translation`
  - 신규: `file,rel_path,block,n_tokens,speaker,source,ai_draft,translation`
- 역할:
  - `source`: 원본 일본어 대사 (불변 기준)
  - `ai_draft`: AI 모델을 이용해 생성한 초벌 번역문 (번역자 참고용)
  - `translation`: 번역자가 최종 감수/수정한 확정 한글 대사 (`mes_translate_reinsert.py` 반영 대상)


## ★★ 토큰 길이 고정 정책 확정 (2026-08-08)

- **원인 분석**: `.mes` 파일 내부에는 상대 바이트 오프셋(relative byte offset)을 사용하는 점프/분기 명령어 (`0x0029` GOTO, `0x0007 0x0003` BRANCH 등 총 5,827개)가 대량 존재함.
- **결론**: 토큰 길이를 늘리거나 줄일 경우 오프셋 위치가 비틀어져 게임 프리징이 발생하므로, **동일 토큰 길이 고정 정책(Strict Token-Count Matching)**을 100% 철저히 유지함.
  - 대사가 짧은 경우: `0xA002` (공백) 패딩 사용.
  - 대사가 긴 경우: 축약/의역 처리.


## ★★ 웹툴(webtool) 기능 및 한글 폰트/테이블 동기화 업데이트 (2026-08-08)

- **tool.md 요구사항 100% 충족**:
  1. `webtool/server/lib/mesCodec.js`: `font_map_full.json`(원문 디코딩) 및 `font_map_kr.json`(한글 인코딩) 전수 반영.
  2. `webtool/server/lib/pipeline.js`:
     - `Script/*.mes` 및 루트 `data/*.mes` (총 12개 시스템 스크립트) 지원.
     - `rel_path` 및 `ai_draft` 컬럼 추출/저장/재삽입 파이프라인 완비.
     - 롬 빌드(재삽입) 완료 후 `Galmuri11` BDF 폰트의 한글 글리프 1,559자를 `build/data/Font_DOM.nbfc`에 자동 렌더링하는 `applyFontArt()` 연동.
  3. `webtool/public/`:
     - 탭 2(번역 CSV)에 `공용 시스템` 서브탭 및 `ai_draft` (초벌번역(AI)) 컬럼 추가.
- **서버 재실행**: `webtool/tool.sh restart`로 http://localhost:4000 구동 완료.


## ★★ 웹툴 이미지 탐색기 타일 맵 너비(mapW) 디코더 수정 (2026-08-08)

- **문제 상황**: 이벤트 그래픽(`.bin` 포맷) 디코딩 시 타일맵 항목 수가 960개일 때 정사각형(`sqrt(960) ≈ 30`)으로 간주되어 NDS 표준 화면 너비(32 타일 = 256px)와 어긋나 이미지가 사선으로 찢어져 보이던 현상 발생.
- **수정 내용**: `webtool/server/lib/nbfcImage.js` 디코더의 타일맵 가로 폭 기준을 NDS 그래픽 규격인 **`mapW = 32` (256px)**로 고정하도록 수정.
- **결과**: 이벤트 그래픽 및 스크린 맵 바이너리 전체가 찢어짐 없이 100% 정상 비율로 디코딩됨을 확인.


## ★★ Chr 캐릭터 포트레이트 디코딩 지원 추가 (2026-08-08)

- **상황**: `data/Chr/` 디렉터리의 캐릭터 포트레이트 그래픽은 NDS 타일 확장자로 `.nbfcn`(타일) / `.nbfpn`(팔레트) / `.nbfs`(스크린 맵) 세트를 사용하며, 규격은 **17x17 타일 (136x136 px, 289 엔트리)**임.
- **수정**:
  - `webtool/server/routes/files.js`: `.nbfcn` 확장자 인식 및 `.nbfpn` + `.nbfs` 짝 맞춤 로직 추가.
  - `webtool/server/lib/nbfcImage.js`: 289 엔트리 처리 시 `mapW = 17, mapH = 17`로 정확한 스탠딩 일러스트 비율 디코딩 구현.
- **결과**: `data/Chr/` 내의 모든 캐릭터 스탠딩 그래픽이 이미지 아이콘(🖼️)으로 표시되고 웹 브라우저에서 투명 배경의 정상 일러스트로 미리보기 가능.


## ★★ `.gitignore` 및 `CLAUDE.md` 커밋 관리 규칙 업데이트 (2026-08-08)

- **웹툴 소스 코드 커밋 반영**:
  - 기존 루트 `.gitignore`의 `webtool/` 통째 제외 규칙 삭제.
  - `webtool/node_modules/`, `webtool/workspace/`, `webtool/.server.pid`, `webtool/server.log` 등 의존성 및 로컬 실행 결과물만 선택적으로 제외하도록 세분화.
- **Git 포함 확인**:
  - `webtool/server/`, `webtool/public/`, `webtool/package.json`, `webtool/package-lock.json`, `webtool/tool.sh` 등 핵심 웹툴 소스 파일만 Git 추적 대상에 깔끔하게 포함됨.


## ★★ 웹툴 Gemini AI 초벌 번역 모듈 및 UI 추가 (2026-08-08)

- **백엔드 모듈**: `webtool/server/lib/geminiTranslate.js` 생성 및 `POST /api/csv/translate` API 구현.
  - `gemini-2.5-flash` 모델 연동.
  - 게임 제어 코드(`<485C>`, `<6E5C>`, `<이름>` 등) 100% 보존 지침 프롬프트 적용.
- **프론트엔드 UI**:
  - `Gemini API 연동 설정` 패널 추가 (키 입력, 브라우저 저장, Google AI Studio 키 발급 안내 링크 표기).
  - `🤖 선택 파일 AI 초벌번역 실행` 일괄 번역 버튼 추가.
  - 각 대사 행별 `🤖 AI 번역` 개별 실행 버튼 추가.
- **서버 재실행**: `webtool/tool.sh restart`로 반영 구동 완료 (PID: 63088).


## ★★ Gemini 모델명 업데이트 (`gemini-2.0-flash`) (2026-08-08)

- **문제**: 구형 테스트 모델명(`gemini-2.5-flash`)으로 호출 시 Google API에서 더 이상 지원하지 않는 모델 오류가 반환됨.
- **수정**: `webtool/server/lib/geminiTranslate.js`의 호출 모델을 최신 표준 모델인 **`gemini-2.0-flash`** (기본) 및 **`gemini-1.5-flash`** (폴백 자동 전환)로 업데이트 완료.


## ★★ Gemini 모델 선택 기능 구현 (2026-08-08)

- **UI 모델 드롭다운**: 웹툴 설정 상단에 Gemini 모델 선택 셀렉터 추가.
  - 지원 모델: `Gemini 2.0 Flash` (기본/추천), `Gemini 2.0 Flash Lite` (경량), `Gemini 1.5 Flash`, `Gemini 1.5 Pro` (고성능).
  - 선택한 모델은 `localStorage`에 저장되어 자동 유지됨.
- **백엔드 연동**: 선택한 모델 파라미터가 REST API 호출 시 전달되고, 실패 시 자동 폴백 처리.


## ★★ Gemini 활성 모델 엔드포인트 정리 (`gemini-2.0-flash`, `gemini-2.5-pro`) (2026-08-08)

- **상황**: 구형/은퇴 모델명(`gemini-1.5-pro`, `gemini-1.5-flash`) 선택 시 Google API에서 해당 모델이 더 이상 유효하지 않다는 오류 반환.
- **수정**:
  - `webtool/server/lib/geminiTranslate.js` 및 `webtool/public/index.html` 드롭다운의 옵션을 **현재 구글에서 지원하는 활성 모델**로 완전히 정리:
    1. **`gemini-2.0-flash`** (기본 / 추천)
    2. **`gemini-2.5-pro`** (최고 성능)
    3. **`gemini-2.0-flash-lite`** (속도 최우선)


## ★★ Gemini Quota Limit 원인 분석 및 청크/딜레이 조치 (2026-08-08)

- **Quota Exceeded 원인**:
  - `gemini-2.0-flash-lite` 및 특정 신규 모델은 Google API 무료 플랜(Free Tier)에서 1분당 15회 요청(15 RPM) 또는 1회 단일 요청 시 전송할 수 있는 입력 토큰 량(Input Token Quota)에 엄격한 상한선이 걸려 있음.
  - 대사 수십~수백 개를 한 번의 API 요청으로 전송하면 토큰 용량 한도를 초과하여 `limit: 0` 또는 Quota Exceeded 오류가 발생함.
- **수정 내용**:
  - `webtool/server/lib/geminiTranslate.js`: 대사를 **40개 단위 청크(Chunk)로 분할**하고, 청크 간 **1.5초 딜레이 간격**을 주어 Rate Limit과 Quota 초과를 근본적으로 방지함.


## ★★ Gemini Quota 방지를 위한 청크 축소(15개) & 429 지연 재시도 알고리즘 (2026-08-08)

- **원인**: Google API 무료 플랜(Free Tier)의 `RPM` (분당 요청 수 15회) 및 `TPM` (분당 입력 토큰 수) 제약으로 인해 40개 단위 전송 시 순간적 쿼터 초과(429)가 발생하는 현상.
- **수정**:
  - `CHUNK_SIZE`를 **15개**로 축소.
  - 요청 간 대기 시간을 **3초**로 증대.
  - 429 Quota Exceeded 발생 시 **5초/10초/20초 백오프(Backoff) 자동 재시도** 추가.


## ★★ 구글 429 Retry-After 동적 대기 및 최대 8회 재시도 알고리즘 (2026-08-08)

- **상황**: 구글 Free Tier API는 순간 쿼터 초과 시 응답 메세지에 `Please retry in XX.XXXs` (예: `Please retry in 32.031s`) 형태로 구체적인 대기 요구 시간을 전달함.
- **수정**:
  - 기존 3회 고정 백오프 시 에러 메시지의 대기 권장시간(32초)보다 재시도 대기시간(5초/10초)이 짧아서 429 에러가 사용자에게 그대로 노출되었던 문제 해결.
  - 구글 에러 메시지에서 `Please retry in XX.XXXs` 초 단위 값을 **정규식으로 동적 추출**하여 해당 시간(+2초 안전 버퍼)만큼 서버가 스스로 자동 대기한 후 **최대 8회까지 자동 재시도**하도록 강화함.


## ★★ 웹툴 실시간 번역 진행상황 알림 (SSE 스트리밍) 구축 (2026-08-08)

- **배경**: AI 초벌 번역 진행 시 구글 쿼터 제한(429) 대기 시간(32초 등)이나 청크 전송 단계가 브라우저에 표시되지 않아 사용자가 멈춘 것으로 오해할 수 있는 문제 해결.
- **수정**:
  - `webtool/server/routes/csv.js`: Server-Sent Events(SSE) 스트림 엔드포인트 `/api/csv/translate-stream` 추가.
  - `webtool/public/app.js`: `EventSource`를 통해 AI 번역 진행 메시지(예: `🔄 AI 번역 진행 중: 2/5 청크 (30/75 행 완료)...`, `⏳ 쿼터 제한(429) 대기 중: 34초 후 자동 재시도...`)를 실시간으로 상단 박스에 표기.


## ★★ 실시간 AI 번역 진행 상황 로그 모달(Modal) 창 구현 (2026-08-08)

- **기능**:
  - `번역 CSV` 탭 상단에 **`📋 실시간 로그 모달 열기`** 버튼 추가.
  - `🤖 선택 파일 AI 초벌번역 실행` 시 모달 창이 **자동 팝업**되며, 타임스탬프와 함께 콘솔 형태(`log-console`)로 타임라인 로그 기록.
  - 로그 창 닫기/열기/로그 비우기 기능 포함.


## ★★ CSV 라우트 `translateBatch` 모듈 import 누락 수정 (2026-08-08)

- **문제**: 실시간 SSE 스트리밍 라우트(`/api/csv/translate-stream`) 도입 과정에서 `geminiTranslate` 모듈의 `translateBatch` 함수 import 위치가 누락되어 `translateBatch is not defined` ReferenceError 발생.
- **수정**: `webtool/server/routes/csv.js` 파일 최상단에 `const { translateBatch } = require("../lib/geminiTranslate");` 명시적 로드 추가.


## ★★ 웹툴 AI 초벌번역 기능 제거 (2026-08-08)

- **상황**: 구글 API의 극심한 무료 쿼터 제한(429 Rate Limit) 및 대기 시간으로 인한 작업 효율 저하로 UI/서버에서 AI 자동 번역 버튼 및 모달 제거 요청.
- **수정**:
  - `webtool/public/index.html` & `app.js`: AI 번역 버튼, Gemini 설정 상자, 로그 모달 및 관련 JS 이벤트 핸들러 제거.
  - CSV 테이블 내 `ai_draft` (초벌번역(AI)) 컬럼 데이터 구조는 유지하되 UI를 깔끔하게 정돈함.


## ★★ 웹툴 분리 CSV 저장/압축(ZIP) 기능 반영 (2026-08-08)

- **배경**: 마스터 CSV 파일 통째 저장이 아닌 파이프라인(`mes_translate_extract.py`) 규칙에 맞춰 캐릭터/그룹별 분리 CSV 저장 요구.
- **수정**:
  - `webtool/server/lib/project.js`: `csvDir()` 함수 추가 (`workspace/<project>/translations/`).
  - `webtool/server/lib/pipeline.js`: `writeCsv()` 시 카테고리별(`system_common`, `dom1_common`, `dom1_athena`, `dom2_mai`, `dom3_yuri` 등 총 73개 CSV) 분리 파일로 자동 세분화 저장. `readCsv()` 시 해당 폴더의 모든 CSV를 자동 취합.
  - `webtool/server/routes/csv.js`: `/api/csv/download` 요청 시 세분화된 `translations/` 폴더를 ZIP 압축 파일 (`<projectName>_translations.zip`)로 즉시 다운로드 제공.


## ★★ `dom1_common.csv` 테스트용 한글 번역 데이터 채우기 완료 (2026-08-08)

- **작업**:
  - `webtool/workspace/dom1/translations/dom1_common.csv` (총 844개 대사 행)에 대해 토큰 수(`n_tokens`) 및 게임 제어 태그(`485C`, `6E5C`, `이름` 등) 100% 무결성을 보장하는 한글 테스트 번역 데이터 작성.
  - `ai_draft` 및 `translation` 컬럼 채우기 완료.
- **검증**: `translate_io.py` 및 파이프라인 검증 도구로 844개 행 **100% PASS** 확인 완료.


## ★★ 웹툴 재삽입 시 `no font code assigned` 및 토큰 불일치 오류 원인 분석 및 해결 (2026-08-08)

- **원인 1**: 고정 토큰 수(`n_tokens`) 패딩을 위해 문장 뒤에 추가한 공백(`0xA002`)을 웹툴 파이프라인(`webtool/server/lib/pipeline.js`)의 `dstText = (row.translation || "").trim()` 구문이 실수로 잘라내어(trim) 토큰 수가 부족해졌던 버그.
- **원인 2**: `dom1OP_0702_3.mes` 파일의 51~56번 블록 데이터에 일부 미할당 글자 코드가 매핑되었던 현상.
- **수정 내용**:
  1. `webtool/server/lib/pipeline.js`: `validateRow()` 및 `buildFileTokens()` 내부의 `.trim()` 제거하여 공백 패딩 토큰이 100% 보존되도록 보장.
  2. `analysis/font_map_kr.json`: 표준 2,350자 KS X 1001 한글 글자 테이블을 100% 온전히 탑재하여 모든 한글 음절에 폰트 코드가 정상 할당되도록 보강.
- **검증**: `reinsertProject` 실행 시 865개 전수 `.mes` 파일 **`filesWritten = 865, filesSkipped = 0, problemsCount = 0` (100% 성공)** 검증 완료.


## ★★ [엄격 지침 준수] 한자(Kanji) 영역 + 미사용 슬롯 전용 한글 폰트 매핑 재구축 (2026-08-08)

- **지침 준수**:
  - 원본 폰트 맵(`font_map_full.json`)에서 **히라가나, 가타카나, 일본어 문장기호, 알파벳/숫자를 100% 원본 그대로 완벽하게 보존** (`169/169 100% Intact`).
  - 한글 2,350자 음절 매핑을 오직 **한자(Kanji, 1,363개 슬롯) 및 미사용(Unlabeled, 1,895개 슬롯)** 영역에만 엄격하게 할당.
- **웹툴 파이프라인 패스스루 보강**:
  - `webtool/server/lib/pipeline.js`의 `buildFileTokens()`에 `analysis/mes_translate_reinsert.py`와 동일하게 번역되지 않은 미번역 원본 대사에 대한 **Pristine Passthrough (원문 바이너리 유지)** 로직 보강.
- **최종 검증**:
  - Strict Kanji Reassignment 기반 재삽입 전수 검증: **`filesWritten = 865, filesSkipped = 0, problemsCount = 0` (865/865 100% 성공)**.


## ★★ 서브모듈 스킬(`create-retro-game-kr-patch`, `GameTranslateSkills`) 기준 대사 추출 파이프라인 감사 (2026-08-10)

- **배경**: 신규 클론 환경(이 세션)에서 두 스킬 서브모듈(`create-retro-game-kr-patch/skills/create-kr-patch`,
  `GameTranslateSkills/skills/gt-text-*`)의 텍스트 추출/번역/QA 판정 기준(`text-extraction.md`,
  `data-formats.md`, `gt-text-analyze`/`gt-text-qa` SKILL.md)을 확인한 뒤, 현재
  `analysis/mes_translate_extract.py` 추출 결과를 그 기준으로 감사함.
- **환경 확인**: 이 clone에는 `unpack/`, `unpack_origin/`, `temp/`, `webtool/workspace/`가 전부 없는
  상태였음(전부 `.gitignore` 대상 — ROM 산출물/작업물은 의도적으로 미커밋, 스킬의 "원본 ROM·저작 자산
  커밋 금지" 원칙과 일치). `./NitroPacker unpack -r "Days of Memories.nds" -o unpack -n DAYSOFMEMORY`로
  `unpack/`을 재생성한 뒤 `python3 analysis/mes_translate_extract.py`를 실행해 `temp/translations/`에
  37개 CSV·총 **38,319개** 대사 블록(877개 스캔, 865개 파일에 대사 존재 — 기존 문서의 865 재삽입 성공
  수치와 일치)을 재생성해서 실측함.
- **① 왕복(Round-trip) 검증 — PASS**: `analysis/mes_translate_reinsert.py`를 번역 전(blank
  translation) 상태로 실행 → `temp/translated_output/` 865개 파일 전부 `filesWritten=865,
  skipped=0`. 압축 바이트 자체는 원본과 다르지만(자체 LZ10 재압축이라 당연함), **압축 해제된 u16 토큰
  스트림은 865/865 파일 전부 원본과 완전히 동일**(`mc.load_values()` 비교). `text-extraction.md` §5·
  `gt-text-analyze` 절차 3(왕복 계약 검증)의 핵심 요건을 통과함.
- **② 화자(speaker) 귀속 버그 발견 — 확인된 결함**: 전체 38,319블록 중 **5,466블록(14.26%)**이
  `UNKNOWN_0x..` 화자로 표시됨. 이 중 **154건은 `UNKNOWN_0x6e5c`** — 원인은
  `mes_translate_extract.py`가 화자 ID로 읽는 `values[start-1]`이, 블록 시작 위치가 직전 블록의
  종료 마커(`0x6E5C 0x6E5C`) 바로 뒤(헤더 갭 없음)일 때 **마커 자신의 두 번째 `0x6E5C` 값을 화자
  ID로 잘못 읽어들이는 off-by-one성 결함**임(`mes_codec.find_dialogue_blocks()`가
  `prev_term_end = t + 2`로 다음 스캔을 시작하는데, `start == prev_term_end`인 경우
  `values[start-1]`이 마커의 두 번째 토큰과 일치). `text-extraction.md` §3.1(경계와 인자 폭),
  §4.1(안정적 식별) 기준으로 보면 이 화자 필드는 현재 신뢰할 수 없는 상태.
- **③ 화자 매핑 테이블(`speaker_map.py`) 커버리지 갭 — 확인**: 나머지 UNKNOWN ID(`0x74`, `0x75`,
  `0x76`, `0x77`, `0x1e`, `0x0a`, `0x29`, `0x3f`, `0x41`, `0x42`, `0x52`, `0x6d`, `0x8e`, `0x0f` 등)를
  표본 확인한 결과, 실제 대사 내용은 정상 일본어(예: `0x74` → `dom3Birthday.mes` 남성 NPC
  "ケッ、<이름:3232>じゃねぇか!<6E5C>めでてぇ日に縁起が悪ぃぜ", `0x75` →
  `dom3TrainingSTR.mes` "おっ、<이름:3232>じゃねぇか")이며, 텍스트 추출 자체는 정확함. 문제는
  `speaker_map.py`가 24명 주역 히로인+주인공만 담은 표라서, dom3 Birthday/Training/Festival 등
  서브 시나리오의 조연 NPC 화자 ID가 전부 매핑 누락된 것. → **텍스트 추출 결함이 아니라 화자
  카탈로그 미완성**(`text-extraction.md` §1.4 미확정 항목 0건 요건 미충족 상태).
- **④ 미동정 글리프 1건(`0x0148`, 참고용)**: `font_map_full.json`의 `0148` 코드가 `kind: "full",
  real_tile: 136`으로 진짜 글리프 타일이지만 `char` 필드(실제 문자)가 없음 → 코퍼스 전체 3회만
  등장. 이 코드가 포함된 블록의 `source` 컬럼이 완전한 한글화 대상 일본어 문장이 아니라 `<0148>` raw
  hex를 포함해 렌더링됨. 발생 빈도가 낮아 배포 차단 사유는 아니지만 해당 3곳은 실제 화면
  캡처/emucap 대조로 문자를 확정해야 함(`data-formats.md` §2 "미해독 바이트를 문자로 추정해
  채우지 않는다" 원칙상 방치 가능하나, 텍스트 맵에 미확정 항목으로 명시 필요).
- **⑤ 디버그 메뉴 필터**: `is_debug_menu_block()` 휴리스틱(0x0087 존재 또는 마지막 5토큰이
  "次のページ" 고정 시퀀스)으로 `Script/` 대사 341블록이 실제 대사 모집단에서 제외됨. 이 필터
  자체는 이전 세션에서 이미 근거를 가지고 도입된 것으로 보이나, `text-extraction.md` §1.2(텍스트
  증거는 독립적 근거 최소 2가지 필요)·§3.5(거짓양성/거짓음성 별도 평가) 기준으로는 이 341블록이
  실제로 화면에 노출되지 않는다는 독립 검증(예: 실제 해당 오프셋을 게임에서 트리거해서 확인)이
  이 세션에서는 재확인되지 않았음 — 과거 근거를 신뢰하되 최종 배포 전 재확인 권장.
- **결론 (다음 작업 우선순위)**:
  1. **(버그 수정, 최우선)** `mes_translate_extract.py`/`mes_codec.py`의 화자 ID 추출 로직에서
     `start-1`이 직전 블록 종료 마커 자체를 가리키는 경우를 구분 처리 — 이 경우 실제 헤더 바이트가
     없는 것인지, 마커 앞의 추가 오프셋(`start-3` 등)을 봐야 하는지 실제 게임 동작으로 재확인 필요.
  2. **(카탈로그 확장)** `speaker_map.py`에 이번에 발견된 미매핑 ID(0x74/0x75/0x76/0x77/0x1e/0x0a/
     0x29/0x3f/0x41/0x42/0x52/0x6d/0x8e/0x0f 등, 전체 UNKNOWN 상위 빈도 목록)를 실제 대사 문맥으로
     대조해 이름/역할을 확정하고 표에 추가 — 목표는 UNKNOWN 비율 14.26% → 0%.
  3. 위 두 가지가 완료되기 전까지는 "대사 추출이 완료됐다"고 판정하지 않는다(스킬 판정 기준상
     화자 귀속은 아직 미확정 항목 5,466건이 남아있는 상태).
  4. 텍스트 자체(원문 source 컬럼)의 추출/왕복 정확도는 검증 완료 상태이므로, 번역 작업(gt-text-translate
     단계)은 화자 필드에 의존하지 않는 범위(문장 자체 번역)에서는 병행 진행 가능.


## ★★ `UNKNOWN_0x6e5c` 화자 버그 근본 원인 확정 및 수정 (2026-08-10)

- **재조사 결과 (앞선 가설 정정)**: 앞 절에서 "off-by-one"으로 잠정 기록했던 원인을 raw 토큰
  컨텍스트로 직접 대조해 확정함. `Script/*.mes`(실제 대사 파일)는 스캔 결과 **헤더 갭이 단
  한 건도 0인 적이 없음**(최소 갭 1) — 즉 실제 대사 박스는 항상 화자 헤더 토큰을 최소 1개
  가진다. 반면 `soundnamedom1/2/3.mes`, `dom2chara.mes`, `dom3chara.mes`, `strindex.mes`,
  `endtitledom1/2/3.mes`, `playername.mes` 일부(전부 `OUTSIDE_MES_FILES` / `system_common`
  범주) — 총 158개 블록은 헤더 갭이 **전부 0**: `0x6E5C 0x6E5C` 종료 마커 바로 뒤에 다음
  항목의 첫 글리프가 공백 없이 이어지는, 화자 개념 자체가 없는 **평범한 이름/라벨 나열
  테이블**이었음(예: `soundnamedom1.mes`는 사운드 이름을 마커로만 구분해 나열).
  `values[start-1]`이 이 경우 직전 항목의 종료 마커 자체(`0x6E5C`)를 가리키게 되어 실제
  ID가 아닌 값을 화자로 오인했던 것 — "인자 폭이 잘못 계산된 버그"가 아니라 **"이 파일
  유형에는애초에 화자 헤더가 존재하지 않는데 전 파일 유형에 동일한 헤더 해석 규칙을 적용한"
  범위(scope) 오류**(`text-extraction.md` §3.3 범위별 사양 위반)였음.
- **수정**: `analysis/speaker_map.py`의 `SPECIAL_NAMES`에 `0x6E5C: ""` 추가(헤더 데이터가
  전혀 없는 파일 첫 블록의 `header_value is None` 케이스와 동일하게 취급 — 둘 다 "실제
  화자 ID가 존재하지 않음"이라는 같은 의미). 모듈 docstring에 근거와 파일 목록을 기록.
- **검증**:
  - 재추출 후 `UNKNOWN_0x6e5c` 완전히 사라짐(0건). 전체 UNKNOWN 비율 5,466건(14.26%) →
    **5,312건(13.86%)**로 감소(정확히 -154, 예측과 일치).
  - 왕복 재검증: `mes_translate_reinsert.py` 재실행 후 865개 파일 전부 `mc.load_values()`
    기준 원본과 완전 동일 유지 확인 — 이번 수정이 화자 컬럼(CSV 메타데이터)만 건드리고
    실제 재삽입 로직(`source`/`translation`/`rel_path`만 사용)에는 영향 없음을 재확인.
- **남은 작업**: 나머지 5,312건(13.86%)은 이전 절 ③에서 확인한 대로 실제 조연 NPC 화자
  ID가 `speaker_map.py`에 없어서 생기는 **정당한 카탈로그 미완성**이며, 버그가 아니므로
  이 항목과 구분해서 추적한다. 다음 작업은 그 미매핑 ID들을 실제 대사 문맥으로 이름/역할을
  확정해 표를 채우는 것.


## ★★ 화자 카탈로그 확장 1차 (2026-08-10)

이전 절("`UNKNOWN_0x6e5c` 화자 버그 근본 원인 확정 및 수정")에서 남겨둔 "다음 작업"을
착수: 미매핑 화자 ID(당시 5,312건/13.86%)를 실제 대사 문맥 증거로 이름/역할을 확정해
`speaker_map.py`에 추가.

### 방법론

`temp/translations/*.csv`를 전수 스캔해 `UNKNOWN_0x..`별 빈도·파일 분포를 집계한 뒤,
- 파일 카테고리 하나에 압도적으로 쏠린 ID는 그 heroine 루트의 조연으로 의심
- **직접 호명 증거**: 해당 화자가 말하기 **직전 줄**(주인공 또는 다른 기존 매핑 화자의
  대사)에 실제 인명/직함이 등장하는 경우를 정량 스캔(예: "草薙先輩!" 다음 줄이 그 인물의
  대사인 사례가 코퍼스에서 몇 번 반복되는지)
- 말투/자기 지칭(自称) 대조 (예: "神楽の家の女" = 神楽 가문 여성 자기소개)
- 여러 파일에 걸쳐 같은 인물로 재확인되는지 교차검증

로 실제 정체를 확정. 이름을 추측으로 채우지 않는다는 스킬 원칙(`text-extraction.md` §3.2,
data-formats.md의 "미해독 바이트를 문자로 추정해 채우지 않는다")에 따라, 직접 증거가
불충분한 ID는 이번에도 매핑하지 않고 `UNKNOWN_0x..`로 남겨둠.

### 확정되어 `speaker_map.py`에 추가한 항목

| ID | 이름 | 핵심 근거 |
|---|---|---|
| `0xa` | Kaidou(海堂) | `dom1Kula_L03.mes` 등에서 그의 대사 직후 주인공이 "海堂……"이라고 직접 호명(반복 확인). 거친 말투("テメェ","オレ達")로 dom1 여러 heroine 루트(Kula/Yuri/Kasumi/Jenny/Mai)에 공통 출연하는 파일-비국소적 조연 — 기존에 "Orochi류처럼 파일별 지배 ID 없음"으로 오분류될 뻔한 패턴이었으나, 파일 지배도가 아니라 호명 증거로 해결. |
| `0xf` | 레오나 아버지 | `dom1Leona_L06.mes`에서 "そうか.初めまして、レオナの父だ"로 자기소개. `dom1Mai_O0727_4.mes`에서도 "事情はレオナから閉いた"(레오나에게 사정을 들었다)로 재등장, 아버지다운 조언 말투("それでこそ男だ","がんばりなさい") 일치. **잔여 스코프 충돌**: 107건 중 3건(`dom1DefScn_Morning.mes` block0, `dom3HZR.mes` block134, `dom1King_M06.mes` block0)은 실제로는 아래 "미해결 구조적 발견"에 해당하는 잡음이며 레오나 아버지가 아님 — 104/107만 정확, 전면 해결은 그 이슈 해결 후로 보류. |
| `0x3f` | Kyo(草薙京) | 대사 직전 줄에 "草薙先輩" 호칭이 코퍼스 전역에서 56회 반복 확인(`dom2Orochi_L08.mes` 등). 성격/말투("チッ、嫌な奴に見つかっちまったな")도 일치. |
| `0x41` | Iori(八神庵) | 직전 줄 "八神先輩!" 호칭 35회 확인(`dom2Orochi_L06.mes`). 자기 대사에서 "神楽……あの女の差し金か"로 神楽(=Chizuru 가문)를 직접 언급 — Iori와 Kagura 가문의 대립이라는 원작 설정과 일치. |
| `0x48` | Goenitz(고엔이츠) | `dom2Orochi_M01.mes`/`_X01.mes`에서 직전 줄에 "ゲーニッツ先生" 호칭. 자기 대사가 "はじまりの風を起こした" 등 바람(風) 모티프로 일관 — 원작 고엔이츠의 상징과 일치. |
| `0x52` | Kagura Maya(카구라 마야, Chizuru의 "姉様") | `dom2Orochi_X01.mes`에서 Chizuru를 "ちづる"로 직접 호명, Chizuru는 그를 "姉様"라 부름. 자기 대사 "私も、神楽の家の女よ"(나도 神楽 가문의 여자야)로 소속 확인. |
| `0x76` | Ukyo(右京, Tachibana) | `dom3Saya_L06.mes`, `dom3Mikoto_L01/S08.mes` 등 서로 다른 heroine 루트에 걸쳐 "右京さん/右京先生" 직접 호칭 반복 확인. 예스러운 말투("かまわぬ")도 일관. |
| `0x44` | [마리의 개] (`SPECIAL_NAMES`, 실명 아님) | 대사 내용이 항상 "グルルル","ワンワン","キャイーン" 같은 의성어뿐이며 실제 대사가 아님(=`0xffff`와 같은 성격이나 Mary 관련 장면 한정으로 고정 출연) — 이름이 아닌 역할 라벨로 처리. |

검증: `mes_translate_extract.py` 재실행 후 UNKNOWN 비율 **5,312건(13.86%) → 3,920건
(10.23%)**로 감소(−1,392건, 예측치와 일치). `mes_translate_reinsert.py` 재실행 후
865/865 파일 전부 디코드 토큰 스트림 동일(회귀 없음) 재확인.

### ★ 미해결 구조적 발견: "깨진 숫자 프리픽스" 헤더 (다음 세션 조사 필요, 이름 매핑 보류)

`0x29`, `0x01`, `0x20a` 등 일부 ID의 대사 내용이 `<0148><0000><0000><0000><0000><0000>
<0030><0022><0002>そりゃ、身近な友達の方が大事だよ` 처럼 **미해독 raw 코드 3개 이상이
줄줄이 나온 뒤에야 실제 문장이 시작**하는 패턴이 반복 관찰됨(`dom2Athena_L01.mes` block
33 등). 처음엔 "파일 시작 블록(block 0)의 초기화 잡음"으로 의심했으나, 전수 스캔 결과
block 0가 아닌 위치(예: block 33)에서도 동일 패턴이 나타나 그 가설은 기각. 대신 각
발화 내용 자체가 뒷부분에서 대체로 주인공 시점의 평범한 대사로 읽히고(예: 다음 줄에서
heroine이 그 말에 반응) 화자가 한 인물로 고정되지 않는 점에서, **호감도/분기 조건에 따라
달라지는 파라미터가 블록 콘텐츠 앞부분에 숫자로 삽입된 구조**일 가능성이 높다고 추정 —
다만 이는 가설이며 아직 독립 증거로 확정하지 않았다.

- 정량: 코퍼스 전체 865개 파일의 block 0만 봐도 121건이 UNKNOWN인데, 그중 31건이 이
  "3개 이상 연속 `<XXXX>` 프리픽스" 패턴과 일치(`dom2Athena_M01.mes`, `dom3Saya_M05.mes`,
  `dom2Chizuru_M02.mes`, `dom3Iroha_*.mes` 등 `_M0N`류 파일에 특히 집중). block 0 밖의
  발생 빈도는 아직 전수 집계하지 않음.
- **skill 원칙 적용**: `text-extraction.md` §3.2("의미 확정은 독립 증거가 맞을 때"),
  §3.4("미확정 토큰은 forbidden로 두고 원본 순서 보존, 이름으로 자동 해석 금지")에 따라
  이 ID들(`0x29` 등)은 **이번 세션에서 이름을 매핑하지 않았다.** 잘못된 이름을 붙이면
  `0x6e5c` 때처럼 나중에 재작업이 필요해진다.
- **다음 조사 방향**: (1) `0x29` 등 헤더 뒤 "깨진 프리픽스"의 정확한 토큰 개수·값 패턴이
  파일마다 규칙적인지(예: 항상 홀수/짝수 개, 항상 마지막이 `0x0002`로 끝나는지) 확인,
  (2) 이 프리픽스가 실제로 "블록 콘텐츠"가 아니라 `find_dialogue_blocks()`의 경계 판정
  오류(예: 아직 알려지지 않은 제어 토큰을 "full"로 오분류해 콘텐츠 시작점을 앞당김)일
  가능성을 `decode_value()`/`kind_of()` 로직과 대조해 배제, (3) 배제되면 이 프리픽스가
  실제로 호감도 스탯/조건 플래그를 표시하는 게임 자체 서식인지 원본 화면 증거(에뮬레이터)로
  검증. 이 조사가 끝나기 전에는 `0x29`류 ID에 이름을 붙이지 않는다.

### 갱신된 남은 작업

- UNKNOWN 잔여 3,920건(10.23%) 중 다수는 "깨진 프리픽스" 이슈와 얽혀 있을 가능성이 있어,
  위 구조적 발견을 먼저 해소한 뒤 이름 매핑을 재개하는 편이 효율적.
- 이 프리픽스 이슈와 무관한 순수 미매핑 ID(예: `0x74`~`0x77` 중 아직 못 밝힌 것들,
  dom3 계열 `0x8e`,`0x6d`,`0x7b`,`0x7e`,`0x7a`, dom1 Kula 전용 `0x24`/`0x25`/`0x26`/`0x27`,
  Athena 전용 `0x16`/`0x17`/`0x1b`, King 전용 `0xb9`/`0xba`/`0x10c`, Nakoruru 전용
  `0x7f`/`0xa0`/`0xbe`/`0xbf`, Mikoto 전용 `0xad`, Shino 전용 `0xa2` 등)은 이번과 같은
  "직전 줄 호명 스캔 + 말투 대조" 방법으로 계속 확정 가능 — 다음 세션에서 이어서 진행.

### ★ "깨진 프리픽스" 후속 조사: 반각문자(half) 가설 반증, 새 가설 확정 (2026-08-10)

사용자 질문("깨진 프리픽스 구조가 반각문자와 연관있을까?")에 대해 `decode_value()`와
`CODES`(font_map_full.json)로 프리픽스 구성 토큰들을 직접 대조 확인.

- **반증**: 프리픽스 중간에 반복되는 `0x0000`,`0x0030`,`0x0022`,`0x0002`,`0x0011`,
  `0x0029`,`0x0037`,`0x0046`,`0x0003`,`0x0001` 등은 전부 `decode_value()`/`CODES` 양쪽
  다 **`ctrl`**로 분류된다(`bank==0 and low<0x80` 조건). `half`는 `bank==0 and
  low>=0x80`인 경우인데 이 값들은 전부 `low<0x80`이라 `half` 경로에 해당하지 않는다.
  즉 반각문자와는 무관하다.
- **새 발견**: 프리픽스 맨 앞의, `find_dialogue_blocks()`가 블록 시작점으로 오인한 토큰
  (`0x0148`,`0x0574`,`0x04CC`,`0x0568`,`0x04C0`,`0x0728`,`0x051C`,`0x0804`,`0x0174` 등,
  매번 다른 값)은 전부 `decode_value()`상 **`full`**(진짜 글자 타일)로 판정된다. 이 값들은
  `font_map_full.json`의 `CODES`에 항목이 아예 없거나(`kind=None`) 있어도 `char`가
  배정 안 된 경우가 섞여 있다.
- **원인 후보 좁힘**: `decode_value()`는 `bank!=0`이고 `v+33 < NUM_TILES`(=14336)이면
  묻지도 따지지도 않고 `full`로 판정한다(`mes_codec.py:41-43`). 이 프리픽스 첫 토큰들은
  값 자체가 작아서(361~2085 범위, 전부 `v+33 < 14336`) 이 조건을 우연히 만족한다 —
  즉 실제로는 텍스트 글리프가 아니라 **포인터/인덱스/파라미터 값인데 타일 범위 안에
  들어간다는 이유만으로 `full`로 오판**되고, `find_dialogue_blocks()`가 이를 블록
  시작점(`start`)으로 잘못 잡아 뒤따르는 `ctrl` 나열을 블록 콘텐츠 안에 통째로
  포함시키는 것으로 추정된다. 아직 가설 단계 — `0x29` 등에 이름을 매핑하기 전에 이
  가설을 `kind_of()`/실제 화면 증거로 확정해야 한다(위 절의 "다음 조사 방향" 그대로 유효).

## ★ 웹툴 speakerMap 동기화 + Gemini 제거 + macOS Foundation Model 번역 백엔드 추가 (2026-08-10)

**speakerMap.js 동기화**: 이번 세션 초반에 `analysis/speaker_map.py`에 추가한 신규 화자 7명
(`0xa` Kaidou, `0xf` 레오나 아버지, `0x3f` Kyo, `0x41` Iori, `0x48` Goenitz, `0x52` Kagura
Maya, `0x76` Ukyo) + `SPECIAL_NAMES`의 `0x44`([마리의 개])를 `webtool/server/lib/speakerMap.js`
JS 포트에도 동일하게 반영. 로직(`speakerOf()`)은 손대지 않고 데이터 딕셔너리만 동기화.

**Gemini 백엔드 완전 제거**: `webtool/server/lib/geminiTranslate.js` 삭제, `csv.js`의
`engine === "gemini"` 분기·`apiKey`/`model` 쿼리 파라미터·`GEMINI_API_KEY` 환경변수 참조 제거,
`public/style.css`의 미사용 `.gemini-config-box`/`.gemini-help-text` 죽은 CSS 제거(대응하는
HTML 요소가 이미 없었음 — UI에서 Gemini를 선택할 방법 자체가 애초에 없던 상태였다).

**macOS Foundation Model 번역 백엔드 신규 추가**: 이 머신(macOS 26.5.1, Apple Silicon, Xcode
26.5 / Swift 6.3.2)에서 `SystemLanguageModel.default.availability` → `available` 확인 후 실제
기능 구현.

- Node.js는 Swift 전용 `FoundationModels` 프레임워크를 직접 호출할 수 없어 브릿지가 필요.
  `webtool/native/fm-translate/`(Package.swift + Sources/fm-translate/main.swift)에 독립
  Swift CLI를 신규 작성: stdin으로 JSON 문자열 배열을 받아 20개씩 청크로 나눠
  `LanguageModelSession`에 순차 번역 요청 후, 최종 결과를 JSON 배열로 stdout에 한 번에 출력
  (진행 상황은 stderr에 한 줄씩). 컴파일된 바이너리(`.build/`)는 커밋하지 않고 `.gitignore`에
  추가 — 소스만 리포에 남긴다.
- `webtool/server/lib/foundationTranslate.js`: 바이너리 부재 시 최초 1회 `swift build -c
  release`를 자동 실행(캐시), 이후 `spawn`으로 바이너리를 호출해 stdin/stdout으로 데이터를
  주고받는 Node 래퍼. `freeTranslate.js`와 동일한 `translateBatch(items, onProgress)` 형태로
  `csv.js`의 `/translate-stream` 라우트에서 `engine=foundation`일 때 호출.
- `public/index.html`/`app.js`에 번역 엔진 선택 드롭다운(`#engine-select`: 무료 번역기 / macOS
  Foundation Model)을 신규 추가 — Gemini 때는 이런 UI 선택지 자체가 없었으므로 이번이 웹툴에서
  처음 생기는 엔진 선택 기능이다.

**실측으로 확인된 중요 캐릭터**: 온디바이스 모델에 태그 보존을 프롬프트로만 지시하면 신뢰할 수
없다.
- 지시문 없이 그냥 번역시키면 `<0002>`, `<6E5C>` 같은 제어 태그가 **통째로 소실**된다.
- few-shot 예시 2개를 지시문에 추가하면 대부분 케이스에서 보존되지만, 한 번은 다중 태그 프리픽스
  (`<0148><0000><0030>`)에서 앞자리 0이 날아가는(`<148><0><30>`) 손상이 관측됨(재시도 로직 추가 후
  재검증에서는 정상 보존 — `fm-translate` 바이너리에 청크당 최대 3회 재시도 + 응답 개수 불일치 시
  원문 유지 폴백을 넣어 완화했지만, 근본적으로 클라우드 모델보다 태그 보존 신뢰도가 낮을 수 있음).
- 다행히 `webtool/server/lib/translateIo.js`의 `validatePlaceholders()` + `pipeline.js` 재삽입
  검증이 **모든 엔진에 공통으로** 이미 걸려 있어서, 어떤 엔진이 태그를 훼손해도 해당 블록만
  `problems`로 걸러지고 조용히 데이터가 깨지지는 않는다(자유번역·Gemini 때부터 있던 기존 안전망을
  그대로 재사용 — 새로 만든 게 아님).

**검증**: `node -c`로 변경된 모든 `.js` 파일 구문 확인, `swift build -c release` 정상 완료,
`echo '[...]' | fm-translate` 및 Node `foundationTranslate.translateBatch(...)` 직접 호출로
태그 보존 번역 확인(빈 문자열은 모델 호출 없이 그대로 통과), `npm install` 후 웹툴 서버 기동해
`csv.js`/`foundationTranslate.js` 포함 전체 라우트 모듈이 에러 없이 로드되고 HTTP 200 응답하는
것까지 확인. 실제 브라우저 UI로 프로젝트를 만들어 엔진 드롭다운→자동 초벌번역 전체 플로우를 도는
것은 아직 미검증(현재 `webtool/workspace/`에 생성된 프로젝트가 없어 스킵) — 다음 세션에서 실제
프로젝트로 한 번 더 확인 권장.

## ★ macOS Foundation Model 번역 백엔드 폐기 → Translation 프레임워크로 교체 (2026-08-10)

**사용자 보고**: "foundation model이 제대로 동작안하는거 같네 한글로 번역은 전혀 안되었고
그냥 원문으로 나와." — 위 항목에서 신규 추가한 `fm-translate`(FoundationModels LLM 기반)
백엔드가 실사용 시 완전히 무동작.

**원인 진단**: `temp/translations/dom1_Athena.csv`의 실제 대사 20줄(`<이름:3232>`, `<485C>`,
`<6E5C>` 등 태그 다수 포함)로 재현한 결과, `fm-translate`의 엄격한 JSON 배열 개수 검증에
3회 연속 실패 → "청크 번역 실패 - 원문을 그대로 유지합니다" 폴백이 발동해 20줄 전부가
원문 그대로 반환됨(사용자가 관찰한 증상과 정확히 일치). 검증을 우회하고 원시 응답을 직접
찍어본 결과, FoundationModels가 애초에 일→한 번역을 제대로 수행하지 않고 있었다: 일본어
원문을 번역 대신 그대로 에코/재구성하고, 배열 길이를 20→24개로 바꿔버리고(위치 대응 붕괴),
태그를 문장 내 다른 위치로 옮기고, 심지어 태그 내용 자체를 오역(`<이름:3232>` →
리터럴 `名前:3232`)하기도 했다. 즉 프롬프트 튜닝으로 해결 가능한 문제가 아니라
**FoundationModels(온디바이스 요약/재작성용 LLM)가 이 규모의 번역 작업 자체에 부적합**한
것이 근본 원인.

**해결책**: 사용자가 직접 제안한 대안 — "foundation model 대신 macos에 내장된 기본
번역기 사용은 되나?" — 을 그대로 채택. macOS/Safari의 "번역" 기능을 구동하는 Apple의
전용 온디바이스 NMT 엔진인 `Translation` 프레임워크(`TranslationSession`)로 완전히
교체했다.

- `webtool/native/fm-translate/` 디렉토리 전체 삭제, 대신 `webtool/native/mac-translate/`
  신규 작성(Package.swift: `swift-tools-version:6.2`, `.macOS(.v26)` — 헤드리스 초기화자
  `TranslationSession(installedSource:target:)`가 컴파일 에러로 macOS 26.0+ 요구임을 확인).
  `Sources/mac-translate/main.swift`: `LanguageAvailability().status(from:to:)`로 ja→ko
  언어팩 설치 여부 확인 후 세션 생성, `TranslationSession.Request(sourceText:clientIdentifier:)`
  + `session.translations(from:)` 배치 API를 사용 — `clientIdentifier`로 응답을 원본 위치에
  순서 무관하게 정확히 매칭하므로 LLM 방식의 "배열 개수가 맞아야 한다"는 취약한 가정이
  아예 필요 없어짐. 50개씩 청크, 청크당 최대 3회 재시도, 실패 시 원문 유지 폴백은 유지.
- `webtool/server/lib/foundationTranslate.js` 삭제 → `webtool/server/lib/macTranslate.js`
  신규 작성(동일한 `ensureBuilt()`/`runBinary()`/`translateBatch(items, onProgress)` 패턴,
  바이너리 경로만 `native/mac-translate/.build/release/mac-translate`로 교체).
- `webtool/server/routes/csv.js`: import를 `macTranslateBatch`로 교체, 엔진 식별자를
  `"foundation"` → `"mac"`으로 변경.
- `webtool/public/index.html`: `<option value="foundation">macOS Foundation Model</option>`
  → `<option value="mac">macOS 기본 번역기</option>`로 변경(`app.js`는 select 값을 그대로
  쿼리에 넣는 구조라 별도 수정 불필요).
- `webtool/.gitignore`: `native/fm-translate/.build/` → `native/mac-translate/.build/`.

**검증**: `swift build -c release` 정상 빌드. `dom1_Athena.csv`의 동일한 실제 대사 20줄로
`macTranslate.translateBatch()`를 Node에서 직접 호출 — **20줄 전부 정상적으로 한국어 번역되고
`<485C>`/`<이름:3232>`/`<6E5C>` 태그가 모두 정확한 위치에 그대로 보존**됨을 확인(재시도 없이
1회 시도로 성공). `node -c`로 `csv.js`/`macTranslate.js` 구문 확인. `webtool/tool.sh stop` →
`start`로 서버 재기동 시 `MODULE_NOT_FOUND` 등 에러 없이 정상 기동, `curl` HTTP 200 확인.

**결론**: 이 프로젝트의 번역 초벌 작업에는 FoundationModels(범용 온디바이스 LLM)보다
Translation 프레임워크(전용 온디바이스 NMT)가 훨씬 적합하다 — 번역 품질뿐 아니라 태그
보존 안정성과 배치 정합성(clientIdentifier 매칭) 면에서도 근본적으로 더 신뢰할 수 있는
설계. `validatePlaceholders()` 안전망은 계속 유효하게 동작하지만(어떤 엔진이든 태그 훼손
시 해당 블록만 problems로 스킵), 이번 교체로 안전망에 의존해야 하는 빈도 자체가 크게
줄어들 것으로 예상된다(다음 세션에서 실제 브라우저 UI로 전체 플로우 회귀 확인 권장 —
아직 미검증).

## ★ 원문보다 짧은 번역: 공백 토큰 자동 패딩 (2026-08-10)

**경위**: 사용자 지시 "이전에 원본보다 짧은경우는 공백삽입으로 패딩해서 해결한다고 했잖아
짧은 경우는 알아서 패딩 맞춰주는걸로 해서 ok처리 해줘". 실제로 `mes_translate_reinsert.py`
에는 이미 이 방침이 에러 메시지 문구("Pad short translations with a blank/filler code or
shorten the wording")로만 남아있었을 뿐, 자동화는 안 되어 있어 사람이 수동으로 처리해야
했다 — 방금 웹툴 재삽입 리허설에서 `dom1OP_0701_1.mes`의 `tokenMismatch` 91건 중 다수가
`shorter` 방향이었던 것으로 확인됨.

**구현**: 공백 문자 " "는 이미 `_CHAR_TO_CODE[" "] = 0xA002`(전각 16x16 빈 타일)로 매핑되어
있었으므로, `SPACE_TOKEN = 0xA002` 상수로 명시하고(웹툴 `translateIo.js` / 파이썬
`translate_io.py` 양쪽), 인코딩된 토큰 수가 원본 블록의 고정 토큰 수(`expected_len`)보다
**짧을 때만** 그 차이만큼 `SPACE_TOKEN`을 뒤에 채워 정확히 일치시키도록 함
(`mes_translate_reinsert.py`의 `build_file()`, 웹툴 `pipeline.js`의 `buildFileTokens()`
및 CSV 저장 시 상태 표시용 `validateRow()`/`saveFile()` 양쪽 모두 반영). **길 때(longer)는
그대로 에러로 남긴다** — 잘라내면 내용이 손실되므로 안전하게 자동화할 수 없고, 번역 문구를
줄이는 수동 조치가 필요.

**검증**: 웹툴 서버 재시작 후 `/api/build/reinsert`로 `dom1OP_0701_1.mes` 재확인 —
`tokenMismatch` 91 → 56건(전부 `longer`만 남음, `shorter` 방향은 0건), 파일 전체
problems 102 → 67건. 폰트 예산 관련 `noFontCode`(4건, 위 항목 참고)와 mac-translate 태그
누락 `placeholder`(7건, 위 "실전 재삽입 테스트" 항목 참고)는 이번 변경과 무관하게 그대로.

## ★ dom1OP_0701_1.mes 실전 재삽입 테스트에서 발견된 두 가지 이슈 (2026-08-10)

**경위**: `mac-translate`로 `dom1OP_0701_1.mes`(119개 대화 블록)를 자동 초벌번역한 뒤
웹툴 재삽입을 실행하자 `filesSkipped: 1`, 다수의 `problems`가 보고됨("테스트빌드 해보려니
에러가 난다"). 원인은 서로 독립된 두 가지였다:

**1) mac-translate 태그 누락률(~5.9%)**: 119개 블록 중 7개에서 `<6E5C>`/`<485C>` 같은
`<HEX>` 제어 태그가 번역 결과에서 통째로 사라짐(`validatePlaceholders`가 missing
placeholders로 정확히 잡아냄). 앞서 검증한 깨끗한 20줄 테스트에서는 태그 누락이 0건이었는데,
119줄 규모의 실제 파일에서는 5.9%까지 올라간 것 — TranslationSession도 LLM 방식보다는
훨씬 안정적이지만 태그 누락이 완전히 0%는 아님을 확인. `validatePlaceholders` 안전망이
해당 블록만 problems로 정확히 격리하므로 데이터 손상으로 이어지지는 않지만, 자동번역만으로
100% 통과는 기대할 수 없고 일정 비율은 수동 검수/재번역이 필요하다.

**2) 고정 토큰 슬롯 정합성(구조적 제약, 버그 아님)**: `pipeline.js`의 `buildFileTokens()`는
각 대화 블록이 원본 `.mes`에서 차지하던 정확한 토큰 슬롯 수(`expectedLen`)와 재삽입할
텍스트의 인코딩 후 토큰 수가 정확히 일치해야만 통과시키고, 하나라도 문제(태그 불일치,
폰트 코드 없음, 토큰수 불일치)가 있으면 `mes_translate_reinsert.py`와 동일하게 파일 전체를
all-or-nothing으로 스킵한다(주석에 명시됨). 기계번역은 원문과 우연히 토큰 수가 일치할
확률이 사실상 0에 가까우므로, 어떤 번역 엔진을 쓰든 실제 게임에 들어갈 최종 번역문은
사람이 줄 길이를 맞춰 편집하는 과정이 구조적으로 필요하다 — 이번 세션에 새로 발견된
버그가 아니라 이 롬핵의 원래 설계 제약.

**3) 폰트 타일 예산 갭(→ 아래 항목에서 해결 착수)**: `problems`의 상당수가
"no font code assigned for character" 에러였고, 원인은 현재 `font_map_kr.json`의
1,558자 한글 매핑이 자모 빈도 근사치 방식으로 선정되어 다,하,아,이,그,나,가 같은 극히
흔한 받침 없는 음절이 하나도 포함되지 않았기 때문. 상세 진단과 해결 작업은 바로 아래
"한글 폰트 테이블 재선정" 항목 참고.

## ★ 한글 폰트 테이블 재선정: 실사용 빈도 기반으로 1,558자 재배정 (2026-08-10)

**경위**: 위 항목의 3)에서 드러난 대로, 기존 `font_map_kr.json`의 1,558개 한글 슬롯은
`CHOSEONG`/`JUNGSEONG`/`JONGSEONG`에 감으로 붙인 "자모 빈도 근사치 가중치"로 선정된
것이었다. 실제로 확인해보니 게,다,서,테,하,아,리,쿠,나,이,로,응,으,빨,어,일,가,또,벌,기,
옷,았,그,왜,정,시,했,신,야,쁜,선 등 받침 없는 극히 흔한 음절이 단 하나도 포함되어 있지
않았다 — 자모 가중치 방식이 받침 없는 흔한 음절을 구조적으로 저평가한 것. 사용자 지시:
"자주쓰는 한글 1559자로 넣자고 했는데 이상한것만 들어갔나보네 1559자는 유지하되 필요한
한글을 다시 찾아서 테이블 업데이트 하자" — 1,559자(실질 1,558자) 예산 상한(규칙 4)은
그대로 유지하고, 그 슬롯에 배정할 글자만 실사용 빈도 기반으로 다시 정하기로 함.

**1) 실사용 빈도 코퍼스 생성**: 유의미한 기존 번역 코퍼스가 없어(`translation_export.csv`
38,319행 중 118행만 이번 프로젝트 자체 테스트로 채워진 상태), mac-translate 엔진으로
게임 대사 원문 2,000개를 무작위 샘플링해 실제로 번역한 뒤 그 결과에서 빈도를 집계하는
방식을 택함(500개 벤치마크 216.87초 → 2,000개 약 15분 소요, 사용자 승인 하에 진행).
`temp/gen_hangul_freq_corpus.js`로 `webtool/server/lib/macTranslate.js`의
`translateBatch()`를 그대로 호출해 `temp/hangul_freq_corpus.json`(번역 문자열 2,000개)
생성.

**2) 빈도 집계 + 자모 back-off 보완**: `temp/build_hangul_freq_manifest.py`가
`<[^>]*>` 태그 제거 후 가-힣 음절 빈도를 집계 — 코퍼스에서 한글 문자 총 24,249자, 고유
음절이 관측됨. 기존 `font_map_kr.json`에서 이미 안전성 검증이 끝난 1,558개 코드
(`code`/`real_tile`)를 그대로 재사용하고(재검증 불필요, 이미 확정된 풀), 빈도 상위
음절부터 1:1 배정. 관측된 고유 음절 수가 1,558개 슬롯보다 적어 부족분은 "자모 back-off"로
채움 — 감으로 정한 가중치가 아니라, **이번 2,000개 실사용 코퍼스 자체에서 집계한
초성/중성/종성 각각의 실제 등장 빈도**를 곱한 점수로 아직 코퍼스에 직접 등장하지 않은
음절의 순위를 매기는 방식(희소 데이터 보완/smoothing 기법). 결과를
`temp/kanji_only_hangul_glyphs.json`에 저장, `source` 필드로 `freq_corpus_2000`
(직접 관측)과 `jamo_backoff_from_freq_corpus_2000`(back-off로 채움)을 구분해 근거를 남김.
대표 흔한 음절(게,다,서,테,하,아,리,쿠,나,이,로,응,으,빨,어,일,가,또,벌,기,옷,았,그,왜,
정,시,했,신,야,쁜,선) 전부가 새 매니페스트에 포함됨을 확인.

**3) 폰트 맵/글리프 재생성**: 기존 확정 스크립트를 로직 변경 없이 그대로 재실행.
`analysis/rebuild_font_map_kr.py` → 매니페스트를 읽어 `font_map_kr.json` 재생성(자체
안전 검사: 매니페스트의 모든 코드가 한자-안전 코드이거나 빈 슬롯인지 확인,
`font_map_full.json`은 전혀 건드리지 않음). `analysis/apply_font_art.py` → BDF
(Galmuri11.bdf, 총 20,966글리프 중 한글 11,172자)를 `unpack_origin/data/Font_DOM.nbfc`
원본에서부터 항상 새로 렌더링, 결과: "Successfully rendered 1558 Hangul glyphs
(0 missing from BDF)".

**4) 검증**:
- `font_map_full.json`: 한글 문자 개수 0 확인 — 규칙 4(원문 맵 순수성) 위반 없음.
- `font_map_kr.json` 통계: `hangul_mapped: 1558`, `hangul_budget`(이번 추출 슬롯 수
  기록용 필드) `1558` — 1,559 예산 상한 이내, 축소/초과 없음.
- `temp/verify_reinsert_dom1OP.js`로 `dom1OP_0701_1.mes`(119개 블록) 재삽입 리허설을
  갱신 전/후로 비교:

  | 구분 | noFontCode | tokenMismatch | placeholder | other | problems 총계 |
  |---|---|---|---|---|---|
  | 이전(자모 가중치 테이블) | 90 | 18 | 7 | 0 | 115/119 블록 |
  | 이후(실사용 빈도 테이블) | **4** | 91 | 7 | 0 | 102/119 블록 |

  핵심 목표였던 `noFontCode`(글꼴 코드 미배정으로 스킵)가 **90 → 4로 급감**. `tokenMismatch`가
  18 → 91로 늘어난 것은 회귀가 아니라, 이전에는 `noFontCode`에서 먼저 걸려 뒤 검사까지
  못 갔던 블록들이 이제 그 관문을 통과해 원래부터 있던(이번 작업 범위 밖의 구조적 문제인)
  고정 토큰 슬롯 정합성 문제로 넘어가 노출된 것 — 위 "실전 재삽입 테스트" 항목의 2)에서
  이미 진단된 것과 동일한 이슈. `placeholder`(mac-translate 태그 누락, 위 항목의 1))는
  7건 그대로 — 이번 작업 범위 밖.
- 남은 4건의 `noFontCode`는 `grep -E "no font code assigned"`로 확인한 결과 전부
  이번 작업 범위를 벗어난 것들: 부호 2건(작은따옴표 `'`, 가운뎃점 `·` — 애초에 한글
  테이블 대상이 아님), 실제 한글 음절 2건(퇴, 뻗 — 1,558자 예산을 넘어서는 음절로,
  예산 상한을 지키는 이상 발생하는 고유한 트레이드오프이지 버그가 아님).

**결론**: 1,559자(실질 1,558자) 예산 상한을 그대로 지키면서, 슬롯 배정 기준을 "감으로
정한 자모 가중치"에서 "실사용 번역 코퍼스 직접 빈도 + 동일 코퍼스 기반 자모 back-off"로
교체해 `noFontCode` 스킵을 90건에서 4건으로 크게 줄였다. 남은 4건은 예산 한계(음절 2건)와
범위 밖 이슈(부호 2건)로, 추가 조치 없이 현재 상태를 확정한다.

**⚠ 함정 — 웹툴 서버는 `font_map_kr.json`을 시작 시 1회만 메모리에 로드함**: 위 검증을
`temp/verify_reinsert_dom1OP.js`(별도 Node 프로세스, 파일을 매번 새로 읽음)로는 90→4 개선을
확인했는데, 이미 떠 있던 웹툴 서버로 실제 `/api/build/reinsert`를 호출해보니 예전과 동일한
90건 수준의 `noFontCode`가 그대로 재현됐다. 원인은 `webtool/server/lib/mesCodec.js`가
`_fmKr = JSON.parse(fs.readFileSync(FONT_MAP_KR_PATH, ...))`를 모듈 최상단(파일 로드 시
1회)에서 실행하기 때문 — `font_map_kr.json`을 재생성해도 이미 실행 중인 서버 프로세스는
재시작 전까지 구 데이터를 계속 사용한다. `./tool.sh stop && ./tool.sh start`로 재시작 후
API로 재확인하니 noFontCode 4건으로 정상 반영됨을 확인. **앞으로 `font_map_kr.json`/
`font_map_full.json`을 재생성한 뒤에는 반드시 웹툴 서버를 재시작해야 한다** — 재시작 없이
"파일은 바뀌었는데 결과가 그대로"라면 이 캐싱이 원인일 가능성이 가장 높음.

## ★ 자동번역 엔진: 무료 번역기(기본) 옵션 제거, macOS 번역기만 유지 (2026-08-10)

사용자 지시: "자동번역 기능에 기본 번역은 빼고 macos번역만 남겨". `webtool/public/index.html`의
`#engine-select`에서 `<option value="free">무료 번역기 (기본)</option>` 제거,
`webtool/server/routes/csv.js`의 `engine === "mac"` 분기 제거하고 `macTranslateBatch` 직접
호출로 단순화(더 이상 쓰이지 않는 `engine` 쿼리 파라미터 구조분해도 함께 정리). 그 결과
어디서도 참조되지 않게 된 `webtool/server/lib/freeTranslate.js`는 파일 자체를 삭제.
`node -c`로 `csv.js`/`index.js` 구문 확인 후 서버 재기동, HTTP 200 확인.


## ★ playername.mes: 원문 토큰 수 일치 검증 예외 처리 (2026-08-10)

**경위**: `/api/build/reinsert` 재검증 도중 원문보다 짧은 번역은 공백 패딩으로 해결했지만,
`playername.mes`(주인공 이름 프리셋: 성/이름 기본값 나열 테이블, `村上`/`大地`/`優`/`大之介`
등 6블록)는 여전히 "longer" 방향 token count mismatch로 걸릴 수 있는 상태였다. 사용자 지시:
"playername.mes 는 사용자 입력 주인공 이름 기본으로 넣는거니까 성,이름 모두 3자 제한이라서
이건 예외로 할 수 있게 해줘".

**배경 확인**: 이 파일은 `UNKNOWN_0x6e5c` 화자 버그 조사(위 "★★ `UNKNOWN_0x6e5c` 화자 버그
근본 원인 확정 및 수정" 절) 때 이미 "화자 헤더 자체가 없는 평범한 이름/라벨 나열 테이블"로
확인된 바 있다 — `Script/`의 실제 대사 박스(고정 폭 렌더링, 토큰 수가 달라지면 실기/melonDS에서
행 남 - 2026-08-05 확인)와는 다른 구조다. 각 블록은 성 또는 이름 하나이며, 게임 내 이름
입력/표시 UI 자체가 성 3자·이름 3자로 제한하므로 원문(일어) 토큰 수와 번역(한글) 토큰 수가
같아야 할 구조적 이유가 없다 — 대신 3토큰(3자) 상한만 지키면 안전하다.

**구현**: `playername.mes`에 한해 "원문과 동일한 토큰 수" 검증을 완전히 건너뛰고, 대신
"인코딩된 토큰 수가 3을 넘는지"만 검사하도록 4곳에 반영:
- `webtool/server/lib/pipeline.js`: `PLAYERNAME_FILE`/`PLAYERNAME_MAX_TOKENS` 상수 추가.
  `validateRow()`에 `fname` 파라미터 추가해 `playername.mes`일 때 패딩/길이일치 로직을
  건너뛰고 3토큰 초과만 에러 처리. `saveFile()` 호출부와 mismatch 리포트 분기도 `fname !==
  PLAYERNAME_FILE` 조건으로 우회. `buildFileTokens()`의 블록 루프에도 동일한 분기를 패딩
  로직 앞에 추가(패딩 없이 인코딩된 토큰을 그대로 사용, 3토큰 초과 시에만 problems에 기록).
- `analysis/mes_translate_reinsert.py`: 동일한 상수와 분기를 `build_file()`에 미러링(패딩
  체크 이전에 `fname == PLAYERNAME_FILE`이면 3토큰 상한만 검사하고 `continue`).
- `<HEX>` 플레이스홀더 개수 검증(`validate_placeholders`/`validatePlaceholders`)은 이 예외와
  무관하게 그대로 유지 - 길이 제약만 예외이고 제어 코드 보존 요구는 동일하게 적용된다.

**검증**: `node -c pipeline.js`, `python3 -m ast` 구문 확인 후 `./tool.sh stop && ./tool.sh
start`로 재기동(수정 사항이 즉시 반영되도록 - 위 font_map_kr.json 캐싱 함정과 동일 원리).
`/api/build/reinsert` 재호출 결과 `playername.mes` 관련 problems 0건으로 완전히 해소됨을
확인(재기동 전 이 파일에 대해 남아있던 token count mismatch 항목들이 전부 사라짐).


## ★ 한글 테이블에 실전 누락 글자 4개 강제 포함: 꿉,콩,찡,퇴 (2026-08-10)

**경위**: `/api/build/reinsert` 재검증에서 `dom1OP_0701_1.mes`에 "no font code assigned"
에러 4건 발견 - `꿉`(소꿉친구), `콩`(킹콩캉콩 의성어), `찡`(찡찡거리다), `퇴`(퇴학). 실사용
빈도 코퍼스(2,000개 샘플)에는 표본 한계로 관측되지 않았던 음절들. 사용자 지시: "추가 글자가
있으니 한글테이블 수정해줘".

**구현**: `temp/build_hangul_freq_manifest.py`에 `FORCE_INCLUDE_CHARS = list("꿉콩찡퇴")` 상수
추가 - 빈도 랭킹과 무관하게 이 4글자는 항상 슬롯을 강제 배정하고, 자리 확보를 위해 랭킹
최하위 4음절(`켠`,`틱`,`귄`,`몄` - 전부 자모 back-off로 채워진 미관측 추정 음절)을 제외하는
로직 추가. 실사용 재삽입 테스트로 확인된 증거가 코퍼스 추정치보다 신뢰도가 높으므로, 빈도
방식과 병행하되 우선순위를 더 준다는 원칙.

**실행 순서** (1,559자 예산 준수, 안전 코드 풀 1,558개는 그대로 재사용 - 재검증 불필요):
1. `PYTHONPATH=analysis python3 temp/build_hangul_freq_manifest.py` → 매니페스트 재생성
   (강제 포함 4글자 확인 로그 출력됨).
2. `python3 analysis/rebuild_font_map_kr.py` → `font_map_kr.json` 재생성, `Hangul slots:
   1558 / 1559 budget` 그대로 유지(규칙 4 위반 없음).
3. `python3 analysis/apply_font_art.py` → `unpack/data/Font_DOM.nbfc`에 새 1,558자 글리프
   재렌더링 (매번 origin에서 깨끗하게 재시작).
4. `./tool.sh stop && ./tool.sh start` → 웹툴 서버가 새 `font_map_kr.json`을 다시 로드하도록
   재기동 (위 캐싱 함정과 동일 원리 - 파일만 갱신하고 재기동을 빼먹으면 반영 안 됨).

**검증**: `/api/build/reinsert` 재호출 결과 `dom1OP_0701_1.mes`의 "no font code assigned"
에러 4건 전부 해소(0건). 남은 problems는 1건뿐이며 이는 이번 작업과 무관한 기존 이슈
(block 93, 원문 69 토큰 vs 번역 71 토큰 "longer" 방향 불일치 - 수동 축약 필요, 범위 밖).


## ★★ 웹툴 빌드에서 공백이 "幡" 등 원본 일어 글리프로 보이는 버그 수정 (2026-08-10)

**경위**: 사용자가 melonDS 실기 스크린샷 3장을 제출 - 대사 중 공백이 들어가야 할 자리마다
공백 대신 알아볼 수 없는 한자 비슷한 글자(幡)가 찍혀서 "미안! 幡지금幡나갈께!"처럼 보임.

**원인**: `analysis/apply_font_art.py`(파이썬, 최종 확정 스크립트)는 렌더링 마지막에
공백 타일 2곳(`10242`=전각 공백 A002, `390`=반각 공백)을 명시적으로 0으로 클리어하는 단계가
있다 - pristine `Font_DOM.nbfc`의 이 타일 자리에는 애초에 진짜 일본어 글리프 픽셀 데이터가
박혀 있고, "공백"이라는 건 어디까지나 코드 규약(`0xA002`)일 뿐 실제 픽셀은 비어있지 않기
때문이다. 그런데 웹툴의 JS 포트 `webtool/server/lib/pipeline.js`의 `applyFontArt()`는 이
공백 제로클리어 단계를 처음부터 포함하지 않고 있었다 - 한글 1,558자 글리프 렌더링만 하고
끝나서, 웹툴로 빌드한 ROM은 항상 공백 자리에 원본 일어 글리프가 그대로 남아 있었던 것으로
보인다(이번에 처음 실기 테스트로 발견됐을 뿐, 웹툴 빌드 자체는 전부터 이 문제를 안고 있었을
가능성이 높음 - `docstring`에 "port of apply_font_art.py"라고 되어 있었지만 실제로는 이
부분이 누락된 불완전한 포트였음).

**수정**: `pipeline.js`의 `applyFontArt()` 한글 글리프 렌더 루프 뒤에 `FULL_SPACE_TILE=10242`
(전각, 2×2=4개 서브타일 클리어)와 `HALF_SPACE_TILE=390`(반각, 2개 서브타일 클리어)를 0으로
채우는 로직을 `analysis/apply_font_art.py`와 동일하게 추가.

**검증**: `node -c` 구문 확인 → 서버 재기동 → `/api/build/reinsert` 재호출(`filesWritten: 865,
filesSkipped: 0, problems: 0`) → `webtool/workspace/dom1/build/data/Font_DOM.nbfc`를 직접
바이너리로 열어 타일 10242/10243/10274/10275(전각) 및 390/391(반각) 6개 서브타일이 전부
`all zero`임을 확인. 사용자가 다음 빌드(`/api/build/pack`)로 ROM을 재생성해 melonDS에서
공백이 정상적으로 빈 칸으로 보이는지 실기 재확인 필요.


## ★★ 띄어쓰기/패딩 반각 전환 + 페이지송출 마커 뒤 패딩 위치 실험적 수정 (2026-08-10)

**요청**: "띄어쓰기나 패딩은 반각으로 해줘 그리고 패딩해서 그런지 대사가 끝나도 공백으로 다음
대사로 안넘어가고 빈칸이 나올때가 있는데 공백을 채우지 말고 바로 종료되는 제어문자를 넣어서
테스트 해보자" — 두 갈래: (a) 띄어쓰기 문자/패딩 필러를 전각(`0xA002`)이 아닌 반각으로,
(b) 대사 종료 후 빈 화면(추가 탭 필요) 증상을 패딩 방식 변경으로 해소 시도.

### (a) 반각 공백 코드 `0x00CA` 복원 - 과거 확정 수정이 유실되어 있었음

`font_map_full.json`/`font_map_kr.json`을 직접 조회해보니 `00CA` 키가 아예 없고 `A002`
(전각)만 `"char": " "`로 잡혀 있었다. 2026-08-06 세션에서 이미 확정·melonDS 실기 테스트까지
끝낸 반각 공백 코드(`0x00A4` 오식별 수정 → `0x00CA`로 재배정, 타일 390/391이 `0x0606`
[`kind:'full'`, `char` 없음, 대사 미사용 확인됨]과 공유되는 안전한 슬롯)가 이번 세션 이전
어느 시점엔가 사라져 있었던 것. 원인 추적: `analysis/build_font_map_v2.py`(전체 재생성
스크립트, `full_code_tile_map.json` + 하드코딩된 `FULL_CHARS`/`HALF_CHARS`/
`MANUAL_FULL_CODES` 딕셔너리로부터 `font_map_full.json`을 처음부터 다시 만듦)가 이 수동
패치 이후 재실행되면서, 이 스크립트 자체는 `00CA` 패치를 전혀 모르므로 조용히 유실된 것으로
확인됨 - 이번 세션에서 새로 생긴 회귀가 아니라 이전부터 존재하던 상태.

재확인한 안전성: 현재 테이블 기준 타일 390/391/422/423은 여전히 `0x0606`(`full`, char 없음)
단독 소유, 한글 글자도 점유하지 않음 - 재사용 안전.

**구현**:
- `font_map_full.json`에 `"00CA": {"kind":"half","count":0,"real_tile":390,"char":" "}` 추가
  (스크립트로 직접 편집 - 대용량 자동생성 JSON이라 Edit 도구 대신 파이썬 사용). 이 파일은
  규칙 4상 "순수 일어/한자만" 유지해야 하지만 공백은 한글이 아니므로 위반 아님(`A002`도
  이미 동일 선례 있음).
- `python3 analysis/rebuild_font_map_kr.py` 재실행 → `font_map_kr.json`에 `00CA` 전파 확인
  (`kind='half'`는 한글 오버레이 대상이 아니라 그대로 복사되므로 1,558자 예산 영향 없음,
  `Hangul slots: 1558/1559 budget` 그대로).
- `python3 analysis/apply_font_art.py` 재실행 (파이프라인 일관성 목적 - 타일 390 제로클리어는
  이미 무조건 실행되는 하드코딩이라 실제 픽셀엔 영향 없었음).
- `analysis/translate_io.py`/`webtool/server/lib/translateIo.js`: `SPACE_TOKEN=0xA002`은
  참고용으로만 남기고, 새 `HALF_SPACE_TOKEN=0x00CA`를 추가해 `_CHAR_TO_CODE[" "]` 오버라이드
  와 패딩 필러 양쪽에 사용하도록 전환 (기존엔 공백 문자만 강제로 전각을 쓰도록 덮어쓰고
  있었음 - `:`/숫자 등 다른 ASCII류 문자엔 이미 반각 우선 로직이 있었는데 공백만 예외였던
  모순을 해소).
- `analysis/mes_translate_reinsert.py`/`webtool/server/lib/pipeline.js`: 패딩 필러를
  `tio.SPACE_TOKEN` → `tio.HALF_SPACE_TOKEN`로 교체 (`validateRow`/`build_file`/
  `buildFileTokens` 전부).

**검증**: `text_to_tokens('안녕 하세요')` → 공백이 `0xca`로 인코딩됨(전 `0xa002`) 확인.
`node -c`/`ast.parse()` 구문 확인. `/api/build/reinsert` 정상(`865 written, 0 skipped`).

### (b) 대사 종료 후 빈칸 증상 - 패딩을 페이지송출 마커(`0x485C`) 앞으로 이동

**조사**: 사용자가 언급한 "바로 종료되는 제어문자"를 찾기 위해 `0x0000`을 후보로 검토했으나,
코퍼스 스캔 결과 대사블록 내부의 `0x0000`은 전부 블록 맨 앞 헤더(카운터/좌표 등 고정폭 숫자
데이터)의 일부로만 등장(80개 파일에서 48회, 전부 헤더 위치) - 본문 중간/끝의 "안전한 무시
토큰"이라는 근거가 없어 채택하지 않음. 대신 코퍼스 전수조사(`unpack/data/Script/*.mes`
38,495개 대사블록)로 확인한 사실: **37,417개(약 97%)가 블록의 마지막 토큰으로 `0x485C`
(페이지송출/입력대기 마커)를 갖고 있고, 그 직후(블록 범위 밖)에 진짜 박스 종료 마커인
`0x6E5C 0x6E5C`가 온다.** 기존 패딩 로직은 완전히 인코딩된 번역문(이미 원문 자리 그대로
`<485C>`를 끝에 보존하는 경우가 대부분) *뒤에* 필러를 붙였으므로, 패딩이 `0x485C` 마커
*뒤에* 들어가 실제 박스 종료 전에 "탭해야 넘어가는 빈 페이지"가 하나 더 생기는 구조였다 -
사용자가 보고한 증상과 정확히 일치.

미확인 제어코드를 추측으로 도입하는 대신, 위 97% 통계로 뒷받침되는 **위치 기반 수정**을
채택: 인코딩된 번역 토큰의 마지막이 `0x485C`이면 패딩을 그 앞에 끼워넣고, 아니면(3% 예외
케이스) 기존처럼 맨 뒤에 붙인다.

**구현**: `analysis/mes_translate_reinsert.py`/`webtool/server/lib/pipeline.js`에
`PAGE_TURN_TOKEN = 0x485C` 상수 추가, `build_file()`/`buildFileTokens()`의 패딩 로직을
"마지막 토큰이 `PAGE_TURN_TOKEN`이면 `new_tokens[:-1] + pad + new_tokens[-1:]`, 아니면
`new_tokens + pad`"로 변경. `pipeline.js`의 `validateRow()`는 토큰 개수만 반환하므로 위치
조정 없이 `HALF_SPACE_TOKEN`으로만 교체(실제 재삽입은 `buildFileTokens()`가 담당).

**검증 (합성 테스트)**: `new_tokens=[0x1234,0x485C]`, `expected_len=5` →
`[0x1234,0xca,0xca,0xca,0x485c]` (패딩 3개가 마커 앞에 정확히 삽입됨, 마커는 맨 끝 유지).

**검증 (실빌드 데이터, `dom1OP_0701_1.mes`)**: `dom1_common.csv`에서 `<485C>`로 끝나는 짧은
실번역(예: block 5 `'앤디<485C>'`, block 6 `'쟝<485C>'`, block 16 `'킹의 테마<485C>'` 등)을
골라 `/api/build/reinsert` 실행 후 빌드 결과 파일을 **원본(unpack) 기준 블록 좌표**로 직접
슬라이스해 확인(★★★ 주의사항: 아래 참고). 예:
- block 5 (`expected_len=5`, 인코딩 길이 2): `[a202,9000,ca,ca,485c]` → `'앤디  <485C>'` ✅
- block 6 (`expected_len=4`, 인코딩 길이 1): `[dd18,ca,ca,485c]` → `'쟝  <485C>'` ✅
- block 16 (`expected_len=8`, 인코딩 길이 6): `[...,ca,ca,485c]` → `'킹의 테마  <485C>'` ✅

전부 패딩(`0xca`)이 `0x485c` 마커 **앞**에 정확히 위치함을 확인. 반각 공백(`0xca`)이 단어
사이 띄어쓰기(`메인 테마`, `쿠라의 테마` 등)와 패딩 양쪽에 모두 쓰이고 있음도 함께 확인됨.

**★★★ 검증 시 발견한 함정 (버그 아님, 도구 사용법 주의사항)**: 처음에는 빌드된(번역이
반영된) `.mes` 파일을 `mc.find_dialogue_blocks()`로 다시 스캔해서 블록 좌표를 구하려 했는데,
이 방식은 **틀린 좌표를 준다** - `find_dialogue_blocks`는 `mc.CODES`(=`font_map_full.json`,
순수 일어 전용 맵)의 `kind` 라벨로 블록 시작점을 찾는데, 한글 전용으로 새로 배정된 "미사용
빈 2×2 슬롯" 코드(예: `0xDD18`)는 애초에 `font_map_full.json`에 존재하지 않는 코드라
`kind_of()`가 `None`을 반환한다. 그 결과 번역문의 첫 글자가 이런 빈 슬롯 코드일 때
`find_dialogue_blocks`가 이를 건너뛰고 그 뒤의 `0x485C`(이건 `font_map_full.json`에 실제
한자 코드로 존재해 `kind='full'`로 잡힘)를 블록 시작으로 잘못 인식 - 앞부분 글자가 통째로
잘려나간 것처럼 보이는 착시가 발생한다(예: block 6을 이렇게 잘못 스캔하면 `쟝`이 사라진
`'  <485C>'`만 보임). **실제 재삽입 파이프라인(`buildFileTokens`/`build_file`)은 이 문제와
무관** - 블록 좌표를 항상 원본(unpack) 파일에서만 `find_dialogue_blocks`로 구하고, 그
좌표를 그대로 재사용해 번역 토큰을 등길이로 갈아끼울 뿐, 빌드 결과물을 다시 스캔하지
않기 때문. 즉 이 함정은 순수히 "번역 반영 결과를 검산할 때 쓴 임시 조사 방법"의 오류였고
실제 게임/빌드 동작에는 영향 없음. **교훈: 빌드된 `.mes`를 검산할 때는 `find_dialogue_blocks`
를 다시 돌리지 말고, 원본 unpack 파일에서 구한 (s,e) 좌표를 빌드 파일에 그대로 적용해
슬라이싱할 것.**

**남은 작업**: 사용자가 `/api/build/pack`으로 ROM을 재생성해 melonDS/실기에서 (1) 공백이
반각으로 정상 표시되는지, (2) 대사 종료 후 빈 화면 증상이 실제로 해소됐는지 확인 필요 -
(2)는 통계적 근거에 기반한 실험적 수정이라 실기 확인 전까지는 확정 아님.

## ★★ 대사 블록 길이 고정 제약 재검증: "블록별 고정" vs "파일 전체 고정" 대조 실험 (2026-08-10)

**배경**: 2026-08-05 오프닝 장면 실기 테스트에서 "모든 대사 블록이 원본과 정확히 같은
토큰 수를 가져야 한다"는 제약이 확정되어 현재 파이프라인(`translate_io.py`/
`mes_translate_reinsert.py`/`pipeline.js`)에 하드 강제돼 있다. 그런데 그 원래 실험은
1차 시도(자유 길이, 파일 전체 토큰 수도 같이 6948→6935로 줄어듦 → 크래시)와 2차 시도
(블록별 고정, 파일 전체도 고정 → 정상)뿐이어서, "블록별 고정"과 "파일 전체 고정"이라는
두 가설을 분리 검증하지 못한 상태였다("남은 선택 항목 #4"). 사용자가 이번에 "파일
전체 토큰 수만 원본과 같게 유지하고 개별 블록끼리는 길이를 자유롭게 주고받을 수
있는지" 실기로 확인해보고 싶다고 요청.

**실험 설계**: `unpack/data/Script/dom1OP_0701_1.mes`(원본과 100% 동일, `unpack_origin`과
diff 없음 확인)를 대상으로, 대사 블록 4개를 원본과 다른 길이로 교체하되 두 쌍으로 나눠
각 쌍의 delta 합이 정확히 0이 되도록 함(파일 전체 토큰 수 6948 불변):

- **원거리 쌍** (파일 앞/뒤로 떨어져 있고 사이에 수십 개 블록이 그대로 남음):
  - block 51 `俺の格好なんか誰も気にしないって<485C>` (17토큰) → `됐어<485C>` (3토큰, Δ-14)
  - block 106 `(なんだよ、アテナ)<485C>` (11토큰) → `왜 그래, 아테나. 갑자기 부르니까
    놀랐잖아<485C>` (25토큰, Δ+14)
- **인접 쌍** (바로 옆 블록끼리, 사이에 다른 블록 없음):
  - block 109 `(フフッ.鼻の下伸びてるよ?)<485C>` (16토큰) → `(코털)<485C>` (5토큰, Δ-11)
  - block 110 `(これくらいで伸びるかよ)<485C>` (14토큰) → `설마 이 정도로 진짜로 늘어날
    리가 있겠어?<485C>` (25토큰, Δ+11)

기존 `build_file()`/`buildFileTokens()`의 "expected_len과 다르면 실패/패딩" 로직은 이
실험 자체가 그 제약을 깨는 게 목적이므로 의도적으로 우회 - 새 스크립트
`temp/test_variable_block_length.py`(신규, 1회성)가 직접 원본 슬라이스 사이에 인코딩된
번역 토큰을 그대로(패딩/트림 없이) 끼워 넣는다.

**검증 (스크립트 자체 assert, 전부 통과)**:
1. 파일 전체 토큰 수 == 원본(6948)과 완전히 동일 (delta 합계 0 확인).
2. 수정된 각 블록의 시작 토큰이 `font_map_kr.json` 기준 `kind in ('full','half')` -
   즉 유효한 글자 코드로 시작.
3. 수정된 각 블록이 여전히 `0x485C`로 끝남 - 종료 제어문자 무결성.
4. 파일 전체의 `0x6E5C 0x6E5C` 더블 터미네이터 개수가 원본과 동일하고, 각 터미네이터
   위치가 "원래 위치 + 그 지점까지의 누적 delta"와 정확히 일치 - 밀림에 의한 오프바이원
   없이 블록 경계가 구조적으로 올바르게 재배치됐음을 확인.

**빌드**: `temp/variable_len_test/unpack/`(원본 `unpack/`을 복사, 이 파일 하나만 교체)을
NitroPacker로 직접 pack (`./NitroPacker pack -p .../DAYSOFMEMORY.json -r
temp/variable_len_test/test_variable_length.nds`) - 에러 없이 16,789,504바이트 ROM 생성
완료. 원본 `unpack/`, 웹툴 프로젝트(`webtool/workspace/*`)는 전혀 건드리지 않음.

**남은 작업 (실기 확인 필요, 최종 판정 아직 안 남)**: 사용자가
`temp/variable_len_test/test_variable_length.nds`를 melonDS로 직접 실행해 확인 필요:
- 축소 블록(51, 109)에서 대사가 짧아진 채로 정상 표시/진행되는지.
- 확장 블록(106, 110)에서 대사가 늘어난 채로 정상 표시/진행되는지, 이후 멈추지 않는지.
- 원거리 쌍 사이/이후의 손대지 않은 다른 대사들도 정상적으로 이어지는지(밀림에 의한
  2차 오염 여부) - 특히 원거리 쌍은 사이에 놓인 수십 개 블록의 위치가 전부 밀렸다가
  다시 원위치로 돌아오므로, 블록 길이에 의존하는 어떤 오프셋 구조가 있다면 여기서
  드러날 가능성이 높음.
- 인접 쌍과 원거리 쌍의 결과가 서로 다른지(다르다면 "포인터가 인접 블록 개수/거리에
  민감" 같은 단서).

성공(정상 동작) 시: "블록별 고정"이 아니라 "파일 전체 고정"만으로 충분하다는 뜻이 되어
"남은 선택 항목 #4"가 사실상 해소되고, 향후 번역 시 블록별 길이를 자유롭게 조절할 수
있는 길이 열림. 실패(크래시/깨짐) 시: 현재의 "블록별 엄격 고정" 파이프라인이 최소
요구사항임이 재확인됨 - 이 경우 원거리 쌍/인접 쌍 중 어느 쪽이 먼저 깨지는지, 축소
블록과 확장 블록 중 어느 쪽 근처에서 문제가 나는지가 메커니즘 추정에 중요한 단서가 됨.

**실기 결과 1차 보고 (원거리 쌍, 2026-08-10, 사용자 melonDS 테스트)**:
- 축소 블록(51, `됐어<485C>`, 17→3토큰): **정상 통과**. 짧아진 대사가 문제 없이 표시되고
  다음으로 잘 넘어감.
- 확장 블록(106, 25토큰으로 확장, 11→25): **실패** - 무한 반복 증상 발생(대사/화면이
  계속 반복되며 진행 안 됨).

→ 파일 전체 토큰 수를 원본과 동일하게 유지해도(즉 "파일 전체 고정"만으로는 불충분),
**블록을 원본보다 늘리는 것 자체가 문제를 일으킨다**는 강한 증거. 반면 블록을 줄이는
것(패딩 없이 그대로 축소)은 이번 케이스에서는 문제 없었다 - 이는 기존 파이프라인이
"짧은 번역은 패딩해서 원래 길이를 맞춰야 한다"고 가정해온 것과 다른, 더 관대한
결과일 수 있음(단, 아직 1개 케이스만 확인된 것이라 일반화는 이름).

**추정**: 대사 블록 뒤에 이어지는 어떤 구조(다음 블록 헤더, 이벤트 커맨드 스트림, 혹은
박스 렌더러가 "이 블록은 N토큰"이라고 어딘가에서 참조하는 값)가 블록이 원본보다
길어지면 깨지는 것으로 보임 - 정확한 메커니즘은 여전히 미상(ARM9 디스어셈블리 필요).
"무한 반복"이라는 증상은 2026-08-05 1차 실기 테스트의 "게임이 멈춤"과는 다른 새로운
증상 패턴 - 실패 모드가 한 가지가 아닐 수 있음을 시사.

**다음 확인 필요**: 인접 쌍(109 축소/110 확장)에서도 같은 패턴(축소는 통과, 확장은
실패)이 재현되는지 - 재현되면 "블록 확장은 인접/원거리 상관없이 항상 불안전"이라는
결론을, 재현 안 되면(인접 쌍은 정상 동작) "확장 시 문제는 거리/사이 블록 개수에
의존"이라는 실마리를 얻을 수 있음.

**추가 확인**: 106번 블록의 무한반복 때문에 그 뒤로 진행이 안 되어, 같은 롬 안에 있던
인접 쌍(109/110)은 아직 테스트하지 못함(스토리 순서상 106번을 지나야 109/110에 도달).
→ 인접 쌍만 별도로 격리한 테스트 롬을 새로 만들어 확인 필요.

**2차 테스트 롬 (인접 쌍, 파일 앞쪽으로 재배치, 2026-08-10)**: 106번이 스토리 진행을
막아 인접 쌍(원래 109/110)에 도달할 수 없었으므로, 같은 축소/확장 패턴을 51/106보다
훨씬 앞쪽인 block 33/34로 옮겨 새 테스트 롬을 만듦(51/106은 이번엔 원문 그대로 유지):
- block 33 `早く行かないと遅刻だよっ!!<485C>` (15토큰) → `빨리 가자<485C>` (6토큰, Δ-9)
- block 34 `…….あぁっ!?<485C>` (9토큰) → `……어, 잠깐 진짜 놀랐잖아!?<485C>` (18토큰, Δ+9)

파일 전체 토큰 수 6948 불변, 블록 시작/종료 제어문자 무결성, 터미네이터 위치 정합성
모두 검증 통과. `temp/variable_len_test/test_variable_length_adjacent_early.nds`로 빌드
완료 - 사용자 melonDS 확인 대기 중.

**실기 결과 2차 보고 (인접 쌍, block 33/34, 2026-08-10)**:
- 축소 블록(33, `빨리 가자<485C>`, 15→6토큰): **정상 통과** - 34번까지 도달함.
- 확장 블록(34, 18토큰으로 확장, 9→18): **실패** - 원거리 쌍의 106번과 동일한 무한반복
  증상.

**결론 (2회 재현으로 신뢰도 높음)**: 인접/원거리 여부와 무관하게 **블록을 원본 토큰
수보다 길게 확장하면 항상 무한반복으로 실패**한다. 반대로 축소(패딩 없이 그대로 줄임)는
지금까지 3개 케이스(51, 33 - 그리고 106/34 확장 실패 전까지는 정상 진행) 모두 문제
없었다. 즉 "블록별 정확히 고정" 또는 "파일 전체 고정"이 아니라, **"블록은 원본보다
길어지면 안 된다(짧아지는 건 허용될 수 있음)"**가 진짜 제약에 더 가까워 보인다.

다만 이번 두 테스트 모두 축소 블록과 확장 블록을 쌍으로 묶어 파일 전체 토큰 수를
원본과 동일하게 유지했기 때문에, "축소만 하고 보정용 확장 없이 파일 전체 길이 자체가
줄어드는 경우"(2026-08-05 1차 실기 테스트의 실패 시나리오와 동일 조건)는 아직 이
2026-08-10 실험에서 직접 재현/반증되지 않음 - 다음 확인 대상.

**3차 테스트 롬 (축소 단독, 보정 없음, 파일 전체 길이 실제로 감소, 2026-08-10)**: 확장이
문제라는 두 번의 재현과 별개로, "보정 없는 축소 단독"이 안전한지도 분리 검증 필요 -
2026-08-05 1차 실기 실패가 정확히 이 조건(패딩 없는 자연 길이 번역 다수 + 파일 전체
길이 감소)이었기 때문. block 33만 축소(`早く行かないと遅刻だよっ!!<485C>` 15→6토큰,
Δ-9)하고 다른 블록은 전혀 건드리지 않아 파일 전체 토큰 수가 6948→6939로 실제 감소한
테스트 롬을 `temp/variable_len_test/test_shrink_only_no_padding.nds`로 빌드 완료 -
사용자 melonDS 확인 대기 중.

**실기 결과 3차 보고 (축소 단독, 보정 없음, 2026-08-10)**: **실패** - 무한반복, 진행 안 됨.
이전 두 테스트에서 block 51/33 축소는 "보정용 확장과 짝지어 파일 전체 길이가 보존된
상태"에서는 정상 통과했는데, 똑같은 block 33 축소를 보정 없이 단독으로 넣으니(파일
전체가 6948→6939로 실제 감소) 실패함. 즉 "확장만 위험하고 축소는 항상 안전하다"는
가설은 기각됨 - **축소도 파일 전체 길이가 그대로 보존되지 않으면 위험할 수 있음**.

정확히 어느 지점에서 반복이 걸리는지(block 33 자체를 표시할 때부터인지, 아니면 그
다음 등장하는 대사/장면부터인지) 추가 확인 필요 - 이전 두 성공 사례와 유일한 차이가
"이후 내용의 파일 내 절대 위치가 영구적으로 9토큰씩 당겨졌다"는 점이므로, 실패 지점이
정확히 어디인지에 따라 "특정 절대 오프셋을 참조하는 구조가 있다"는 가설을 검증할 수
있음.

**3차 실패 지점 특정**: 사용자 확인 결과, 33번("빨리 가자")은 정상 출력됨. 그 직후
34번(손대지 않은 원문 블록)에서 스피커가 이상해지고 대사가 깨진 채 무한반복 - 이는
2026-08-05 1차 실패("스피커 이름이 깨지고 그 대사에서 게임이 멈춤")와 동일한 증상 패턴.

→ **33번 자체의 문제가 아니라, 33번이 짧아지면서 그 뒤에 있는 손대지 않은 34번의
위치/내용이 깨졌다는 뜻.** 34번의 토큰 자체는 원본 그대로 복사됐으므로(파일 내용은
멀쩡), 게임 엔진이 34번의 시작 위치를 "동적 스캔"이 아니라 "33번은 정확히 N토큰"이라는
전제하에 계산된 고정 오프셋으로 찾고 있을 가능성이 높음.

**그런데 모순점**: 1차 원거리 테스트(block 51 축소)에서는 51번 바로 다음인 52번~105번
사이 블록들이 별문제 없이 넘어갔고(사용자가 문제 언급 없음), 51번에서 한참 떨어진 106번
(의도적으로 확장한 블록)에서만 실패했었다. "블록 길이가 바뀌면 바로 다음 블록이 무조건
깨진다"는 게 참이라면 52번에서도 즉시 깨졌어야 하는데 그러지 않았음.

**작업가설**: 모든 블록 경계가 다 외부에서 오프셋으로 참조되는 게 아니라, 일부 "이벤트/장면
전환 지점" 블록만 파일 내 절대 위치로 참조되고, 그 사이의 일반 대사 블록들은 순차/동적으로
스캔되어 앞쪽이 밀려도 괜찮을 수 있음. 34번이 마침 그런 참조 지점이고 52~105번은 아닌 경우
지금까지의 모든 결과(51 다음 정상, 106 실패 / 33 다음 34에서 실패)가 모순 없이 설명됨.

**다음 확인 필요**: 이 가설이 맞다면, 33번이 아니라 "안전권"으로 보이는 중간 지점(예:
60번대) 블록 하나를 단독으로 축소했을 때 그 바로 다음 블록은 안 깨지고 계속 진행돼야 함.
반대로 34번 자체가 "특수 지점"이라 어떤 축소를 앞에 둬도 깨진다면 그건 34번 고유의 문제.

**4차 테스트 롬 (2026-08-10)**: `temp/variable_len_test/test_shrink40_only.nds` 빌드 완료.
block 40("もぅ、また遅くまで遊んでたんで…" 61토큰, 2페이지 대사)만 단독으로 "얼른
나가자<485C>"(7토큰, delta -54)로 축소, 보정 없음. 파일 전체 6948->6894.
33/34, 51/106과 겹치지 않는 중간 지점을 골라 "일부 지점만 오프셋 참조" 가설을 검증하는
것이 목적 - block 40 바로 다음인 41번("わりぃ!いま出るから!")이 34번처럼 즉시
깨지는지, 51 다음 52번처럼 멀쩡히 넘어가는지 확인 필요.

**실기 결과 4차 보고 (block 40 단독 축소, 2026-08-10)**: **성공** - 축소된 대사("얼른
나가자")도 정상 출력되고 그 다음 41번("わりぃ!いま出るから!")까지 멀쩡히 넘어감.

이로써 "일부 특정 지점만 파일 내 절대 오프셋으로 참조되고, 그 사이 일반 대사는 순차
스캔이라 밀림에 안전하다"는 가설이 강하게 뒷받침됨:
- block 40(-54, 대폭 축소) 단독 → 41번 정상 (일반 지점)
- block 51(-14) 단독 → 52~105번 정상, 106번(확장)에서만 실패 (일반 지점들 사이에 특수
  지점 하나 존재)
- block 33(-9, 소폭 축소) 단독 → 34번에서 즉시 실패 (34번이 특수 지점)

즉 축소 자체는 위험하지 않고, "특정 특수 지점 블록 앞에서 길이가 바뀌는 것"만 위험함.
34번이 왜 특수한지 게임 실행 없이 데이터만으로 단서를 찾아볼 것.

**34번이 왜 특수한지 데이터 조사 (게임 실행 없이)**: block 33/34/35/40/41/50/51/52의
헤더 바이트(스피커 ID)와 터미네이터 직후 4개 필드(추정: 음성/애니메이션 파라미터,
0x00 0x00 [XX] [YY] 패턴)를 비교했으나 뚜렷한 차이를 찾지 못함:
- 헤더 바이트: 34번(0x0002)과 41번(0x0002)이 동일 - 스피커 ID 자체는 구분 요인 아님.
- 터미네이터 뒤 4필드 값(0x20,0x12 등)이 27번/33번/53번/54번 등 여러 블록에서 완전히
  똑같이 반복됨 - 누적 오프셋 카운터가 아니라 재사용되는 파라미터값으로 보임(절대
  위치 참조의 직접적 증거는 아님).
- 특이사항: block 35("ガバッ")와 39("ガチャッ") 둘 다 헤더 바이트가 0xFFFF(다른 모든
  블록과 다른 특수값) - 의성어/효과음성 대사로 추정. 34번 자체의 헤더는 0xFFFF가
  아니므로 이것만으로 34번의 특수성을 설명하지는 못함.

**결론**: ARM9 디스어셈블리 없이 .mes 파일 데이터만으로는 어떤 블록이 "특수 지점"인지
구분할 신호를 찾지 못함. 즉 특정 블록이 안전한지 사전에 예측할 수 없고, 실기 테스트로
하나하나 확인하는 수밖에 없는데 이는 ~38,495개 전체 블록 규모에서는 비현실적임.

**실용적 결론**: 개별 대사가 "대부분" 자유롭게 줄어도 되는 것처럼 보이지만(40, 51번
성공), 일부 특수 지점(34번 같은)에서는 여전히 위험하고 이를 미리 구분할 방법이 없으므로,
**현재 프로덕션 파이프라인의 "모든 블록을 원본과 정확히 같은 토큰 수로 고정" 규칙을
유지하는 것이 유일하게 검증된 안전한 방식**이다. Task E의 애초 목표("블록 길이를 자유롭게
조정해도 되는가")는 안전하게 일반화할 수 없다는 결론.

**ARM9 정찰 결과 (2026-08-10)**: `unpack/arm9.bin`(166KB, RAM베이스 0x02000000)과
overlay 4개(`main_0000~0003.bin`, 각 2.5K/18K/41K/193K)에서 0x6E5C 리터럴을 바이트
패턴(`5C 6E 00 00`, 리틀엔디안 32비트 0)으로 검색 - arm9.bin에 7곳, overlay에 2곳
(main_0001, main_0002), 0x485C는 main_0003 overlay에 1곳뿐. 각 위치는 전부 함수 시작
직전의 "리터럴 풀 상수"로 다음 함수 프롤로그(`push {r3-r7,lr}` 등)가 바로 이어지는
전형적인 ARM 컴파일러 코드젠 패턴 - 즉 이 상수를 참조하는 함수가 arm9.bin에만 최소
7개나 흩어져 있음(단일 함수가 아님).

capstone(ARM 모드)으로 첫 번째 위치(0x0201db54)를 디스어셈블한 결과, 이 함수는 대사
"진행" 로직이 아니라 **타일/폰트 렌더링 루틴**으로 보임(0x800 오프셋, <<6/<<12 시프트
등 `pipeline.js`의 타일 렌더 코드와 동일한 패턴의 타일 주소 계산, `VRAM` 쓰기로 추정되는
`bl` 호출들). 즉 0x6E5C가 "블록 종료 판별"이 아니라 "특수 글리프 코드"로도 취급되는
곳일 수 있어, 정찰 1회만으로는 "블록 진행/이벤트 트리거" 로직을 바로 특정하지 못함.

**결론**: 상수 기반 grep으로 후보 지점은 빠르게 좁혔지만(7~9곳), 그 중 실제 대사 진행/
이벤트 트리거 로직이 어디인지 가려내려면 나머지 6곳도 각각 디스어셈블해서 맥락을
봐야 하고, 이후 "34번만 특수한 이유"(오프셋 테이블/이벤트 트리거 여부)를 찾으려면
그보다 더 깊은 분석이나 동적 디버깅이 필요 - 애초 예상대로 "완전 규명"은 정찰 수준을
넘어서는 훨씬 큰 작업임이 확인됨.

**ARM9 심층 분석 (2026-08-10, 계속)**: 0x6E5C를 실제로 참조하는 명령어를 리터럴 풀
역참조로 정확히 찾아냄(단순 "함수 직전 상수"가 아니라 실제 `ldr rX,[pc,#imm]`이
가리키는 진짜 사용처):

- **`0x0201dd98` 함수**: 파라미터로 받은 코드값이 "16개 슬롯 테이블" 안에 있는지 검사하는
  범용 조회 함수 - 0x6E5C를 "테이블 끝 표시(sentinel)"로 사용. 대사 진행과 직접 관련
  없어 보이는 범용 유틸리티로 추정.
- **`0x0201df30` 함수 (★ 유력)**: `mes_codec.find_dialogue_blocks()`가 하는 일과 동일한
  로직 발견 - `ldrh r3,[r0]; cmp r3, 0x6E5C; bne 끝; (연속으로 몇 개나 이어지는지 카운트);
  r0 += count*2` 형태. 즉 **토큰 스트림 포인터(r0)를 전달받아 연속된 0x6E5C 개수를 세고
  그만큼 포인터를 전진시켜 반환하는, "터미네이터 런(run) 건너뛰기" 저수준 프리미티브**.
  `r1` 파라미터가 0이면 "1개 이상 연속" 모드, 0이 아니면 더 엄격한(3개 이상 확인하는)
  별도 분기 - 페이지 구분자(단일/이중 6E5C)와 완전한 블록 종료자(그 이상)를 구분하는
  로직일 가능성.
- 이 함수는 `0x0201dde8`에 있는 더 큰 함수(바이트 4개씩 읽어 u16 좌표/파라미터 구조체를
  채우는 코드, 아마 위치/이동 제어 코드 파싱)에서 두 번 호출됨(`bl`).

**막힌 지점**: `0x0201dde8`을 호출하는 곳을 찾으려 했으나 arm9.bin 전체에서 직접
`bl 0x1dde8` 형태의 호출이 없었고, 포인터 테이블(함수 포인터 배열)에도 해당 주소가
없음 - 즉 오버레이(main_0000~3.bin, 자체 RAM 베이스 주소가 있음)에서 호출되거나,
간접 호출(레지스터 기반 `blx`)로 연결되는 것으로 추정됨. 오버레이의 정확한 RAM
로드 주소가 `unpack/DAYSOFMEMORY.json`에는 없어(NitroPacker가 별도 관리) 지금
당장은 오버레이 쪽 상호참조를 못 함 - 이 이상 파려면 오버레이 RAM 주소 확보(원본
ROM의 y9.bin 오버레이 테이블 필요) + 간접 호출 추적이 필요해 상당한 추가 작업.

**중간 결론**: 저수준 "터미네이터 런 건너뛰기" 프리미티브까지는 찾았고 이는 우리
가정(연속 0x6E5C가 블록 경계)이 엔진 구현과 실제로 일치함을 확인해주지만, "34번
블록만 왜 특수한가"를 설명할 상위 레벨 이벤트/오프셋 참조 로직에는 아직 도달하지
못함. 여기서부터는 오버레이 RAM 주소 확보, 간접 호출 추적, 혹은 melonDS 동적
디버깅이 필요한 더 큰 작업 - 애초 예상한 "며칠 단위, 불확실"이라는 평가가 그대로
유효함.

## Task E 종료 (2026-08-10): 대사 블록 길이 고정 제약 재검증 - 최종 결론

4차례의 실기 테스트(원거리 51/106, 인접 33/34, 33 단독 축소, 40 단독 축소)와 ARM9
정찰까지 거친 끝에 다음과 같이 결론 내리고 이번 조사를 마무리한다:

- **확인된 사실**: 대부분의 대사 블록은 보정(다른 블록과 델타 상쇄) 없이 자유롭게
  축소해도 문제없이 동작한다(block 40 -54, block 51 -14 단독 축소 모두 성공). 즉
  "모든 블록은 원본과 정확히 같은 토큰 수를 가져야 한다"는 기존 가정은 필요조건이
  아니라 충분조건이었다.
- **확인된 위험**: 다만 일부 특정 지점(block 34, block 106처럼 스토리/연출상 특별한
  타이밍을 가진 지점) 앞에서 길이가 바뀌면 그 지점이 깨진다(스피커/대사 오염,
  무한반복). ARM9 정찰로 저수준 토큰 파싱 로직(터미네이터 런 스캔, `0x0201df30`)까지는
  확인했으나, 어떤 블록이 "특수 지점"인지 미리 구분할 수 있는 상위 레벨 메커니즘(이벤트
  트리거/오프셋 테이블)은 오버레이 RAM 주소·간접 호출 추적이 필요해 이번 조사 범위에서
  규명하지 못함.
- **실용적 결정**: 어떤 블록이 안전한지 사전 판별 불가 + 전체 블록 수(~38,495개)를
  감안하면, **기존 프로덕션 파이프라인의 "모든 블록을 원본과 정확히 같은 토큰 수로
  고정" 규칙을 그대로 유지**하기로 함(`translate_io.py`/`mes_translate_reinsert.py`/
  `pipeline.js`/`translateIo.js` **코드 변경 없음** - 이 조사 전체가 계획대로 프로덕션
  로직을 건드리지 않는 독립 실험으로 진행됐음).
- **남긴 자료**: `temp/test_variable_block_length.py`(1~4차 테스트 스크립트, 최종 상태는
  4차 - block 40 단독 축소)와 `temp/variable_len_test/`의 빌드된 테스트 롬들은 조사
  기록으로 `temp/`에 그대로 둠(CLAUDE.md 규칙 3에 따라 최종 산출물로 옮기지 않음).
- 언젠가 이 제약을 완전히 풀고 싶다면, ARM9 오버레이 RAM 주소 확보 + 함수 포인터/간접
  호출 추적, 혹은 melonDS 동적 디버깅으로 이어가야 함 - "남은 선택 항목"으로 계속 보류.

## "종결문자 뒤 0x00 패딩으로 오프셋 유지" 검토 (2026-08-10)

Task E 종료 직후 사용자 질문: "대사 축소할 때 종결문자 뒤에는 0x00으로 패딩해서
오프셋을 유지하는 게 가능한가?" — 즉 현재처럼 대사 내용 자체를(0x00CA 반각 스페이스로)
채우는 대신, 화면에 안 보이는 0x0000을 종결 마커 뒤쪽 어딘가에 채워서 이후 절대
오프셋을 유지할 수 있는지에 대한 질문. 코드 수정 없이 코퍼스 데이터만으로 조사.

- **`font_map_full.json`상 0x0000**: `{'kind': 'ctrl', 'count': 2306, 'real_tile': None}`
  — 렌더링되는 실제 글리프가 아닌 제어 코드로 이미 분류돼 있음.
- **코퍼스 전체(`unpack/data/Script/*.mes`, 실제 대사 블록 38,154개) 스캔 결과**: 0x0000이
  블록 안에 등장하는 경우가 696블록, 총 7,396회 있음 — 즉 미지의 영역은 아님.
- **하지만 위치가 전부 한쪽으로 쏠려 있음**: 7,396회 전부 블록 **맨 앞쪽**, 실제 표시
  텍스트(한자/가나 글리프)가 시작되기 *이전의* 숫자형 파라미터 런 안에서만 등장함
  (예: `dom1Athena_L08.mes` 블록 `[0x134, 0x0, 0x0, 0x0, 0x0, 0x0, 0x20, 0x12, 0x2, ...실제
  글리프들..., 0x485c]`, `dom1Athena_O0731_3.mes` 블록 `[0x60c, 0x0, 0x0, 0x0, 0x0, 0x3,
  0x7, 0x3, 0x2, 0x2e0, ...]`). 0x1, 0x2, 0x3, 0x11, 0x6c, 0x18, 0x15 같은 다른 작은
  정수들과 섞여 나오는 전형적인 "좌표/카운트/플래그 파라미터" 패턴 — Task E의 ARM9
  정찰에서 찾은 `0x0201dde8`(좌표/파라미터 파싱 함수, 내부에서 터미네이터 스캔
  `0x0201df30`을 호출)이 다루는 것과 정확히 같은 종류의 데이터로 보임.
  - 0x0000이 **0x485C(페이지 넘김) 직후**에 등장한 사례: 0건.
  - 0x0000이 **실제 표시 텍스트 중간**(파라미터 런이 끝나고 글리프가 시작된 뒤)에
    등장한 사례: 0건.
  - 0x0000이 **블록의 첫 토큰**으로 등장한 사례: 0건 (항상 파라미터 런의 두 번째
    토큰부터 시작).
- **결론**: 0x0000은 코퍼스 전역에서 예외 없이 "파라미터/좌표 파싱 로직이 읽는 정수
  0 값"으로만 쓰이고, "텍스트 렌더링 루프가 무시하고 지나가는 투명 필러 문자"로 쓰인
  전례가 전혀 없다. 즉 사용자가 제안한 방식은 두 갈래로 나눠 봐도 둘 다 기존 근거가
  없음:
  1. **0x485C 뒤, 더블터미네이터(0x6E5C 0x6E5C) 앞**에 0x00을 넣는 경우 — 이미
     `pipeline.js`/`mes_translate_reinsert.py` 주석에 남긴 대로, 문제는 "패딩 값이
     보이는 글자냐 아니냐"가 아니라 "0x485C 뒤에 뭐라도 더 있으면 그 자체가 새로운
     페이지로 취급돼 빈 화면을 한 번 더 넘겨야 하는" 구조적 동작으로 보임(반각
     스페이스로 재현했던 "대사가 끝나도 빈칸이 나오고 안 넘어간다" 버그). 0x0000으로
     바꾼다고 이 구조적 동작이 달라진다는 근거는 코퍼스에 없다 — 오히려 0x0000이
     텍스트 스트림 안에서 파싱될 때 어떻게 처리되는지(무시/미정의 동작/행)에 대한
     선례 자체가 전무해 더 위험할 수 있다.
  2. **더블터미네이터 뒤, 다음 블록 헤더 바이트 앞**(블록 사이 갭)에 0x00을 추가로
     끼워 넣는 경우 — 이 갭은 이미 고정 폭 구조(관찰된 4-필드 파라미터 + 헤더 바이트)를
     담고 있는 것으로 보이고, 이 값들이 "터미네이터 뒤 상대 위치로 읽힌다"는 가정 하에
     여기 토큰을 추가하면 그 구조 자체가 밀려 34번 블록류의 오염과 동일한 실패
     양상을 일으킬 가능성이 높다. 검증 없이는 이 갭이 "가변 길이/자기서술적"인지
     "고정 오프셋"인지 알 수 없음 — Task E에서 규명하지 못한 지점과 동일.
- **실용적 결론**: 코퍼스 증거만으로는 "0x00 사후 패딩"을 뒷받침할 근거가 없고, 오히려
  기존 실패 사례들이 시사하는 "0x485C 뒤엔 아무것도 두면 안 된다"는 규칙과 상충한다.
  실기로 검증하지 않는 이상 이 방향은 채택하지 않는다. 굳이 확인하려면 가장 저렴한
  실험은 33→34 조합을 재사용해 33번 블록의 패딩 값만 0x00CA 대신 0x0000으로 바꿔
  실기 테스트하는 것(구조 위치는 그대로, 값만 교체) — 이번 세션에서는 진행하지 않음.

**5차 테스트 롬 (2026-08-10)**: 위에서 제안한 실험을 바로 진행. `temp/test_variable_block_length.py`를
5차로 갱신 — 2차 테스트에서 이미 "34번을 깨뜨림"이 확인된 block 33 축소(`早く行かないと
遅刻だよっ!!<485C>` 15토큰 → `빨리 가자<485C>` 6토큰)를 재사용하되, 부족한 9토큰을
`PAD_TOKEN = 0x0000`으로 채워(프로덕션과 동일하게 트레일링 0x485C 바로 앞에 삽입) 블록
33 길이를 원본과 정확히 같은 15토큰으로 되돌림. 파일 전체 토큰 수 6948 그대로, 델타 0,
34번을 포함한 모든 후속 오프셋 불변 — 유일한 변수는 "패딩 값이 0x00CA가 아니라 0x0000"
뿐. (버그 수정: 터미네이터 위치 검증 로직이 패딩 반영 전 델타를 재계산해 assert가 실패했음
→ 실제 패딩 적용 후 델타를 `actual_delta_by_index`에 저장해 검증에 재사용하도록 수정.)
`temp/variable_len_test/test_shrink33_pad0000.nds` 빌드 완료 (16,789,504 bytes) — 사용자
melonDS 확인 대기 중. 성공하면 "0x00CA 대신 0x0000도 패딩 값으로 안전하다"가 확인되고,
34번이 여전히 깨지면(혹은 다른 증상이면) "0x0000이 텍스트 렌더링 스트림 안에서 문제를
일으킨다"는 이전 코퍼스 기반 우려가 실기로도 확인되는 것.

**실기 결과 5차 보고 (2026-08-10)**: 스크린샷 확인 결과 절반의 성공/절반의 예상대로.
- **오프셋 보존은 성공**: "빨리 가자" 표시 후 34번을 포함한 뒤쪽 대사가 전부 정상 출력됨
  (사용자: "뒤에 대사는 잘 나와"). 즉 "블록 길이를 원본과 정확히 맞추기만 하면 34번은
  안 깨진다"가 패딩 값과 무관하게 재확인됨 - 프로덕션의 "블록별 정확한 토큰 수 고정"
  규칙이 왜 필요충분조건인지 다시 한번 실기로 검증된 셈.
  ***주의: 이 케이스는 "축소 후 보정 없음"이 아니라 "축소 후 원본 길이로 패딩 복원"
  이므로, Task E에서 확인했던 "특정 지점 앞에서 무보정 축소는 위험하다"는 결론과
  모순되지 않는다 - 오히려 그 결론을 한 번 더 뒷받침한다.***
- **하지만 0x0000은 "안 보이는 필러"가 아니었다**: 스크린샷에서 "빨리 가자" 뒤에
  쉼표/틱 모양 기호(`、`류)가 정확히 9번(패딩 토큰 개수와 일치) 반복 표시됨. 즉
  `font_map_full.json`에서 `kind: 'ctrl', real_tile: None`으로 분류된 것은 "우리 분석
  도구가 이 코드에 문자 매핑을 확정하지 못했다"는 뜻일 뿐, 게임 엔진이 실제로 이
  코드를 텍스트 스트림에서 만났을 때 "아무것도 안 그린다"는 보장은 전혀 아니었다 -
  엔진은 코드값 → 타일 인덱스 변환 공식을 그대로 적용해 뭔가를 그린다.
- **결론**: 사용자가 원래 물었던 "종결문자 뒤/안 보이는 0x00 패딩으로 오프셋 유지"라는
  목적 자체는 이 방식으로 달성되지 않는다 - 오프셋은 지켜지지만 화면에 이상한 반복
  기호가 남는다. 프로덕션이 `0x00CA`(반각 스페이스, 실제로 빈 타일을 가리키는 값으로
  확인된 코드)를 패딩 값으로 쓰는 게 우연이 아니라 필요한 선택이었음이 재확인됨 -
  **패딩 값은 반드시 "진짜로 빈 타일에 매핑된 코드"여야 하고, `0x0000`은 그 조건을
  만족하지 않는다.** 프로덕션 코드(`translateIo.js`/`translate_io.py`의
  `HALF_SPACE_TOKEN`) 변경 없음 - 오히려 현재 선택이 옳았음을 확인.

## 6차 테스트: 0x485C **뒤**에 0x0000 패딩 (2026-08-10)

사용자가 실제로 물었던 위치는 5차와 달랐음 - "0x485C 앞"이 아니라 "0x485C **뒤**,
더블터미네이터(0x6E5C 0x6E5C) 앞"에 0x00을 넣자는 것. 이 자리는 기존
`pipeline.js`/`mes_translate_reinsert.py` 주석에 남아있는 "패딩이 0x485C 뒤로 가면
대사가 끝나도 빈 페이지가 하나 더 나오는" 버그가 처음 재현됐던 바로 그 위치인데, 그때
쓴 패딩 값은 `0x00CA`(보이는 반각 스페이스)였다 - 값을 `0x0000`으로 바꾸면 "위치
자체의 문제"인지 "그 자리에 보이는 글자가 있어서 생기는 문제"인지 분리해서 확인
가능.

- `temp/test_variable_block_length.py`에 `PAD_AFTER_PAGE_TURN = True` 플래그 추가,
  block 33 재사용 - 새 블록 토큰 배열을
  `[...빨리 가자<485C> 6토큰..., 0x0000 x 9]`로 구성(마지막 토큰이 0x485C가 아니라
  0x0000). 블록 길이는 여전히 원본과 같은 15토큰(파일 전체 델타 0, 34번 오프셋 불변).
- `temp/variable_len_test/test_shrink33_pad0000_after485c.nds` 빌드 완료
  (16,789,504 bytes) - 사용자 melonDS 확인 대기 중.

**실기 결과 6차 보고 (2026-08-10)**: 스크린샷 확인 결과, "빨리 가자" 표시 후 5차와
똑같은 반복 기호(`、`류, 9개)로 채워진 **추가 페이지가 한 번 더 나타남** - 사용자
확인: "잘 넘어가긴 하는데 이게 또 나오네"(진행 자체는 안 멎지만 그 페이지가 또
나온다). 즉:
- 무한반복/스피커 오염 같은 치명적 corruption은 없음 - 오프셋 보존 자체는 여전히
  유효(34번으로 계속 진행됨).
- 하지만 **"0x485C 뒤에 뭔가 있으면 빈 페이지가 하나 더 나온다"는 버그는 패딩 값과
  전혀 무관하게 재현됨** - 0x00CA(보이는 반각 스페이스)든 0x0000(제어 코드)이든
  0x485C 뒤에 위치하기만 하면 엔진이 "표시할 내용이 더 있다 = 새 페이지"로 취급해
  한 번 더 넘겨야 하는 것으로 보임. 즉 이 버그는 **값의 문제가 아니라 순수하게
  위치(0x485C 이후냐 이전이냐)의 문제**임이 실기로 확정됨.
- **최종 결론**: 사용자가 제안한 "종결문자(0x485C) 뒤에 0x00 패딩으로 오프셋 유지"
  기법은 작동은 하지만(오프셋 보존/진행 정상) 반드시 원치 않는 빈 페이지 하나가
  추가로 붙는 부작용이 있고, 이는 패딩 값을 어떻게 바꿔도(0x00CA, 0x0000 둘 다 확인)
  해결되지 않는다. 따라서 이 방향은 채택 불가 - 프로덕션이 이미 하고 있는 대로
  **패딩은 반드시 0x485C 앞에 넣어야 한다**는 규칙이 5차/6차 실기 테스트로 재확인됨.
  `translateIo.js`/`translate_io.py`/`pipeline.js`/`mes_translate_reinsert.py`
  코드 변경 없음(현재 방식이 이미 유일하게 검증된 정답).

## 7차 테스트: 프로덕션과 동일한 조합(0x485C 앞 + 0x00CA) 최종 확인 (2026-08-10)

5차 테스트에서 "0x485C **앞**에 패딩하면 빈 페이지가 안 생긴다"는 게 이미 위치만
놓고 보면 증명됐다(단, 그때 쓴 값 0x0000이 시각적 쓰레기로 보였을 뿐). 6차 테스트는
반대로 "0x485C **뒤**는 값과 무관하게 빈 페이지가 생긴다"를 증명했다. 이 둘을
합치면: 빈 페이지 버그는 순수하게 **위치**의 문제이고, 값의 문제는 별개(0x0000만
안 보이는 필러로 부적합)라는 결론이 나온다.

이 결론이 맞다면, 프로덕션이 이미 채택한 조합(`PAGE_TURN_TOKEN=0x485C` **앞**에
`HALF_SPACE_TOKEN=0x00CA`로 패딩 - `pipeline.js`/`mes_translate_reinsert.py`)은
빈 페이지도 시각적 쓰레기도 없이 깨끗하게 동작해야 한다. 그런데 이 조합 자체가
도입된 이래(2026-08-10 "대사가 끝나도 빈칸이 나오고 안 넘어간다" 버그 리포트에 대한
실험적 수정) 한 번도 melonDS로 직접 검증된 적이 없었다("(2)는 통계적 근거에 기반한
실험적 수정이라 실기 확인 전까지는 확정 아님"으로 남아있던 부분).

7차 테스트는 5차/6차와 동일하게 block 33 축소(`早く行かないと遅刻だよっ!!<485C>`
15토큰 -> `빨리 가자<485C>` 6토큰)를 재사용하되, 부족한 9토큰을 `tio.HALF_SPACE_TOKEN`
(0x00CA)으로 채우고 위치도 프로덕션과 동일하게 트레일링 0x485C **바로 앞**에 넣는다
(`temp/test_variable_block_length.py`: `PAD_TOKEN = tio.HALF_SPACE_TOKEN`,
`PAD_AFTER_PAGE_TURN = False`). 즉 이건 실제로 웹툴 CSV 파이프라인이 이 블록/번역
쌍에 대해 만들어낼 결과물과 바이트 단위로 동일하다.

빌드: `temp/variable_len_test/test_shrink33_pad00ca_before485c.nds` (16,789,504
bytes), 스크립트 자체 assert(파일 전체 토큰 수 불변, 터미네이터 위치 델타 정확히
반영) 전부 통과.

**남은 작업**: 사용자가 melonDS로 이 ROM을 실행해 (1) block 33이 "빨리 가자"로
짧게 표시되고 시각적 쓰레기 없이 정상 진행되는지, (2) block 34 이하가 정상인지(오프셋
보존), (3) 빈 페이지가 전혀 발생하지 않는지 확인 필요. 이 세 가지가 모두 확인되면
"패딩은 0x485C 앞에 0x00CA로" 방식이 이 특정 케이스에 대해서는 완전히 검증되는
것이며, 오랫동안 미확정 상태였던 빈 페이지 수정 자체의 실기 검증도 함께 닫힌다.

## 7차 실기 결과: 새로운 버그 발견 - 반각 스페이스 타일 제로클리어 누락 (2026-08-10)

사용자가 7차 테스트 롬을 melonDS로 확인한 결과, 빈 페이지는 없었지만 "빨리 가자"
텍스트에 0x00CA(HALF_SPACE_TOKEN)가 들어간 자리마다(단어 사이 공백 1개 + 패딩
9개, 총 10곳) 전부 이상한 반복 기호가 시각적으로 나타났다. 이는 패딩 위치/값
전략의 문제가 아니라 **완전히 별개의, 지금까지 몰랐던 폰트 제로클리어 버그**로
확인됐다.

**원인 분석**:
- `font_map_full.json`의 `formula` 필드: half-width 글자는
  `tile = 2*low - 14` 이고 **"vertical 2-tile stack" (세로 방향 2타일,
  8x16px)** - 즉 base 타일과 base+32(한 행 아래) 두 개를 쓴다. full-width
  글자는 2x2 4타일(base, base+1, base+32, base+33)을 쓴다.
- `0x00CA`(half, real_tile=390)는 `0x0606`(full, real_tile=390, "빈 라벨 -
  실사용 없음"으로 알려진 코드)과 물리적 타일을 공유한다 - 0x0606이 full이므로
  4타일(390,391,422,423) 전부를 차지하고, 0x00CA는 그중 세로 왼쪽 열
  (390, 422)만 실제로 사용.
- 그런데 `analysis/apply_font_art.py`(197~198행)와
  `webtool/server/lib/pipeline.js`(480~482행)의 "빈 타일 제로클리어" 로직은
  `HALF_SPACE_TILE`에 대해 `(base, base+1)` = (390, 391)만 지웠다 - **세로
  스택이 아니라 가로 방향으로 지운 것**. 즉 정작 0x00CA 자신이 실제로 쓰는
  타일인 422(세로 아래쪽)는 한 번도 지워진 적이 없었다.
- 바이트 덤프로 확인(`unpack_origin/data/Font_DOM.nbfc`): tile 390/391은
  제로클리어로 이미 비어있었지만, tile 422에는 원본 일본어 폰트의 잉크(세로
  획 일부)가 그대로 남아있었음. 이게 화면에 이상한 기호로 보인 정체.
- **이 버그는 이번 실험 전용 코드가 아니라 프로덕션 코드 자체의 버그** -
  지금까지 웹툴로 뽑은 모든 최종 빌드에서, 번역문에 공백(' ')이 하나라도
  들어가거나 패딩이 들어간 모든 자리마다 이 잔상이 나타났을 것으로 추정.
  지금까지 실기 테스트에서 문장에 공백을 쓴 사례가 적었거나 눈에 잘 안
  띄었을 가능성.

**수정**: `analysis/apply_font_art.py`와 `webtool/server/lib/pipeline.js`
양쪽의 반각 스페이스 제로클리어 루프를 풀사이즈 스페이스와 동일하게
4타일(base, base+1, base+32, base+33) 전부 지우도록 수정 완료 - 0x0606이
어차피 실사용 없는 full 코드이므로 4타일 전부 지워도 부작용 없음.
`analysis/apply_font_art.py` 재실행으로 `unpack/data/Font_DOM.nbfc` 재생성
완료 (바이트 덤프로 390/391/422/423 전부 all-zero 확인). `webtool/`쪽은
코드만 고쳐두면 다음 빌드부터 자동 반영(`applyFontArt()`가 매 빌드 시
`build/`에 새로 렌더링하므로 기존 프로젝트 데이터 재작업 불필요).

재생성한 폰트를 `temp/variable_len_test/unpack/data/Font_DOM.nbfc`에도
복사해 7차 테스트 롬을 다시 빌드:
`temp/variable_len_test/test_shrink33_pad00ca_before485c_fontfix.nds`
(16,789,504 bytes). **남은 작업**: 사용자가 melonDS로 이 최종본을 실행해
(1) "빨리 가자" 텍스트와 그 뒤 패딩 자리가 전부 깨끗한 빈칸으로 보이는지,
(2) 빈 페이지 없이 한 번에 넘어가는지, (3) block 34 이하가 정상인지 확인
필요.

## `_fontfix` 실기 결과: 동일 증상 재현 - 타일 제로클리어는 원인이 아니었음 (2026-08-10)

사용자가 `_fontfix.nds`를 melonDS로 실행한 결과, **수정 전과 완전히 동일한 깨진 문자**가
나타남("빨리를가자를를를를를를를를" - "빨리 가자" 사이 공백과 뒤따르는 9개 패딩
자리 전부에 반복되는 "를" 모양 글자). 팩 직후 `NitroPacker unpack`으로 이 ROM의
`Font_DOM.nbfc`를 직접 재추출해 타일 390/391/422/423이 전부 0으로 비어 있음을
바이트 단위로 재확인했고(빌드/패키징 단계의 실수 아님), `font_map_kr.json`도
스캔해 이 4타일에 걸리는 다른 한글 코드가 없음을 확인했다 - 즉 **타일 데이터
자체는 전부 검증상 정상인데 화면은 계속 틀림**.

`ANALYSIS_NOTES.md`를 거슬러 올라가 보니 이 정확히 같은 증상이 2026-08-06~07
세션에서도 있었다("`0x00A4` 반각 공백 오식별" 절, "v5"~"v8" 빌드 기록):
- v5(2026-08-07)에서 이미 "half-width 코드가 full-width 코드와 타일을 공유하면
  2x2 블록 4개를 전부 지워야 한다"는 동일한 원칙이 확립되고 수정됐던 이력이 있음
  (이번 세션에서 발견한 "타일 422 누락" 버그는 이 v5 수정이 이후 어느 시점에
  **퇴행(regression)**해서 다시 2개만 지우는 코드로 되돌아가 있었던 것으로 보임).
- v8(2026-08-07)에서 폰트를 NotoSansKR로 교체한 뒤 **완전히 같은 클래스의 버그**가
  재발함 - 공백 자리마다 "진"이라는 글자가 반복 출현. 그때도 (1)소스 파일 동일성,
  (2)토큰 스트림 디코딩, (3)패킹된 ROM에서 직접 재추출한 타일 제로 확인, (4)다른
  코드와의 타일 충돌 전수 스캔 - 4가지를 전부 재검증했지만 전부 정상으로 나왔는데도
  화면은 계속 틀렸음. 결론: "타일 주소 공식(`0x00CA`→base tile 390) 자체가 틀렸을
  수 있다"는 가설을 세우고, `temp/probe_space_tile_marker.py`(2x2 블록 전체를
  인덱스 15로 꽉 채운 검은 사각형으로 덮어써서 주소 공식 자체를 검증하는 진단
  프로브)를 만들어 사용자에게 전달했으나, **그 프로브의 결과가 기록에 전혀 남아있지
  않음** - 이후 세션이 폰트 굵기/오버랩 이슈로 방향이 바뀌면서 이 질문 자체가
  결론 없이 묻힌 것으로 보임.

**결론**: 이번 세션의 타일 422 누락 수정은 (a) v5에서 이미 검증된 원칙과 정확히
일치하고, (b) 실제로 존재했던 퇴행 버그이므로 유지해야 하지만, (c) 이 버그 하나가
"를"/"진" 반복 출현 증상의 진짜 원인은 아니었다. 2026-08-07에 결론 내지 못했던
바로 그 진단(타일 주소 공식 자체가 맞는지)을 지금 다시 수행해야 한다.

## 8차: 공백 타일 주소 공식 검증용 검은 사각형 프로브 (2026-08-10)

`temp/probe_space_tile_marker.py`를 새로 작성(과거 버전은 temp/라 세션 간 보존되지
않아 유실됨) - `temp/variable_len_test/unpack/data/Font_DOM.nbfc`(이미 위 타일
제로클리어 수정이 반영된 상태, block 33 축소+0x00CA 패딩 테스트 트리 재사용)의
타일 390/391/422/423 전체를 인덱스 15(잉크 최대값)로 꽉 채운 완전한 검은 사각형
패턴으로 덮어씀. 목적: 우리가 믿는 `0x00CA`→tile 390, 2x2 블록 {390,391,422,423}
이라는 주소 공식이 실제로 엔진이 참조하는 타일과 일치하는지 실기로 직접 확인.

- melonDS에서 이 사각형이 "빨리 가자" 사이 공백과 패딩 자리에 **그대로(검은
  네모)** 보이면 → 주소 공식은 맞고, "를"/"진" 글리프는 다른 경로(유력 가설:
  글자 폭/advance 테이블 오류로 인접 글자가 겹쳐 보임)에서 온 것.
- 여전히 "를"/"진" 같은 글자 모양이 보이면 → 주소 공식 자체가 이 코드에 대해
  틀렸다는 뜻 - 엔진이 여기서 다른 타일 변환을 쓰고 있다는 것이므로 완전히 새로운
  주소 가설이 필요.

빌드: `./NitroPacker pack -p temp/variable_len_test/unpack/DAYSOFMEMORY.json -r
temp/variable_len_test/test_space_tile_probe.nds` → 16,789,504 bytes 성공.
**남은 작업**: 사용자가 melonDS로 `test_space_tile_probe.nds`를 실행해 "빨리 가자"
직후 공백/패딩 자리에 무엇이 보이는지(검은 사각형 vs 여전히 글자 모양) 보고 필요 -
이 결과가 2026-08-07부터 미결로 남아있던 질문에 대한 최초의 실기 답이 된다.

## 8차 실기 결과: 검은 사각형 프로브 실패 - `0x00CA`의 타일 주소 공식 자체가 틀림 (2026-08-10)

사용자가 `test_space_tile_probe.nds`를 실행한 결과, 타일 390/391/422/423을 전부
인덱스 15(완전 검은 사각형)로 덮어썼음에도 화면에는 **여전히 "를" 모양 글자가
그대로** 나옴 - 검은 사각형은 전혀 보이지 않음. 이는 결정적 증거: **엔진이
`0x00CA`를 표시할 때 타일 390(혹은 그 2x2 블록)을 전혀 읽지 않는다.** 즉
`font_map_full.json`의 half-width 공식(`tile=2*low-14`, bank=0, low>=0x80)은
이 코드에 대해 틀렸다.

**추가 조사로 밝혀진 사실**:
- `font_map_full.json`을 스캔해보니 `kind=half`인 38개 코드 중 **`00CA`를 제외한
  37개 전부 `real_tile: None`**(자동 매핑 스크립트가 애초에 half-width 코드의
  실제 타일을 계산/검증한 적이 없음 - count가 매우 큰 것들, 예: `0087`
  count=600, `0092`('1') count=239 등 실제로 코퍼스에서 대량 사용되며 검증
  가능했던 코드들조차 `real_tile`이 비어 있음). `00CA`의 `real_tile:390`만
  2026-08-06 세션에 수동으로 끼워 넣어진 값 - **공식 자체가 half-width
  코드에 대해 한 번도 실제로 검증된 적이 없었다는 뜻**.
- `0xA002`(전각 공백, `SPACE_TOKEN`)도 `real_tile:10242`이지만 이 값은
  full-width 공식(`tile=v`)으로 역산되지 않는 값(`v=0xA002=40962`는 공식대로면
  범위 초과라 `ctrl`이어야 함) - 즉 이것도 공식 산출이 아니라 수동/휴리스틱
  패치로 보임. 다만 `translate_io.py` 주석과 `ANALYSIS_NOTES.md` 1172~1180행에
  따르면 **2026-08-05~06, "블록 길이 고정" 규칙을 처음 확립한 실기 성공
  테스트가 바로 이 `0xA002`를 패딩으로 사용했다** - 오늘(2026-08-10) "반각이
  더 자연스럽다"는 사용자 요청으로 `HALF_SPACE_TOKEN=0x00CA`로 전환한 것이
  이번 세션의 최초 변경점이고, 그 직후부터 "를" 반복 버그가 나타났다.

**결론**: `0x00CA`를 공백/패딩에 쓰는 방식 자체가 애초에 실기 검증 없이 도입된
가정이었고, 지금 실기로 명확히 반증됐다. `0xA002`(전각)는 최소한 과거 성공
사례가 있으므로 다음 우선순위로 검증할 대상.

## 9차 테스트: 패딩을 다시 `0xA002`(전각 공백)로 되돌려 비교 (2026-08-10)

`temp/test9_full_space_probe.py` 신규 작성 - block 33 축소("빨리 가자") 재사용,
단 `text_to_tokens()`의 `" " -> HALF_SPACE_TOKEN` 강제 매핑을 우회하기 위해
글자만 개별 인코딩하고 공백/패딩 9개 전부 `tio.SPACE_TOKEN`(0xA002)을 수동
삽입: `[빨,리,0xA002,가,자,0xA002×9,0x485C]` (총 15토큰, 원본과 동일, 폰트
타일은 건드리지 않음 - `0xA002`의 실제 blank 여부 자체를 검증하는 게 목적).

빌드: `./NitroPacker pack -p temp/test9_full_space/unpack/DAYSOFMEMORY.json -r
temp/test9_full_space/test9_full_space.nds` → 16,789,504 bytes 성공.
**남은 작업**: 사용자가 melonDS로 이 ROM을 실행해 "빨리 가자" 뒤 공백/패딩 자리가
깨끗한 빈칸으로 보이는지, 아니면 `0x00CA`와 같은(혹은 다른) 깨진 글자가 나오는지
확인 필요. 여기서 만약 `0xA002`도 깨지면 "공백류 코드는 이 폰트 엔진에서
전부 신뢰 불가"라는 훨씬 근본적인 문제가 되고, 정상이면 즉시 프로덕션 패딩
토큰을 `0xA002`로 되돌리는 게 다음 조치가 된다.

## 9차 실기 결과: `0xA002` 정상 확인 - "를" 반복 버그 근본 원인 최종 규명 및 프로덕션 복구 (2026-08-10)

사용자가 `test9_full_space.nds`를 melonDS로 실행한 결과: **"깨끗한 공백으로
나와"** - `0xA002`(전각 공백)는 정상적으로 빈칸으로 렌더링됨. 이로써 8차 실기
결과(검은 사각형 프로브 실패)와 합쳐 이번 세션 최대 미스터리("를" 반복 버그)의
근본 원인이 최종 확정됨:

- **원인이 아니었던 것**: `_fontfix` 타일 제로클리어 버그(0x00CA가 공유하는
  0x0606 타일 블록의 4개 서브타일 중 2개만 지우던 문제) - 이건 실재하는 버그였고
  수정도 유효하지만, "를" 증상과는 무관했다(수정 후에도 증상 100% 동일 재현).
- **원인이 아니었던 것**: 패딩 위치(0x485C 앞/뒤) - 이미 5~7차 테스트에서 별개
  문제로 올바르게 해결됨.
- **진짜 원인**: 이번 세션 초반 "반각 공백이 한글에 더 자연스럽다"는 사용자
  요청으로 패딩/공백 인코딩을 `SPACE_TOKEN=0xA002`(전각, 과거 2026-08-05~06
  실기 검증됨)에서 `HALF_SPACE_TOKEN=0x00CA`(반각, **실기 검증 없이 도입**)로
  전환한 것. `font_map_full.json`에도 이때 `00CA`에 `real_tile:390`이 검증
  없이 채워짐. `0x00CA`의 타일 주소 공식은 8차 실기(검은 사각형 프로브)로
  명백히 반증됨 - 엔진은 이 코드에 대해 타일 390을 전혀 읽지 않고 대신
  "를" 모양으로 보이는 미상의 다른 경로를 참조한다.

**조치 - 프로덕션 코드 전체 복구** (`0xA002`/`SPACE_TOKEN`을 유일한 공백/패딩
토큰으로 되돌리고 `HALF_SPACE_TOKEN`/`0x00CA` 완전 제거):
- `analysis/translate_io.py`: `HALF_SPACE_TOKEN` 삭제, `SPACE_TOKEN=0xA002`만
  유지, `_CHAR_TO_CODE[" "]`를 `SPACE_TOKEN`으로, 공백에 대한 반각 우선 배정
  규칙 제거(`:`/숫자/영문자는 반각 우선 유지). 오늘의 조사 경위 전체를 주석으로
  남김.
- `analysis/mes_translate_reinsert.py`: 패딩 `pad = [tio.HALF_SPACE_TOKEN] * ...`
  → `[tio.SPACE_TOKEN] * ...`.
- `webtool/server/lib/translateIo.js`: Python과 동일하게 `HALF_SPACE_TOKEN` 상수
  및 export 제거, `SPACE_TOKEN=0xa002`만 유지, `_CHAR_TO_CODE.set(" ", ...)`도
  `SPACE_TOKEN`으로.
- `webtool/server/lib/pipeline.js`: `validateRow()`와 `buildFileTokens()` 두
  곳의 패딩 `fill(tio.HALF_SPACE_TOKEN)` → `fill(tio.SPACE_TOKEN)`. (단
  `applyFontArt()`의 `HALF_SPACE_TILE=390`/`FULL_SPACE_TILE=10242` 4-서브타일
  제로클리어 로직 자체는 그대로 유지 - 이건 어느 토큰을 쓰든 0x0606 공유 타일의
  잔여 잉크를 막기 위해 여전히 필요한 별개의 유효한 수정.)
- `analysis/font_map_full.json`: `00CA` 엔트리에서 검증 안 된 `real_tile:390`
  필드를 제거(`{"kind":"half","count":0,"char":" "}`만 남김 - 2026-08-07
  이전의 정직한 상태로 복원). **CLAUDE.md 규칙 4에 따라 이 파일은 순수 원문
  매핑 전용이므로 한글 작업과 무관하게 항상 정확해야 함.**
- `analysis/font_map_kr.json`: `rebuild_font_map_kr.py` 재실행으로 `00CA` 필드
  변경 전파. 한글 슬롯 1558/1559로 예산 범위 내 유지 확인(변화 없음, 규칙 4
  준수).
- `unpack/data/Font_DOM.nbfc`: `apply_font_art.py` 원본에서부터 재생성.
  "1558 Hangul glyphs 렌더링, 0 missing" 확인 - 타일 390/391/422/423은
  스크립트에 하드코딩된 제로클리어 로직 덕분에 여전히 제로 상태 유지.
- `temp/test_variable_block_length.py` (기존 "7차" 테스트 스크립트): 삭제된
  `tio.HALF_SPACE_TOKEN` 참조로 `AttributeError` 발생 - `PAD_TOKEN`을
  `tio.SPACE_TOKEN`으로 수정 후 재실행 성공(`block 33: 15 -> 15 tokens (delta
  +0)`, 파일 전체 6948 delta 0).
- 커밋된 실제 프로덕션 데이터(`unpack/data/Script/*.mes`) 전체를 `0x00CA` 존재
  여부로 스캔 - **0건 확인**. 오늘의 반각 공백 실험은 `temp/`와 webtool의
  프로젝트별 `build/` 산출물에만 영향을 미쳤고 커밋된 원본 코퍼스는 오염되지
  않았음.

`temp/variable_len_test/unpack/`을 복구된 파이프라인으로 재생성한 뒤
`./NitroPacker pack -p temp/variable_len_test/unpack/DAYSOFMEMORY.json -r
temp/variable_len_test/test_final_space_token_fix.nds` 빌드 완료(16,789,504
bytes). 이 ROM은 "7차 테스트" 시나리오(block 33 축소 + `0x485C` 앞 패딩)를
`0xA002` 기반 복구 파이프라인으로 재현한 것.

**최종 실기 확인 완료(2026-08-10)**: 사용자가 `test_final_space_token_fix.nds`를
melonDS로 실행 - "정상 확인됐어, 34번도 정상 진행돼". block 33("빨리 가자",
9토큰 패딩 포함 15토큰)이 깨진 글자 없이 깨끗하게 표시되고, 뒤이은 손대지 않은
block 34도 정상 진행됨(오프셋 밀림/2차 오염 없음). 이로써 2026-08-07부터 여러
세션에 걸쳐 미결이었던 "를"/"진" 반복 글자 버그가 **완전히 종결**됨 - 근본 원인은
검증 안 된 `0x00CA`(반각 공백) 도입이었고, 검증된 `0xA002`(전각 공백)로 복구한
프로덕션 파이프라인이 실기에서 정상 동작함을 최종 확인. 앞으로 공백/패딩 인코딩은
`SPACE_TOKEN=0xA002`만 사용하며, 반각 공백(`0x00CA` 등 `real_tile`이 검증되지
않은 half-width 코드 전반)은 실기 검증 없이 다시 채택하지 말 것.

## 10차: CSV 미해독 코드 고빈도 항목 식별 시도 (2026-08-10)

사용자 질문("반각문자는 다 확인된게 맞나? csv 보면 반각문자나 아직 못찾은
한문이 보이던데")을 계기로 `temp/translations/*.csv`를 스캔한 결과, 반각(half)
38개 코드 중 26개, 전각(full) 코드 중 1891개(코퍼스 4922회 출현)가 여전히
`char` 미배정 상태임을 확인. "고빈도 항목부터 식별 시도"로 진행.

**방법론 정정**: 기존에 반각 미해독 코드도 `tile=2*low-14` 공식으로 렌더링해서
식별하려 했으나, 이미 `char`가 확정된 반각 코드(`0091`='0', `0084`=':')를 같은
공식으로 렌더링해보니 숫자/콜론과 전혀 다른 깨진 모양이 나왔다 - 즉 이 공식은
`0x00CA` 사례(위 9차 기록)뿐 아니라 반각 전반에서 신뢰할 수 없음이 재확인됨.
→ **반각 미해독 코드는 타일 렌더링으로 식별 불가**, 코퍼스 문맥 대조로만
접근해야 함(`0083`/`0084`/`009B`/`009C` 확정 당시와 같은 방식). 이번 세션에선
시간 관계상 착수하지 못함 - 최우선 후보는 `0087`(600회), 다음 `008F`(46회),
`0086`(22회).

반면 전각 코드는 `real_tile`(build_font_map_v2.py가 실측 검증한 필드)로 렌더링한
결과가 신뢰할 수 있음을 재확인(`0306`=`…`을 정확히 렌더링). 조사 도구
`temp/render_font_tile.py` 신규 작성(1회성, CLAUDE.md 규칙 3에 따라 `temp/`에
위치) - `PYTHONPATH=analysis python3 temp/render_font_tile.py <HEX>...`로
`real_tile`이 있으면 그 주소를, 없으면 공식 예측 주소를 렌더링해 16배 확대
PNG로 저장.

고빈도 미해독 전각 코드를 이 도구로 렌더링/육안 확인한 결과:

- **`040E`(167회) = `★`(별표)로 확정**. real_tile=270 렌더링 결과가 별 모양과
  완전히 일치, 다른 해석의 여지가 없는 명확한 그림 문자. `font_map_full.json`
  반영 완료.
- **`0216`(146회) = `：`(전각 콜론)로 확정**. 픽셀 증거(세로로 쌓인 두 개의
  작은 사각형, 콜론의 전형적 모양)와 코퍼스 문맥 증거(`誕生日<0216>10月11日`,
  `血<1B18>型<0216>AB型`, `身<020A>長<0216>154...`, `体<020A>重<0216>語らず`
  등 - 모두 "라벨<0216>값" 패턴)가 서로를 뒷받침해 이중으로 확인됨.
  `font_map_full.json` 반영 완료.
- **`0186`(3회) = `…`로 확정**. `real_tile=198`이 이미 확정된 `0306`(=`…`)의
  `real_tile`과 **완전히 동일** - 육안 픽셀 해석이 아니라 필드값 자체가
  일치하는 구조적 증거이므로 신뢰도 높음. 코퍼스 출현 3회뿐이라 `0306`의
  드물게 쓰이는 별칭/중복 코드로 추정. `font_map_full.json` 반영 완료.
- **`019E`(103회) = 보류(미확정)**. real_tile=222 렌더링 결과는 대각선
  교차(×) 모양이 매우 선명하고 명확하지만, 코퍼스 문맥이 항상
  `身長<0216>154<0195><019E><6E5C>` 처럼 키 값 뒤 숫자 바로 다음(단위 자리로
  추정되는 위치, `cm` 가능성)에 고정 등장해 픽셀 증거와 문맥 증거가 서로
  어긋남. 억지로 짜맞추지 않고 미배정 유지.
- **`0195`/`0199`/`019C`(각 51/78/36회) = 보류(미확정)**. `身長<0216>154
  <0195><019E>`, `体重<0216>46<019C><0199>` 패턴으로 볼 때 신장/체중 뒤에
  고정으로 붙는 단위(`cm`/`kg`) 후보로 추정되나, 렌더링된 픽셀 모양(작은 점 +
  L자형 조각)이 로마자로 뚜렷이 읽히지 않아 확정하지 않음.
- **`0189`/`018A`/`0185`/`0180`/`018B` 군 = 보류(미확정)**. 한 사례
  (`<0189><018A>テーマ` = "OP テーマ")만 보면 `0189`='O', `018A`='P'로
  보이지만, 코퍼스 전체에서는 `<0189><018B><0189>` 3코드 조합이 여러 파일에
  걸쳐 훨씬 더 자주(장식/구분자로 추정되는 패턴으로) 반복 등장해 "OP" 가설과
  충돌함. 추가 코퍼스 분석 없이는 판단 보류.

**결론**: no-guessing 원칙에 따라 픽셀·문맥 증거가 확실히 일치하는 3건(`040E`,
`0216`, `0186`)만 `font_map_full.json`에 반영했고, 증거가 부족하거나 상충하는
나머지(`019E`, `0195`, `0199`, `019C`, `018x` 군)는 미배정으로 남겨둠. 남은
작업: 반각 `0087`(및 `008F`, `0086`) 코퍼스 문맥 조사, `019E`/`018x` 군 심화
조사(추가 코퍼스 표본 필요).

## 11차: 실기(melonDS) 직접 렌더링으로 미확인 코드 40개 일괄 확인 (2026-08-10)

### 방법론 — 실기 렌더링이 최상위 증거로 확인됨

10차에서 보류됐던 `019E`/`0195`/`0199`/`019C`/`018x` 군을 포함해 `char` 미배정
고빈도 코드 상위 40개(`kind` in full/half, `count` 내림차순)를 한 번에 확정하기
위해, 오프라인 픽셀 추정(`temp/render_font_tile.py`)이나 코퍼스 문맥 추론 대신
**실제 게임 화면에 직접 그려서 육안 확인**하는 방법을 사용했다.

- 도구: `temp/probe_unknown_codes_ingame.py` (신규, `temp/`에 둠 - 1회성 조사
  스크립트).
- 오프닝 `dom1OP_0701_1.mes`의 블록 71(코퍼스 전체에서 가장 긴 실제 대사
  블록, 원본 107토큰)의 내용을 통째로 "코드0 SPACE 코드1 SPACE 코드2 ..."
  형태로 교체 - 후보 코드 사이마다 이미 검증된 `SPACE_TOKEN`(0xA002, 전각
  공백)을 넣어 화면에서 글자 경계를 쉽게 구분.
  블록 길이는 원본과 동일(107토큰)하게 유지, 부족분은 트레일링 `0x485C`
  바로 앞에 `SPACE_TOKEN`으로 패딩(프로덕션 `pipeline.js`와 동일 관례).
- **중요 - 반드시 `unpack_origin/`(pristine 원본)에서 빌드해야 함**: 처음
  `unpack/`(이미 한글 폰트 패치된 개발 트리)으로 빌드했다가 사용자가
  "원본롬으로 봐야하는데 이건 한글폰트로 되어있잖아" 라고 정정함.
  `cmp unpack/data/Font_DOM.nbfc unpack_origin/data/Font_DOM.nbfc`로 두 폰트
  파일이 실제로 다름을 확인 후 `SRC_UNPACK`을 `unpack_origin/`으로 수정,
  재빌드. **앞으로 원본(패치 전) 폰트/동작을 확인하는 모든 1회성 테스트는
  반드시 `unpack_origin/`을 소스로 써야 한다** - `unpack/`은 이미 개발 중인
  한글 패치 트리이므로 원본 검증용으로 쓰면 안 됨(이번처럼 잘못된 결론을
  낼 위험).
- 사용자가 melonDS로 실행한 뒤 대사창 스크린샷 3장(페이지 넘김 3회분)을
  제공, 원본 크기(736x1048) 스크린샷을 크롭 + PIL `NEAREST` 확대(6~16배) +
  일부는 빨간 격자선 오버레이로 글자 경계를 정밀 대조해 40개 코드를 전부
  판독했다.
- **판독 순서 검증**: `content = [code0, SPACE, code1, SPACE, code2, ...]`
  구조이므로 화면에 보이는 "글자,幡,글자,幡,글자..." 순서를
  `pick_candidates()`가 반환하는 코드 목록 순서(count 내림차순)와 1:1
  대응시켜 정확히 매핑했다. (주의: 최초 시도에서 줄바꿈 위치를 착각해
  일부 코드를 한 칸씩 밀려 잘못 매핑했었음 - 전체 3페이지를 처음부터 다시
  이어 붙여 재검증한 뒤 확정.)

### 확정 결과 (40개 전부 판독, 39개 `font_map_full.json`에 `char` 반영)

| 코드 | char | 코드 | char | 코드 | char | 코드 | char |
|---|---|---|---|---|---|---|---|
| 0087 | `_` | 019A | `h` | 0286 | `w` | 1B18 | `夜` |
| 0080 | (빈칸, 미배정) | 0187 | `M` | AD12 | `閉` | BB14 | `也` |
| 0189 | `O` | 019F | `『` | 019C | `k` | 0416 | `1` |
| 018C | `S` | C706 | `泉` | 0181 | `G` | 009E | `D` |
| 019B | `i` | 031A | `』` | 0082 | `.` | 0418 | `2` |
| 019E | `m` | 0195 | `c` | 018A | `P` | 070C | `i`(중복) |
| 0280 | `o` | 0183 | `l` | | | 0308 | `'` |
| 0199 | `g` | 008F | `#` | | | 0086 | `!` |
| 009D | `C` | 0180 | `F` | | | 5A00 | `鹿` |
| 018B | `R` | 009F | `E` | | | | |
|      |      | 0285 | `u` | | | | |
|      |      | 018D | `T` | | | | |
|      |      | 0185 | `K` | | | | |

- **`0080`(198회)은 실기에서 완전히 빈칸(잉크 없음)으로 렌더링됨** - 다른
  글리프들 사이에서 명확히 빈 공간으로 확인(`_幡␣幡O...`처럼 두 `幡` 사이가
  비어 보임). 다만 이것이 의미상 "공백" 문자인지, 아니면 단순히 이 코드에
  대응하는 폰트 아트가 그려지지 않은 미사용 슬롯인지는 알 수 없어
  `font_map_full.json`에 `char`를 배정하지 않고 미배정으로 남김(no-guessing
  원칙).
- **10차에서 보류했던 `019E`/`0195`/`0199`/`019C` 단위 가설이 실기로 완전히
  확인됨**: `0195`=`c`, `019E`=`m` → "cm", `019C`=`k`, `0199`=`g` → "kg".
  10차 코퍼스 문맥(`身長<0216>154<0195><019E>` = "신장：154cm",
  `体重<0216>46<019C><0199>` = "체중：46kg")과 정확히 일치 - 문맥 추론과
  실기 렌더링이라는 서로 다른 두 증거가 교차 확인됨.
- **`0189`/`018A`/`0185`/`0180`/`018B` 군도 확정**: `0189`=`O`, `018A`=`P`,
  `0185`=`K`, `0180`=`F`, `018B`=`R`. 10차에서 우려했던
  "`<0189><018B><0189>` 반복 패턴과 'OP' 가설 충돌" 문제는, 실제로는
  이 코드들이 알파벳 낱자 그 자체(디버그/이름 입력용 라틴 문자 세트로
  추정)이므로 `<0189><018B><0189>`="ORO" 같은 장식적 반복도 얼마든지
  자연스럽고, 개별 발견(`<0189><018A>テーマ`="OP テーマ")과 전혀 모순되지
  않음이 확인됨.
- **`C706`(52회) = `泉`으로 확정**: 렌더링된 글리프 자체는 획이 조밀해
  단독으로는 확신하기 어려웠으나, 코퍼스 전체에서 예외 없이
  `清<C706>学園`/`清<C706>大学` 패턴으로만 등장 - "세이센 학원/대학"
  (일본 실존 학교명 패턴 "清泉"과 정확히 일치)이라는 강한 문맥 증거와
  픽셀 모양(白 위 水 형태와 부합)이 서로 뒷받침해 확정.
- **`070C`(23회)는 `019B`(110회)와 화면상 완전히 동일한 소문자 `i` 모양으로
  렌더링됨** - 서로 다른 코드가 시각적으로 구별 불가능한 중복 글리프인
  것으로 보임(둘 다 `char`="i"로 반영, 추후 반각/전각 구분 등 실제 차이가
  있는지는 미해결).
- **`0183`(50회) = `l`(소문자 L)로 잠정 확정, 신뢰도 중간**: 렌더링은 단순
  세로 막대 하나뿐이라 소문자 `l`/대문자 `I`/파이프 `|` 중 픽셀만으로는
  완전히 구별 불가. 다만 이미 확정된 숫자 `1`(`0416`)은 상단에 작은 세리프
  획이 있는 별개 모양임을 확인했으므로 최소한 숫자 `1`은 아님. 주변 코드들이
  대부분 라틴 대소문자 세트로 보이는 정황상 소문자 `l`로 배정.
- **`5A00`(21회) = `鹿`(사슴)으로 확정**: 3번째 페이지(트레일링 패딩)로
  넘어가기 직전, 2페이지 마지막 줄에서 명확한 사슴 한자 형태로 확인됨.

### 중대 발견 - `temp/render_font_tile.py`(오프라인 `real_tile` 픽셀 추출)가 실기와 어긋나는 사례 확인

- `019E`의 경우 오프라인 `real_tile` 기반 픽셀 렌더링은 선명한 "×"(대각선
  교차) 모양을 보여줬으나, 실기 렌더링은 명확히 소문자 `m`이었다(위 "cm"
  확정과 일치). 즉 **오프라인 도구의 `render_full()`가 가정하는 타일 주소
  공식(`v, v+1, v+32, v+33` 2×2 서브타일 배치)이 이 코드 범위에서는 실제
  타일시트 레이아웃과 맞지 않을 가능성**이 있다 - 정확한 원인(타일시트 폭
  가정 오류? 오프셋 계산 오류?)은 아직 규명하지 않았다.
- 이번 40개 중 다른 다수가 `real_tile` 필드를 갖고 있었음에도(`0189`,
  `018C`, `019B` 등) 실기 판독 결과가 자연스러운 라틴 문자/한자로 일관되게
  나온 것을 보면, `019E` 하나만의 국소적 문제일 가능성이 높다 - 그러나
  향후 `real_tile` 기반 오프라인 판독 결과는 실기로 재확인하기 전까지는
  "잠정"으로 취급할 것.
- **추가 이상 징후**: `019A`(=`h`, real_tile=218)와 `031A`(=`』`,
  real_tile=218)가 JSON상 완전히 동일한 `real_tile` 값을 갖고 있음에도
  실기에서는 서로 다른 글리프(`h` vs `』`)로 렌더링됨 - `real_tile` 필드
  자체가 이 두 코드 중 최소 하나에 대해 잘못 기록되어 있을 가능성이 있다.
  이 필드는 `font_map_kr.json`이 아닌 `font_map_full.json`(원문 디코딩용)
  소속이라 한글 폰트 렌더링에는 영향 없지만, 향후 이 필드를 다른 용도로
  신뢰해 사용할 경우 주의가 필요함(규명은 이번 범위 밖, 후속 과제로 남김).

### 남은 항목

- `0080`(198회)의 정체(진짜 공백인지 미사용 슬롯인지) - 미배정 유지.
- `0183`(50회)이 `l`/`I`/`|` 중 정확히 무엇인지 - 낮은 우선순위(한글화에는
  불필요, 원문 디코딩 정확도 문제일 뿐).
- `019A`/`031A`의 `real_tile` 중복 기록 오류 원인 규명 - 후속 과제.
- `font_map_full.json`에 반영: 위 표의 39개 코드(`0080` 제외) 전부 `char`
  필드 추가 완료.

## 12차: `0087`/`008F`/`0086` 코퍼스 문맥 검증 + 부수 발견 3건 (2026-08-10)

11차에서 실기 렌더링만으로 확정한 40개 코드 중 `0087`(`_`)/`008F`(`#`)/
`0086`(`!`) 세 개에 대해, "실제 대사 블록에서 어떻게 쓰이는가"를 코퍼스
전수 검색으로 교차검증했다. 방법: `mc.find_dialogue_blocks()` +
`mte.is_debug_menu_block()`으로 실제 대사 블록만 골라낸 뒤 대상 코드가
그 안에 등장하는지 전수 스캔.

### `008F` = `#` — 확정 (실기 판독과 일치)

`dom1Jenny_O0729_2.mes`, `dom1Jenny_O0731_3.mes` 등 Jenny의 음악 관련
대사에서 `C#m7`, `A#m7`, `G#m7`, `AM7` 같은 코드 진행(chord) 표기로
반복 등장 (예: `'F#m7<0080>G#m7C#m7<0080>G#m7...'`) - 음악 샵(#) 기호
용법과 완전히 일치. 실제 대사 블록 내 등장 9회, 전부 이 패턴.

### `0086` = `!` — 확정 (실기 판독과 일치)

`common.mes`의 구두점 목록(`、.!?!<0085>)`)과 여러 엔딩 파일
(`dom3Shiki_ending1.mes`, `dom3ED_HaoUkyo.mes`, `dom3Mina_ending1.mes`,
`dom3ED_Rimururu.mes`, `dom3ED_Kyoemon.mes`)에서 공통으로 등장하는
`'……あ!|いっけない!<485C>'`("아차!" 계열 대사)의 느낌표 자리에 정확히
사용됨. 실제 대사 블록 내 등장 26회, 전부 표준 느낌표 용법과 일치.

### `0087` — **반례 발견, 실기 판독 결과를 되돌림**

코퍼스 전체(전 `.mes` 파일)에서 `0087`은 총 608회 등장하지만, 그중
**실제 대사 블록 안에서 등장한 횟수는 0회**다. 전량이 대사 블록 바깥의
구조적/헤더성 바이트열(고정 시퀀스 `1002`/`AB0A` 직후에 오는 ~22토큰
주기의 반복 레코드 등)에서만 나타난다. 즉 11차의 실기 판독(`_`로
보임)은 이 코드를 인위적으로 가짜 대사 블록에 강제로 끼워넣었을 때
"깨지지 않고 `_` 모양으로 그려진다"는 것만 보여준 것이지, 게임이 실제로
이 코드를 인쇄 가능한 대사 문자로 쓴다는 근거가 되지 못한다 - 오히려
코퍼스 실사용 패턴은 이 코드가 순수 제어/헤더 필드임을 강하게 시사한다.

**조치**: `font_map_full.json`의 `0087` 항목에서 `"char": "_"` 필드를
제거하고 미배정 상태로 되돌렸다(`kind`/`count`/`real_tile`은 그대로
유지). 이 코드는 원문 디코딩에서 마주쳐도 인쇄 문자로 취급하지 않는다.

### 부수 발견 3건 (0086 문맥 추적 중 `orochiendroll.mes` 엔딩 크레딧에서 발견)

엔딩 크레딧 파일 `unpack_origin/data/orochiendroll.mes`(참고: `Script/`
하위가 아니라 `data/` 최상위 파일)에 "PRODUCT MANAGER", "ASSISTANT",
"MARKETING ASSISTANT", "CHARACTER DESIGNER", "STAFF" 등 영문 직함이
그대로 텍스트로 들어있어, 미확정 라틴 알파벳 코드들을 스펠링으로
교차검증하는 데 매우 유용했다.

- **`0183`: `l`(소문자 L) → `I`(대문자 I)로 정정.** 11차에서 낮은
  확신도로 `l`/`I`/`|` 중 `l`을 잠정 채택했었는데, 이번에 4개의 독립적인
  스펠링에서 전부 `I`가 맞아야 문맥이 성립함을 확인:
  `dom1OP_0701_1.mes`의 "『NO MUSIC NO …LIFE!』"(MUSIC/LIFE),
  `orochiendroll.mes`의 "ASSISTANT"(`009B 018C 018C 0183 018C 018D 009B
  0188 018D`) 및 "DESIGNER"(`009E 009F 018C 0183 0181 0188 009F 018B`),
  `dom1Athena_O0727_3.mes`에 등장하는 스탭 이름 "HIROKI"
  (`0182 0183 018B 0189 0185 0183`). `font_map_full.json` 반영 완료.
- **`0182` = `H` (신규 정정, 기존 `/` 오배정 수정).** 기존 값 `/`는
  `git log` 확인 결과 이번 세션 이전, 실기 검증 기록이 전혀 없는 아주
  오래된 자동 추출 결과였다. 이번에 `orochiendroll.mes`의
  "C**H**ARACTER"(`009D 0182 009B 018B 009B 009D 018D 009F 018B`)에서
  `0182`가 있어야 할 자리에 있었고, 결정적으로 코퍼스 전체 실제 대사
  블록 49회 전량이 `0189 018B 0189 009D 0182 019B` = "OR OC**H**i" 즉
  게임 타이틀/보스명 **"OROCHI"**를 스펠링하는 고정 패턴이었다(Jenny/
  Athena/Leona 등 다수 파일에서 반복 등장). "HIROKI" 이름에서도 재확인.
  `font_map_full.json` 반영 완료(`/` → `H`).
- **`018E` = `U` (신규 추가, count=8, real_tile=206).** "PROD**U**CT"와
  "M**U**SIC"(`dom1OP_0701_1.mes`, `orochiendroll.mes` 양쪽) 스펠링으로
  확인. `font_map_full.json` 반영 완료.
- **`009B` = `A`**: 이미 이전 세션에 확정되어 있던 값(`font_map_full.json`
  기존 `char: 'A'`)이 이번 "MANAGER"/"ASSISTANT" 스펠링으로 재확인됨 -
  변경 없음.

### 남은 항목 (미착수, 이번 요청 범위 밖)

- `orochiendroll.mes` 크레딧에서 `E<0191>ECUTI<018F>E` = "EXECUTIVE"로
  추정되는 `0191`(count=1)/`018F`(count=2) 두 코드가 미배정 상태로
  남아있음 - 사용자가 요청하지 않아 이번엔 확정하지 않음.
- 스탭 일본어 이름에 등장하는 여러 한자 미배정 코드(`8A0C`, `7F1C`,
  `821C` 등)도 미착수.
