# NitroPacker / Days of Memories 한글화 프로젝트 — 폴더/기록 규칙

이 프로젝트에서 작업할 때는 아래 규칙을 항상 지킬 것. 컨텍스트 컴팩팅으로 대화 기록이 요약되어도
이 파일은 매 세션 자동으로 다시 로드되므로, 규칙 자체를 잊지 않도록 반드시 여기 최우선으로 따를 것.

## 1. 작업 기록은 `analysis/ANALYSIS_NOTES.md`에 남긴다

새로운 발견, 버그 수정, 확정된 코드/문자 매핑, 시행착오 등 "이 세션에서 무엇을 왜 했는가"에 해당하는
내용은 전부 `analysis/ANALYSIS_NOTES.md`에 append할 것. 대화가 끝나고 컴팩팅되면 대화 내용 자체는
사라지므로, 다음 세션에서 참고할 수 있는 유일한 진본은 이 파일과 auto-memory(`project_dom_hangulization.md`)
뿐이다. 작업을 마칠 때마다(중간에 끊기더라도) 그 시점까지의 진행 상황을 이 파일에 기록해둘 것 —
세션이 끝날 때 한 번에 몰아서 쓰지 말고, 발견/수정이 확정되는 즉시 기록.

## 2. 한글패치에 필요한 최종 산출물만 커밋/관리한다

- 파이프라인 스크립트 (`analysis/`): `mes_codec.py`, `speaker_map.py`, `translate_io.py`,
  `mes_translate_extract.py`, `mes_translate_reinsert.py`, `apply_font_art.py`, `lz10.py` 등
- 웹 툴 소스 코드 (`webtool/`): `server/`, `public/`, `package.json`, `tool.sh` 등 핵심 파일 (`node_modules/`, `workspace/`, 로그/임시파일 제외)
- 최종 데이터: `font_map_full.json`, `font_map_kr.json`, `tool.md`, `README.md`
- 기록: `ANALYSIS_NOTES.md`

1회성 조사/디버그 스크립트, 검증용 렌더링 이미지, 실험적 프로브 결과물은 커밋 대상에 두지 말 것.

## 3. 테스트/조사용 임시 자료는 프로젝트 루트의 `temp/`에 둔다 (`analysis/` 안이 아님)

`temp/`는 `analysis/`의 하위 폴더가 아니라 프로젝트 루트(워크스페이스 내 `temp/`,
`unpack/`·`analysis/`와 같은 층)에 둔다. 다음 유형은 전부 여기에 저장한다:
- 코드 확인용 1회성 스크립트 (예: `scan_all_codes_v8.py`, `render_tilestrip.py`)
- 그 스크립트들이 만들어내는 결과물 (`tilestrip_*.png`, `probe_*.png` 등)
- 실험적 프로브, 중간 검증 스크립트

`render_tilestrip.py`처럼 반복적으로 쓰는 조사 도구 자체도 `temp/`에 스크립트로 남겨두고, 매번
만들어내는 출력 이미지도 같은 곳에 둘 것 (분석이 끝났다고 최종 산출물 쪽으로 옮기지 말 것 — 최종
산출물은 위 2번 목록뿐). `temp/`의 스크립트는 `analysis/`의 `mes_codec.py` 등을 import하므로
실행 시 `PYTHONPATH=analysis`를 지정할 것(예: `export PYTHONPATH=analysis && python3 temp/render_tilestrip.py <hex>`).

## 4. 응답은 항상 한글로 한다

이 프로젝트에서 사용자에게 보내는 모든 응답(설명, 요약, 진행상황 보고 등)은 항상 한글로 작성할
것. 코드/파일명/커밋 메시지 등 원래 영문이어야 하는 부분은 예외로 하되, 그 외 대화체 텍스트는
전부 한글로 쓴다.

## 5. 폰트 매핑 테이블 및 한글 타일 예산 엄격 준수

- **원문용/한글용 폰트 맵 분리**:
  - `analysis/font_map_full.json`: 순수 원본 일어/한자 매핑 테이블 (원문 추출 및 디코딩용). **절대 한글 글자를 덮어쓰지 말 것.**
  - `analysis/font_map_kr.json`: 한글 완성형 코드 매핑 테이블 (한글 텍스트 역인코딩 및 타일 렌더링용).
- **한글 타일 예산 = KS X 1001 완성형 전체 2,350자, 안전 기준은 "코드 단위"가 아니라 "real_tile 풋프린트 배타성"**:
  - 2026-08-11 세션에서 안전 판정 기준을 코드 단위(단순 kanji/free-slot 여부)에서 **`real_tile` 그룹 배타성 + 2×2 풋프린트(오프셋 `{0,1,31,32,33}`) 완전 배타성** 검증으로 교체하고, 이를 통해 안전 배정 상한이 1,359자가 아니라 **3,123개**(한자 단독 타일 1,752 + 미판독 단독 타일 1,347, 이후 재검증 수치)까지 늘어난다는 것을 확인함. 이 기준으로 **2,350자 전체**를 실제로 배정 완료했고 melonDS 실기 확인까지 마쳤다 (`analysis/ANALYSIS_NOTES.md`의 "한글 타일 예산 1,558 → 2,350자(완성형 전체) 확장" 및 후속 "글리프 파손(`、`/`「`) 근본원인 규명" 항목 참고).
  - 따라서 **1,559자 상한 및 "2,350자 전체 할당 절대 금지"는 더 이상 유효한 제약이 아니다** (이전 버전의 이 규칙은 폐기됨). 현재 `analysis/font_map_kr.json`은 2,350자(별칭 동기화 포함 실제 매핑 2,353개) 전체가 정식으로 배정되어 있고 이것이 번역 작업의 유효한 한글 문자 풀이다.
  - 한글 폰트 맵을 재생성할 때는 반드시 `analysis/rebuild_font_map_kr.py`(real_tile 풋프린트 배타성 검증 내장)를 통해서만 할 것 — 이 검증을 우회한 수작업 배정은 가나/구두문자/제어코드 파손 사고(2026-08-06, 2026-08-11 두 차례 발생 이력)를 재현할 위험이 있다.
  - 번역 시 실제 사용 가능한 글자 집합은 `analysis/font_map_kr.json`의 `codes`에서 `char`가 한글 완성형 음절인 항목들이다 — 이 안에 없는 한글 음절은 `translate_io.text_to_tokens()`가 인코딩 실패(`no font code assigned`)로 걸러내므로, 번역문 작성 후 반드시 이 인코더로 실제 검증할 것(눈대중으로 "한글이니까 되겠지"라고 가정하지 말 것).

## 6. 캐릭터별 CSV 번역 작업은 기본적으로 병렬(다중 에이전트)로 처리한다

캐릭터별 `dom2_*.csv` 번역처럼 ~100행 단위 배치로 쪼갤 수 있는 작업은, 한 배치씩 순차로 진행하지
말고 처음부터 여러 배치를 동시에 병렬 에이전트에 맡겨 시간을 절약할 것 (Chizuru 배치 11-13을
병렬로 처리해 성공한 이후 확립된 기본 방침).

- 대상 CSV의 서브파일(.mes) 경계(각 파일이 시작하는 row와 block 0 오프셋)를 먼저 스캔해 전체
  오프셋 표를 만들고, 이를 모든 병렬 에이전트에게 그대로 제공한다 (에이전트가 각자 다시 스캔하지
  않도록 — 중복 작업 방지).
- 각 에이전트는 자기 담당 행 범위만 맡되, 공유 파일(`translations/dom2_*.csv`,
  `webtool/workspace/dom1/translations/dom2_*.csv`)은 절대 직접 수정하지 않고 반드시
  `temp/<csv이름>_batchN_work.csv` 같은 개인 사본에서만 작업 후, 최종 검증된
  `{row_index: 번역문}` dict를 텍스트로 반환하게 한다 (동시 쓰기 충돌 방지).
- 오라클(`analysis/mes_translate_reinsert.py`)은 CSV 전체를 검사하므로, 병렬로 진행 중인 다른
  배치의 미번역 행이 스킵으로 뜨더라도 각 에이전트는 자기 담당 범위(오프셋으로 계산한 global row)
  밖의 스킵은 무시하도록 지시한다.
- 모든 병렬 에이전트가 완료되면, 메인 스레드가 순차적으로 각 배치의 dict(또는 작업 사본 파일)를
  실제 공유 CSV 두 트리에 병합하고, 병합 후 전체 CSV에 대해 오라클을 다시 돌려 `0 skipped`를
  확인하고, 두 트리가 byte-identical한지 `diff`로 재확인한다.
- 병합이 끝나면 `analysis/ANALYSIS_NOTES.md`에 배치별 완료 기록을 남긴다 (규칙 1 그대로 적용).

## Why

작업 세션 중간에 컨텍스트가 컴팩팅되면 대화 기록의 세부사항(정확한 코드값, 수정 근거, 검증 과정)이
손실될 수 있다. `ANALYSIS_NOTES.md`와 폴더 구조 자체를 진본으로 유지해야 다음 세션이(혹은 컴팩팅
이후의 같은 세션이) 온전히 이어서 작업할 수 있다. 폴더가 섞이면(임시 파일이 최종 산출물과
뒤섞이면) 어느 게 신뢰할 수 있는 결과물인지 알 수 없게 되어 같은 조사를 반복하게 된다.
