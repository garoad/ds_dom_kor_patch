DOM 한글패치 툴 개발
1. 어느 플랫폼에서도 작업이 가능하도록 nodejs 기반 웹 툴을 만든다.
2. 원본롬을 읽어서 unpack하고 작업이 가능하도록 하고 unpack된 파일을 읽어서 자동으로 번역할 csv를 만든다.
3. csv항목은 file(원본파일 이름),block(대사블록),n_tokens(원본 대사 길이제한),speaker(화자),source(원본대사),translation(번역) 순서로 만든다. csv가 번역작업의 세이브파일이므로 저장,읽기가 가능해야한다.
4. 현재 그래픽작업은 하지 않았지만 참고할 수 있도록 이미지 파일들은 디렉토리 폴더를 탐색해서 미리보기가 가능하도록 한다.
5. 작업된 csv 기반으로 unpack된 파일에 번역된 대사로 원본 대사를 교체하고 최종적으로 nds 파일이 export 되도록 롬빌드 기능도 넣는다.
