DOM 한글패치 툴 개발
- 어느 플랫폼에서도 작업이 가능하도록 Python(Flask) 기반 웹 툴을 만든다. (2026-08-28, 백엔드를 Node.js에서 Python으로 전환)
- 원본롬을 읽어서 unpack하고 작업이 가능하도록 하고 unpack된 파일을 읽어서 자동으로 번역할 csv를 만든다.
  - 원본롬은 drag&drop 및 파일탐색기를 통해 불러온다.
- csv항목은 file(원본파일 이름),block(대사블록),n_tokens(원본 대사 길이제한),speaker(화자),source(원본대사),ai_draft(기계번역),translation(번역) 순서로 만든다. csv가 번역작업의 세이브파일이므로 저장,읽기가 가능해야한다.
  - csv항목은 공용시스템, dom1,2,3 탭이 있고 하위에 각 캐릭터별로 나뉘어있음.
  - 번역탭에는 원본대사를 복사해 올 수 있도록 각 라인에 원본복사 버튼을 넣는다.
  - 대사길이 제한이 있으므로 제한을 넘어가면 수정하도록 경고 표시를 한다. 원본대사보다 짧을 경우 공백문자를 자동으로 넣어 패딩한다.
- 현재 그래픽작업은 하지 않았지만 참고할 수 있도록 이미지 파일들은 디렉토리 폴더를 탐색해서 미리보기가 가능하도록 한다. bin 확장자의 이미지도 있으므로 이것도 미리보기가 가능해야함. 타일이 어긋난 이미지도 정상적으로 볼 수 있어야 한다.
- 작업된 csv 기반으로 unpack된 파일에 번역된 대사로 원본 대사를 교체하고 최종적으로 nds 파일이 export 되도록 롬빌드 기능도 넣는다.
  - 아직 번역이 안되어서 빠진 항목은 스킵한다.

## 이후 추가 구현된 기능 (2026-09-01 기준)

- 여러 프로젝트(롬) 목록을 관리하고 전환할 수 있는 프로젝트 목록/전환 UI 및 API 추가.
- 프로젝트 진행률 대시보드 추가: 총 블록 수, 번역 완료 블록 수, 빌드/출력 파일 존재 여부를 한눈에 표시.
- 전체 CSV 통합 검색 기능 추가: 원문/번역/AI초벌/화자/파일명을 대상으로 검색하고, 결과를 하이라이트해서 해당 파일·블록으로 바로 이동할 수 있음.
- macOS 내장 번역엔진을 이용한 자동 초벌번역 기능 추가, 진행 상황을 실시간 로그로 스트리밍해서 보여줌.
- 번역란에 원본 복사 버튼 외에 "기계번역(AI초벌) 복사" 버튼 추가.
- 카테고리별로 나뉜 CSV들을 ZIP으로 한 번에 다운로드하는 기능 추가.
- 저장 안 된 번역 변경사항이 있을 때 경고 기능 추가: 탭 전환·파일 전환·창 닫기 시 확인창을 띄워 실수로 작업 유실되는 것을 방지.
- 선택지(다중 옵션) 대사 지원: 구분자(백슬래시 또는 원화기호)로 입력하면 옵션별로 자동 분할 인코딩하고 헤더 필드까지 자동 패치.
- 페이지 넘김 제어코드를 CSV 원문/번역에서는 숨기고, 저장 시 자동으로 붙였다 뗐다 처리(작업자가 신경 쓰지 않아도 됨).
- 플레이어 이름 입력 파일 전용 글자수 제한 처리 및 길이 제한이 없는 특정 파일군 별도 처리 추가.
- 디버그 메뉴 전용 대사 블록을 자동 감지해 번역 대상 CSV에서 제외하는 기능 추가.
- 재삽입 시 원문-번역 공통 헤더를 안전하게 보존하기 위한 위험 오퍼코드 감지 및 헤더 스플라이스 안전장치 추가.
- 재삽입 시 커스텀 한글 비트맵 폰트를 폰트 파일에 자동으로 구워 넣는 기능 추가.
- 이미지 실제 교체(리팩) 기능 추가: PNG를 업로드하면 팔레트 근사매칭 후 타일/스크린맵으로 재인코딩해서 롬에 실제로 반영(스펙 초안 당시엔 미리보기만 있었음).
- 여러 PNG를 파일명 기준으로 자동 매칭해서 한 번에 일괄 교체하는 배치 기능 추가.
- `.bin` 확장자 배경 그래픽의 타일/스크린맵/팔레트 3파일 세트를 자동으로 탐지·매칭하는 로직 확장.
- 서버 백그라운드 운영 스크립트(`tool.sh`) 추가: start/stop/restart/status 명령과 PID·로그 파일 관리.
- 제어문자(`<이름>`, `<485C>`, `<6E5C>` 등)의 의미를 설명하는 도움말 패널 UI 추가.
